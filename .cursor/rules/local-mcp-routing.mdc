---
description: 本地 MCP 工具路由——何時用 constitution／recall／local_research／map_reduce
alwaysApply: true
---

# 本地 MCP 路由

優先用 MCP 濃縮／檢索，**不要**為跨檔探問整檔讀入大量原文。

## 決策表

| 需求 | 用 |
|---|---|
| 憲章／生效規格／RULING **精確原文** | `constitution-mcp`（`get_clause`／`get_spec_clause`） |
| 跨檔、不知路徑、要短結論 | `local_research` |
| 已知多個路徑要合併摘要 | `local_map_reduce`（paths≤12） |
| 已知單一 path／貼上長文 | `local_summarize`／`local_extract` |
| 只要片段＋`path:line` 出處 | `project-memory` → `recall`（預設 hybrid） |
| 索引是否新鮮／有 FTS | `memory_status`；缺 FTS 或過舊 → 提示重建 `python3 -m tools.project_memory_mcp index` |

## 硬邊界

- 本地 LLM／語意檢索輸出一律 **[I] 輔助**，**不得**貼入任何 [N] 治理文書。
- 治理權威路徑（`constitution/`、生效 `specs/*-SPECIFICATION.md`）不做 LLM 濃縮；改走 constitution-mcp。
- 深度推理與最終判斷留在 Cursor；本地工具只做大進小出。
