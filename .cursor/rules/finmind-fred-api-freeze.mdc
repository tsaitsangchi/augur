---
description: FinMind／FRED 外部 API 操作凍結（憲章→實作路線圖全部落地前）— sync／probe／放量一律停
alwaysApply: true
---

# FinMind／FRED API 操作凍結（憲章→實作路線圖全部落地前）

> **生效**：2026-07-24 Steward 指示 · **階段**：`constitution-to-implementation` 全部階段落地完成前 · **位階**：[I] 工具護欄（非憲章 [N]）  
> **關鍵詞**：FinMind · FRED · API freeze · 操作凍結 · sync · probe · audit · constitution-to-implementation

## 凍結範圍（禁止，除非用戶明示解凍）

本專案階段**一律不開** FinMind API 與 FRED API。下列一律 **停／不啟動／不續跑**：

- 任何會打 **FinMind** 或 **FRED** 外部網路的：`sync`／`probe`／放量／窄窗／寬窗／`daily_maintenance` audit／heal
- 含用戶先前已授權、但尚未完成者——例如 **Dividend 重建**、**窄窗 audit**（`TaiwanStockDividend` rebuild）→ **暫停**，勿繼續打 API
- `sync_finmind*`、`ingestion/finmind` 實際 fetch、FRED series 拉取、會觸發上述呼叫的 runner／cron／nohup

見 403／ban／額度訊號 → **停、不重試風暴**（與 CLAUDE #24 同精神；本凍結下根本不應開打）。

## 允許（本凍結不擋）

- 純本地 **DB 唯讀**／對帳讀庫／schema 檢查（零外部 API）
- **預測／訓練／回測／切分／推估**——消費庫內已落地 raw／features／panel（as-of）；**凍結 ≠ 不能預測**；**預測拍板／執行不因本凍結而否決**（見 `predict-vs-market-api.mdc`；追溯＝`audits/PREDICT-ORTHOGONAL-API-RULING-20260724.md`）
- **零網路** script、本地 pytest（mock／免 API）
- 計畫書、ultracode、治權理解、報告撰寫、封存（不觸發 FinMind／FRED）
- 常駐本地服務（advisor／chat／admin／Ollama）——**不**經 FinMind／FRED
- R5／路線圖其餘 **零 API** 實作（wiring、AST 閘、本地測試等）——**不解凍 API**

> **交叉一句**：本 rule **只凍取數**；**不解凍**。預測正交定義與追溯 yes **不**改本凍結條件。

## 解凍條件（AI 不自行解凍）

**前提（兩者皆須）**：

1. **憲章→實作路線圖（constitution-to-implementation）全部階段落地完成**——見 `reports/augur_constitution_to_implementation_roadmap_20260724.md`。  
   **「全部落地」正式定義（Steward `INV1-LAND-MECH`，2026-07-24）**＝**機械近程完備** ∧ Steward 書面接受殘留 **partial 另帳**（G-CAT／G-DIV／G-ATTEST／G-HAR／10-14／`evaluated_pass=0`／R6 S3a 等——清單 SSOT＝`audits/ROADMAP-INV1-FULL-LANDING-DEFINITION-20260724.md` §2）。  
   **「計畫落地」≠ 足夠**；**R5 S1–S2／局部階段完成 ≠ 解凍**；**另帳項未清 ≠ 已可開 API**（倒果為因禁止）。拍板登錄＝`audits/ROADMAP-INV1-APPROVED-20260724.md`。
2. 用戶**明示**解凍（下列措辭之一或等價）——**`INV2-THAW-STILL-REQUIRED`**：即使前提 (1) 已依 LAND-MECH 成立，**仍須本句**才開 API：
   - 「憲章→實作路線圖全部落地後解凍」／「constitution-to-implementation 全部完成後再開 FinMind／FRED」
   - 「解凍 FinMind／FRED」／「解凍 API」

**禁止**：以「先前授權仍有效」「窄窗很輕」「只 probe 一下」「某計畫／R5 階段已綠」「所有計畫落地」「INV1-LAND-MECH 已拍＝已解凍」自行解凍或試打。

## Agent 檢查清單

接到會碰市場資料同步／audit／Dividend／FRED 的任務時：

1. 先讀本 rule + `HANDOFF.md` 操作凍結指標  
2. 若仍凍結 → 拒跑外部 API，改本地／計畫路徑，並回報凍結狀態  
3. 發現仍在跑的 FinMind／FRED 進程 → **安全停掉**，報告標 **PAUSED（API 凍結）**
