# 環境 / secrets
.env
.env*
venv/
.venv/
.venv-mcp/
.webui_secret_key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# 資料 / 模型輸出（machine-local，不進 git；由 from-zero 重建）
# 錨定根目錄：勿誤傷 src/augur/models/ 套件原始碼（未錨定之 models/ 會遞迴匹配任意層）
/data/
/models/
/models_artifacts/
augur-data/
*.dump
*.log
logs/
.db_export/

# 報告產出（JSON dump 等；.md 報告視情況 force-add）
reports/*.json

# OS
.DS_Store

# Claude Code 本地狀態（不進 git）
.claude/settings.local.json
.claude/*.lock
.refresh_pipeline.lock

# 合倉前獨立 code clone（過渡期；終態不再需要）
ref_augur/

# 工作目錄打包檔（zip 可能含 .env）
*.zip

# project-memory-mcp 語意索引（各機重建）
.project_memory/

# qdrant 本機初始化標記（衍生物）
.qdrant-initialized

# GPU 驗證本機衍生物
ops/gpu-verify/.gpu-test-venv/
ops/gpu-verify/saxpy
