# 環境 / secrets
.env
venv/
.venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# 資料 / 模型輸出（machine-local，不進 git；由 from-zero 重建）
# 錨定根目錄：勿誤傷 src/augur/models/ 套件原始碼（未錨定之 models/ 會遞迴匹配任意層）
/data/
/models/
/models_artifacts/
*.log

# 報告產出（JSON dump 等；.md 報告視情況 force-add）
reports/*.json

# OS
.DS_Store

# Claude Code 本地狀態（不進 git；settings.json 共享設定如需可另 force-add）
.claude/settings.local.json
.claude/*.lock
.db_export/
.webui_secret_key
.refresh_pipeline.lock
.env*
