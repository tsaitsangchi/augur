# 系統架構大憲章 v1.0.0（骨架 — 撰寫中）

> ⚠️ **狀態：骨架待 review**。本檔目前只有**結構大綱**；待用戶確認
> `系統核心思想_v1.0.md`（靈魂）+ `原則精華_v1.0.md`（15 條）後，再逐部填入內容。
> 目標篇幅 **800–1500 行**（對比舊憲章 13,951 行，砍 ~90%）。
> 設計原則：**主題導向（非時間軸）**；憲法記現行法律，演變史進 `docs/archive/`。

---

## 第一部　系統本質
- 指向 `系統核心思想_v1.0.md`（不重複，只摘要 + 連結）。
- 預測標的 / 任務 / 資料源 / 邊界一頁總覽。

## 第二部　不可違反的 15 條原則
- 直接承載 `原則精華_v1.0.md` 的 15 條（A 資料 / B 建模 / C 風險治理）。
- 這是憲法核心。每條 WHAT｜WHY｜ENFORCE。

## 第三部　架構分層
- 管線：`raw → feature → universe → model → validate`。
- 每層：職責、輸入/輸出、不可越界邊界、對應 `src/augur/` 模組。
- 橫切層：core（path/db/generic_schema SSOT）、audit（對帳/特徵稽核）。

## 第四部　維運矩陣（從零重建）
- **從零 12-PHASE 序列**（環境 → schema → 名冊 → FRED → raw sync → 對帳 → 宇宙 → 特徵 → gate → 特徵 audit → 訓練 → 驗證）。
- 每 PHASE：指令、產出、audit gate、已知順序陷阱。
- Long-running 治權（5-min 回報 / 防睡眠 / resume）。

## 第五部　升版規則
- 新增一條不可違反原則 → 升版；微修正 → 改條文不新增。
- 升版優先級 / 變更審查。

## 附錄 A　經驗結論（真實數據，可重現）
- 從舊專案萃取的**真實結論**（非考古）：dead/冗餘 features、model 比較、gate 閾值（如 walk-forward 可信度門檻）、beta/alpha 拆解。
- 每筆附 source（程式/DB/API）+ 口徑（raw vs purged）。
- ⚠️ 數據待**在 Augur 重跑後**以 source-traceable 補入（避免直接搬舊數字 → 違反 #15）。

## 附錄 B　考古索引
- 指向舊 repo（`stock_backend` v6.x）之 §14.7-A~DJ / 255 treaty，供必要追溯。
- 不搬內容，只留「要查歷史去哪翻」的索引。
