# 合憲補正交接（HANDOFF）— 2026-07-17

* **分支**：`remediation/impl-2026-07-17`（基於 main `493fd73`）
* **性質**：AUGUR-MC v1.3 合憲審計補正之程式實作（步 7 AUD-02 + 步 11 結構補正），**全數走分支、未 apply 生產 DB、未併 main**。
* **上游治理**：憲章、Layer 1–4 規格、審計報告、補正行程存於本倉 `constitution/`／`specs/`／`audits/`（[tsaitsangchi/augur](https://github.com/tsaitsangchi/augur) monorepo；原獨立倉已併入）。本分支對應 `audits/REMEDIATION-ROADMAP.md` 之步 7、11。
* **驗證狀態**：py_compile 全通過；免 DB 免 API selftest 全綠；import_isolation 稽核 exit 0 零誤報。**DB 語義（trigger RAISE／同交易回滾／UNIQUE/FK/REVOKE 生效）本機無 PostgreSQL 未實跑。**

---

## 一、本分支內容（2 個 commit）

### commit `5cee263` — 步 7 AUD-02 raw_supersede_log（critical，P4.E5 禁 last-write-wins）

heal 覆寫前同交易快照舊列至 append-only 帳表，**upsert 主路徑 256-272 語義一 byte 不動**。決策 A（append-only trigger＋tombstone 法規抹除例外）、決策 B（attestation_run_id nullable）由 Steward 2026-07-17 拍板。詳 [AUD-02-raw-supersede-log.md](AUD-02-raw-supersede-log.md)。

新增 `scripts/migrate_raw_supersede_ddl.py`；改 `generic_schema.py`（快照 hook＋gate＋_jdump＋fail-loud）、`schema.py`（INFRA_DDL 第四表＋actor 欄）、heal 鏈透傳（reconcile/sync/ingest/daily_maintenance/full_universe_attest）、`setup_predict_role.py`、`import_isolation.py`。

### commit `7932ba9` — 步 11 結構補正（AUD-04/05/06/07/08/10/11，majors）

依 AUGUR-ID/AUGUR-KS 規格之結構基礎設施，**全新增式**（既有 raw 表/TaiwanStockInfo/消費者不動）：

| Table | AUD | 規格 | 用途 |
|---|---|---|---|
| entity_type_catalog | 04 | ONT T.1-61、ID.12 | 型別值域住 DB＋命名空間隔離 |
| entity_registry | 04 | ID.11-14 | 系統鑄造 augur_id、永不刪除 |
| entity_alias | 04/06 | ID.11、ID.30(a) | 外部碼降 provisional，非 identifier |
| identity_claim | 06 | ID.30-32、KS.20 | 跨來源同一性四要件＋confidence 槽承 L4 |
| identity_lifecycle_event | 05 | ID.40-44 | merge/split/retire/relist/redirect＋lineage＋代碼重用紅旗 |
| entity_attribute_version | 07 | ID.60-61 | 屬性 as-of 雙時間 SCD-2 |
| authorization_grant | 10 | P5.E1 | 自動行動機讀授權（遷入 shell 註解拍板） |
| automation_action_log | 10/11 | P5.E1 | 行動六元組留痕（取代家目錄純文字 log） |
| prediction_serving_log | 08 | KS.40-42 | prediction_values append-only 伴生表（既有不動） |

python：`src/augur/identity/{identifier,claim,lifecycle,attribute_version}.py`、`src/augur/execution/action_log.py`；migration：`migrate_identity_ddl.py`、`seed_entity_type_catalog.py`、`migrate_automation_action_ddl.py`、`migrate_prediction_serving_ddl.py`。

---

## 二、⚠️ 部署閘（人類必辦，依序）

本分支程式碼**不得直接 apply 或併 main**。依 CLAUDE.md #7/#19/#20 與憲章 P5：

1. **備援環境實測（#7）**：於有 PostgreSQL 之環境逐一跑各 migration `--check`（唯讀 VERIFY）與 `--selftest`，並**實測 DB 語義**：append-only trigger 對非 owner 之 UPDATE/DELETE/TRUNCATE 是否 RAISE、同交易回滾、UNIQUE/FK 違反遭拒、tombstone/de_identify 受控路徑、`setup_predict_role --apply` 後 `augur_predict` 對新表 SELECT 遭拒（`test_predict_role_isolation` 需 PG＋role 在線方非 skip）。
2. **核心模組逐檔檢視（#19）**：重點 `generic_schema.py`（共用寫入路徑）、`full_universe_attest.py`（逾原卷宗清單之 heal 透傳擴張，須確認獲認可）、identity 四模組與 migration 之 DB 語義。
3. **表 owner ≠ 應用角色**：append-only trigger 擋非 owner；owner 隱含全 DML/DISABLE TRIGGER，部署層須確保應用連線角色非表 owner（migration 檔頭已 ⚠ 載明）。
4. **P5 拍板**：經上述後由人類拍板，方可 apply 生產 DB、`setup_predict_role --apply`、併 main。

---

## 三、⚠️ 升 Steward／P5 裁決之項目（不擅自實作）

| # | 事項 | 說明 |
|---|---|---|
| **A** | **步 7 #1：P4.E5 主路徑殘餘破口** | `sync_by_date` 每日 resume 自 DB max(date) 重抓、可對同 (stock_id,date) 以修正結算值經**主路徑**（snapshot_reason=None）覆寫而不留 supersede 痕——仍屬 last-write-wins。補它須把快照下沉主路徑或令 daily-resume 亦快照，**違反已拍板之「主路徑一 byte 不動」硬約束**。**是否重開設計，須 Steward 裁決。** |
| **B** | **原則精華 #7 條文改** | #7（correction＝覆蓋為當前值）須改「新版本入庫、舊版標 superseded」以與 AUD-02 一致——屬 Layer 4 治權檔變更，須 Steward 拍板，code 不先行。原則精華檔頭已揭露此緊張。 |
| **C** | **步 11 Issue 8：行動授權模型** | `automation_action_log.authorization_ref` 是否收斂 NOT NULL、或引 `unauthorized` 狀態＋強制回流、或暫行最高風險授權佔位——屬 P5 授權政策與 §P5.E2 缺位預設之過渡處置裁決。 |
| **D** | **步 11 Issue 2：型別語義資料驅動** | 「型別→終結事件→Evidence 義務」由型別表資料驅動（取代硬編碼白名單）為新增表型設計決策，逾本輪比例原則，flag 予 P5 設計裁決。 |
| **E** | **Layer 2/3 充任認定前置** | AUGUR-ONT/AUGUR-ID 之合規聲明須補 WM.44 逐條對應矩陣（Layer 4 已示範完整矩陣可參照）；三草案（ONT/ID/KS）之 Steward 充任認定未成就前不生效力。 |

---

## 四、Follow-up 分階段（本輪未做，須另立計畫拍板）

結構基礎設施已就位，但**既有消費者尚未改繫**（依 AUD 補正方向「raw 表可不動、逐步改繫」之分階段）：

* **Phase 1**：世界概念 registry 接線、SQL 字面表名直綁消除（AUD-01 code 面，涉 37 檔消費者）。
* **Phase 2**：feature_values/core_gate 產業判定改讀 `entity_attribute_version` as-of；as-of 視圖為預測消費唯一入口。
* **Phase 4**：prediction_values 本體改 append-only（切至 prediction_serving_log 語義），改 advisor/風控消費。
* **Phase 5**：攝取准入點 resolve-or-mint 接線（ID.11 runtime 義務結清——**現為「機制就位、義務未結」，合規聲明不得計為已落實**）；watchdog/selfheal/heal 改繫 `action_log`。
* Confidence 語義填充、as-of 重建引擎、resolution 演算：DEFER Layer 4/5（規格已定掛鉤）。

---

## 五、參照

* 憲章：`AUGUR-MC v1.3`（augur-constitution `constitution/META-CONSTITUTION.md`）
* 規格：AUGUR-WM v1.0（生效）、AUGUR-ONT/AUGUR-ID/AUGUR-KS v0.1-draft（待充任認定）
* 審計：`audits/CODE-COMPLIANCE-AUDIT-2026-07-16.md`（已驗證＋裁決定調）
* 行程：`audits/REMEDIATION-ROADMAP.md`
* 封存 tag：`augur-mc-v1.3-compliance-seal`（治權檔憲章從屬聲明＋SSOT 正名，已於 main）
