# 系統架構大憲章 v1.34.0

**性質**：Augur 架構治理之最高承載文件（位階次於靈魂與原則精華）——回答「系統**怎麼建、怎麼運作、怎麼治理**」。
**位階**：精神以 `docs/系統核心思想_v1.5.0.md`（靈魂）為準；不可違反之法律全文以 `docs/原則精華_v1.7.1.md`（20 條）為準（SSOT）。本憲章承載**架構分層 + 維運序列 + 升版規則**，並把那 20 條法律**對映到「三個敵人 × 管線」**框架上。
**脊椎**：整部憲章用兩條軸組織——
- **三個敵人（WHY）**：假資料 / 偷看未來 / 自我欺騙——系統存在的每條紀律都在防它們。
- **管線（HOW）**：`raw → feature → universe → model → validate` + 橫切（core / audit）。
**讀法**：第一部懂精神，第二部懂為什麼，第三部懂架構，第四部查法律，第五部照著重建，第六部知道怎麼改憲法。目標：**30 分鐘讀懂全系統法律**。

---

## 第一部　系統本質

Augur 是一套**只用真實資料、誠實預測台股**的系統：從 FinMind / FRED 抓進可溯源的日級資料 → 算成 source-pure 特徵 → 選出資料乾淨的核心股 → 訓練模型預測未來 H 日的**相對強弱**（橫斷面排序 → top-N）→ 以 walk-forward 誠實驗證並附**可信度**。

> 全文見 `系統核心思想_v1.5.0.md`。一句話：**只信真兆。**

| 面向 | 內容 |
|---|---|
| 標的 | 台灣上市櫃個股 |
| 任務 | 橫斷面相對強弱排序（週/月/季/年多尺度）→ top-N |
| 資料 | FinMind API + FRED；**日為最小單位** |
| 產物 | 分數/機率 + 可信度（walk-forward IC / Eff-t / 勝率） |
| 邊界 | 不日內、不保證獲利、不擇時躲崩盤、不用未來資訊（anti-leakage）、不下單動錢、不存無源值 |

### 資料本質：PostgreSQL＝唯一真相來源（single system of record）〔v1.26.0〕

本專案**一切資料的唯一真相來源(SSOT)與系統記錄，皆為本地 PostgreSQL**——市場 raw、`feature_values`、universe、模型輸出、知識層（`knowledge_*`／`philosophy_*`）、catalog 元資料、乃至未來權限（`app_user`／群組），全部住 PostgreSQL。

- **外部 API（FinMind／FRED／OpenAlex…）為原始擷取源、非資料家**：一律先落地入 PostgreSQL，下游只讀 PostgreSQL（#9 真實來源之 (b) DB query 為主通道；抓取即入庫、不繞道記憶體／檔案當權威）。
- **衍生索引與傳輸工件非 SSOT、可拋棄**：serving 索引（Milvus 等）、JSON／CSV／md 匯入匯出工件，皆得由 PostgreSQL 全量重建或匯入，永不成第二權威家（承 pgvector＝SSOT 索引鐵則、#29b「repo JSON≠來源 SSOT」、#12）。
- **設定與程式碼不承載資料**：`.env`＝機密設定非資料；code＝邏輯非資料；資料只在 DB。
- **跨機遷移＝`pg_dump`**（#30），非搬檔案。
- **定位**：本條為 #9（真實來源）＋#12（單一 SSOT）＋#29b（資料驅動、來源住 DB）之**系統級明文收斂／總綱**，非新增第 21 條法律。

---

## 第二部　三個敵人（防線總綱）— 為什麼有這些法律

量化系統最容易死在三件事上。20 條法律**全部**是在防這三個敵人（#17 為取得穩健之 precondition：封鎖則無真資料→一切下游停擺）。

### 敵人 ①　假資料（AI 幻像）
- **長相**：imputed / zero-fill / hardcoded 知識 / 推估 / 系統自己補的值——沒有真實 API 來源。
- **致命後果**：用假資料跑出來的模型沒有意義；錯誤從源頭污染一切下游。
- **防線（原則）**：**#1 零幻像 ★** · #2 API 即權威 · #3 純通用 ingestion · #4 日為最小單位 · #5 型別紀律 · #6 冪等+resume · #7 DB↔API 對帳 · #9 思想≠特定值（hardcoded 值＝注入假資料）· #10 核心股質>量（不完整股排除）· #16 clean-room（防 stock_backend 硬編值經 code 回流）· #17 API 速率公民（主動限速,防 IP 封鎖致無真資料）· #18 抓取依 API 探測（方式+範圍、補多維度抓取,確保抓到 API 實有真資料）。
- **最危險的管線層**：`raw`、`feature`、`universe`。

### 敵人 ②　偷看未來（時間洩漏）
- **長相**：用了決策當下還不知道的資訊（未來函數 / 標籤窗洩漏 / 發布日錯置）。
- **致命後果**：回測很美、實戰崩盤——最隱蔽、最致命的自欺。
- **防線（原則）**：**#8 anti-leakage ★**（≤t 設定、t+1 套用、walk-forward IC 附 purged 口徑；含 survivorship/選股偏差防護）。
- **最危險的管線層**：`feature`（特徵時點）、`model`（訓練/驗證切分）、`validate`。

### 敵人 ③　自我欺騙（灌水回報）
- **長相**：把「打算做的」當成果、拿單次極值當定論、單看 gain 判特徵、以準確率宣稱有效、報喜不報憂、**憑「我以為」代替查證——尤其「我以為不重要／不需要」而跳過 → 漏做 → 系統出錯**。
- **致命後果**：文件失真比沒文件更危險——驅動錯誤決策。
- **防線（原則）**：#10 核心股質>量（不誇大覆蓋）· #11 五鏡特徵治理 · #12 單一引用源 · #13 空頭防護規則地板優先 · #14 經濟價值判定 · **#15 誠實回報 ★** · #16 clean-room（防未重建 code 之自欺回流）· #19 可控風險下逐級逼近最佳奇異點（試錯即進步、重覆驗證再定論，防把單次觀測當結論）· #20 自驅動 × 實證決策（憑「我以為」下決策＝自欺；主動必配實證）。
- **最危險的管線層**：`feature`（特徵存廢）、`validate`（成績宣稱）、報告層。

> ★ = 各敵人的**基石防線**（#1 / #8 / #15，即原則精華「三條基石」）——一條守一個敵人；守住這三條，等於守住三個敵人的核心。

### 防線地圖（敵人 × 管線 → 守的原則）

| 管線層 \ 敵人 | ① 假資料 | ② 偷看未來 | ③ 自我欺騙 |
|---|---|---|---|
| **core**（橫切） | #2 #3 #5 | — | — |
| **raw** | #1 #2 #3 #4 #6 #7 #17 #18 | — | — |
| **feature** | #1 #9 | #8 | — |
| **universe** | #1 #10 | — | #10 |
| **model** | #1 | #8 | — |
| **validate** | — | #8 | #12 #13 #14 #15 |
| **audit**（橫切） | #7 | — | #11 #15 #19 |
| **catalog**（橫切） | #2 #3 #18 | — | #15 |
| **philosophy / advisor**（橫切） | #1 #16 | #8 | #14 #15 |
| **開發/協作**（橫切） | #16 | — | #16 #20 |

> 三個敵人共同點：都讓你「**以為自己懂，其實在自欺**」。北極星問題——**這是真兆，還是假兆？**——就是隨時可用的總檢查。

---

## 第三部　管線分層架構 — 系統怎麼建

資料流：`raw → feature → universe → model → validate`，外加兩個橫切層 `core` / `audit`。
**鐵律：各層職責不越界**——選股不算特徵、特徵不抓 API、驗證不讀訓練產物。

### 橫切 · core（`src/augur/core/`）
- **職責**：路徑/設定、DB 連線、**generic_schema（auto-schema SSOT）**、schema 推導 helper（查 DB information_schema）、infra log 表 DDL（PHASE 1 bootstrap）。
- **守**：① — #2 API 即權威、#3 純通用、#5 型別紀律。
- **邊界**：不抓 API、不選股、不算特徵；只提供地基設施。
- **SSOT**：型別規則（字串 VARCHAR(255)/TEXT、數字 NUMERIC(20,6) 自動擴大）唯一住在這裡。

### 1 · raw（`src/augur/ingestion/`）
- **職責**：通用 ingester 把**任意** FinMind/FRED dataset 自動建表落地（無白名單）。
- **輸入→輸出**：API 回應 → DB 原始表（每值 byte-equal API）。
- **守**：① — #1 #2 #3 #4 #6 #7 #17 #18。
- **邊界**：不算特徵、不選股；intraday 不入庫（只存日級衍生）——**#4 日為資料本質排除準則，其餘日級真實資料皆抓、不以維度/非個股排除；唯資料量規模物理界限者除外（分點/權證/鉅額分點 `OUT_OF_UNIT` 排除，判準全文＝原則精華 #3）**。

### 2 · feature（`src/augur/features/`）
- **職責**：只用 source-pure 原始值，算成特徵面板（panel）；自建 `feature_*` 表（`CREATE IF NOT EXISTS`）。
- **輸入→輸出**：raw 表 →（每股每面板）source-pure 特徵；**算不出真實值的不寫入（缺列，不存 fake / 不存 zero-fill）**。完整度（哪些股缺哪些特徵）交由 universe 層判定。
- **守**：① 不造假特徵、**不存無源值**（#1 #9，扣靈魂「不存沒有 API 來源的值」）；② 特徵時點 anti-leakage（#8）。
- **邊界**：不抓 API、不選股；公式禁 hardcoded 知識/閾值（#9）。
- **特徵發現方法（v1.10.0 入憲承載）**：「**如何找出有用的特徵值**」之方法依據——**三鏡頭 × 紀律漏斗**：同一批 raw 欄位經三種旋轉視角（**第一性原理**＝資訊內容/七軸、**八二法則**＝分布形狀/六軸 cutoff-free 泛函、**康波循環**＝時間結構/六軸 data-driven 相位）生成候選假說，再過**四道漏斗 + 系統級收尾關卡**（紀律閘 #1/#8/#9 → 五鏡 #11 → out-of-sample walk-forward → **提拔關卡**〔v1.13.0：入生產前強制 as-of + 去相關 Eff-t（HAC、禁裸 iid）+ 多 seed 多因子增量；Δ≤0 不提拔〕→ **經濟價值驗證**〔v1.14.0 入憲、系統級 #14：特徵集＋模型須過 purged walk-forward 投組之 CAGR/Sharpe/MaxDD/Calmar 對比基準,風險調整後優於基準才算成功——**IC 撐住 ≠ 可交易,靈魂成功定義是經濟價值非 IC**〕）淘汰;**有用＝驗證活下來、非設計出來**。詳法見 `reports/augur_feature_discovery_methodology_20260626.md` §四（憲章承載框架、方法檔承載詳法，守第六部 SSOT + 30 分鐘可讀）；屬 #1/#8/#9/#11/#14/#15 之操作化收斂、非新增原則。後續一切特徵值產生作法以該檔為依據，偏離（漏過漏斗/硬編特定值/洩漏時點/裸用 iid Eff-t/未過提拔關卡逕入生產/未過經濟驗證即宣稱成功）即治權違規。

### 3 · universe（`src/augur/universe/`）
- **職責**：核心股選拔——核心股 ＝ **全部「source-pure 完整」的股**（無評分、無排名上限）；任一面板任一特徵缺值（不完整）即排除；自建 `core_universe_*` 表。
- **完整度判準之兩項限定〔v1.24.0 承載,稽核決5〕**（生產 build 自 2026-06-30 啟用、此前未入憲；性質＝完整度 gate 之邊界限定、非評分排名）：
  - **(a) 流動性分位地板**：以 `dollar_volume_log_20d` 之**動態橫斷面百分位**（如最近 panel P25）為下界剔極不流動股——**動態百分位非硬編絕對值**（守 #9）、非評分（過線即入、線上不排名）；理由＝極不流動股之特徵值雜訊大、不具可交易性。
  - **(b) conditional 結構性豁免**：金融保險業無月營收申報制度 → 對其豁免月營收完整度要求（`--exempt-revenue-financial`），否則整板塊被 gate 結構性誤排（非資料真缺）。
- **輸入→輸出**：raw 表（完整候選）＋ feature 面板（完整度收緊 ＋ 上述兩限定）→ committed 核心股名單（**完整度關，無評分排名**）。
- **守**：① source-pure gate（#1）、不完整股排除（#10）；③ 質>量（#10，可以少、不以數量為目標、**不排名**）。
- **邊界**：不算特徵、不訓練、**不評分排名**；selection 以資料正確+精準（＋流動性可交易性、結構性豁免）為判準。

### 4 · model（`src/augur/models/`）
- **職責**：訓練模型（樹家族為主軸，transformer 為輔）產 artifacts + registry；自建 `model_registry` 表。
- **輸入→輸出**：核心股 × source-pure 特徵 → 模型檔。
- **守**：① 只吃 gate 通過之完整核心股（已保證 0 缺值，#1）；② 訓練/驗證切分無洩漏（#8）。
- **邊界**：不抓 API、不選股；只載入核心股完整面板（缺值股已被 universe gate 排除，無 imputed 可濾）。

### 5 · validate（`src/augur/evaluation/`）
- **職責**：自包含 walk-forward，產各週期可信度 + 經濟價值。
- **輸入→輸出**：核心股 × 特徵（自己 train→predict）→ 各週期 metrics。
- **守**：② purged 口徑（#8）；③ #12 單一引用源、#13 規則地板、#14 經濟價值、#15 誠實。
- **邊界**：**不讀 model 層 artifacts**（雙軌獨立，自做 train/predict）；metric + panel 窗由單一 helper 算（#12）。

### 橫切 · audit（`src/augur/audit/`）
- **職責**：DB↔API byte-level 對帳（無幻像 attestation）+ 訓練前特徵稽核（**五鏡**）。
- **守**：① #7 對帳；③ #11 五鏡、#15 誠實。
- **邊界**：唯讀稽核，不改資料、不選股。

### 橫切 · catalog（元資料登錄，`dataset_catalog` / `column_catalog`）　〔v1.7.0 新增 · v1.8.0 官方參考入憲〕
- **職責**：把「怎麼抓每個 dataset」的元資料（表/欄/型別/中文/最優抓取模式/data_id 來源/最早日期/排除旗標/provenance/last_verified）落地為 DB 登錄表，作 raw 層抓取之**單一驅動依據**。**SSOT 分工〔v1.24.0 校正,稽核決9〕**：**抓法元資料**（型別/最優模式/最早日/排除旗標/provenance）以**表為 SSOT**（API-derived、動態重算）；**表/欄中文名**非 API-derivable → 以 **`datasets_zh.md` 人工策展為源**，經 `seed_table_zh`／`seed_column_zh` 灌入 catalog（md＝中文名輸入 SSOT、非由表生成）。二者不同源、各有其正當 SSOT。
- **權威輸入源**〔v1.8.0〕：tier / intraday / sponsor / single-day 等抓法分類由 **FinMind 官方 `docs/finmind-references/datasets.md`**（入憲、可刷新）解析驅動（`_parse_official_datasets`，大小寫容錯）+ live API 探測（schema/最早日）+ augur 策展（中文/髒值/`OUT_OF_UNIT` operational）；**取代 code 硬編白名單**（守 #3/#18；官方未列則退回探測/硬編備援、不脆）。
- **守**：① — #18（API-derived 探測結果之**持久化**，非硬編白名單）· #3 純通用 · #2 API 即權威 · #15（每欄標 provenance + last_verified；型別/中文＝augur 推導非 FinMind 權威，須明標）。
- **邊界**：只驅動「**怎麼抓**」、**不保證「資料對」**（對帳/#7 仍獨立）；是**可刷新快取、非鐵契約**——登錄失效 → 引擎 adaptive fallback 重探並回寫（守 #18、不脆）；存原料**動態重算**最優模式（不凍結會變的值如 n_dates）。
- **詳細設計**：見 `reports/augur_metadata_catalog_design_20260615.md`（憲章承載架構框架、詳設計住設計檔，守第六部 SSOT + 30 分鐘可讀）。

### 橫切 · philosophy（哲學素養框架層：投資哲學因子假說 ＋ 廣博哲學素養）　〔v1.16.0 立 / v1.17.0 擴博學〕
- **表**：`philosophy_school` / `philosophy_principle` / `principle_factor_map` / `philosophy_source` / `philosophy_thinker` / `philosophy_work` / `philosophy_work_text` / `philosophy_chunk` / `stock_philosophy_tag` / `philosophy_build_meta`（DB 實查 2026-07-03；完整 schema 以 DB `information_schema` 為權威 #2）；知識底座 `knowledge_*` 諸表見「知識層多域擴充準則」。
- **職責**：augur 為「**博學的投資大師 AI**」之知識骨架，兩層——**(a) 投資哲學層**：把投資大師智慧（真實權威文獻）結構化為「**可證偽因子假說 ＋ 股票分類框架**」（學派 / 原則 / 原則→因子映射 / 文獻來源 / as-of 分類 / build_meta），作 **feature 層特徵假說理論來源 ＋ validate 解讀層**（三鏡頭：第一性 / 八二 / 康波，即其操作化）；**(b) 廣博哲學素養層**：廣納**人類哲學經典原典全文**（東方：兵法 / 道家…；西方：柏拉圖 / 亞里斯多德…公版），作**解讀與智慧素養素材**（可解釋性、思想深度）。
- **共同不變式（明文從屬三敵、以下各準則段皆從屬此、不逐段複誦）**：① **哲學/知識＝假說與素養、非真兆**——來源限**真實文獻、禁 AI 生成入庫**（敵人① / clean-room #16；`source_type` / `work_type` 禁 `ai_generated`、`license` 白名單 DB CHECK〔哲學原典限公版；知識層 item_text 雙軌見下〕）。② **素養內容量化零價值、僅素養/解讀素材、不產投資因子、不進預測管線**——投資因子唯一經 school（`domain='investment'`）→ principle → factor_map → **out-of-sample ＋ 經濟價值 #14** 鏈裁決（「**驗證活下來、非大師說了算**」、過四道漏斗）。③ 股票哲學分類 as-of（#8，從 ≤t `feature_values` 量化判定）。④ `validated_ic/econ` 由 augur 自身實證回填、可溯源（#15）。
- **邊界**：哲學（投資＋廣博）純假說來源＋解讀/素養、**不取代真實資料**（不進預測管線＝共同不變式②）；augur 仍是「**有廣博哲學素養的量化顧問、非 AI 占卜大師**」（靈魂 v1.5.0）。
- **原典抓取範圍界定（資料納入準則）〔v1.17.0〕**：**納入**人類**哲學**經典原典——投資哲學 / 戰略思維 / 行為財務（核心、可產因子假說）＋ 東西方哲學經典（柏拉圖 / 亞里斯多德 / 兵法 / 道家…素養層）；須**公版**（維基文庫 raw wikitext / Gutenberg .txt 等真實來源、`license` 公版可溯源）。**排除**：**非哲學**之離題領域（純自然科學 / 物理 / 數學，如相對論）——**能抓 ≠ 該抓**；AI 生成內容（敵人①）。**方法**：本地抓取解析、零 Claude usage（#28）、逐字無 AI 摘要改寫（#1）、冪等可續可排程。判據：「**這是人類哲學思想經典嗎？**」——是→納；否（純科學 / AI 生成）→排。
- **版權著作核心精神入庫準則（現代大師合規路）〔v1.18.0〕**：現代版權著作（全文法律不可抓、#1 ＋ 版權）之**核心精神**,經**真實文獻出處的 `philosophy_principle` 原則條目**（引書名／篇章／頁碼 citation、非 AI 整理摘要）→ `principle_factor_map` 因子假說 → **#14 經濟價值驗證**入庫；採用由 **#14 裁決、非大師權威說了算**（`validated_ic` / `validated_econ` 實證回填 #15）。**嚴禁 AI 整理／摘要版權著作內容入庫**——那是 AI 生成假兆（違 #1）＋ 侵權,`work_type` / `license` DB CHECK 硬擋。核心精神之**合法載體 ＝ 可證偽因子假說 ＋ 真實文獻 citation**,非著作摘要。（性質＝三敵明文強化、非新增原則，見修訂歷程 v1.18.0。）
- **知識層多域擴充準則(通用知識底座)〔v1.19.0〕**：philosophy 橫切層之資料底座擴為**通用知識管線**——`knowledge_source`(來源 registry:全球公開知識庫 API + 策展入口)→ `knowledge_query`(領域×子領域查詢分類法)→ acquire(通用擷取引擎)→ `knowledge_staging`(payload + provenance 暫存待審)→ promote(晉升正式表);承載「博學投資大師 AI」(靈魂 v1.4.0「博學」)之**多域素養知識**,得依**決策層人拍板**納入管理/能源材料/太陽能材料/化學/電子/物理/生物等知識域(「能抓≠該抓」判準不變;機制=INSERT registry 列零 code,工具層 SSOT=CLAUDE #29b;三敵從屬＝共同不變式①②、每筆帶真實來源 provenance)。**全文准入雙軌〔v1.20.0〕**:哲學原典 `work_text` **維持限公版**(v1.17.0/v1.18.0 判準不動);知識層 `knowledge_item_text`(OA 論文/報告)=公版 **+ CC 白名單開放授權**(`cc-by`/`cc-by-sa`/`cc0`,license 欄 DB CHECK 硬擋、逐字轉載附授權與來源——CC≠公版但授權明文允許轉載,守 #1 真實來源;**NC/ND/版權未明一律停在 metadata**)。`domain` 欄(school / staging / query)隔離保因子假說鏈純度(共同不變式②之機制)。
- **檢索與顧問前端（素養層唯讀出口）〔v1.21.0〕**：`augur.philosophy.retrieval`（RAG 混合檢索：e5-small 384 維 pgvector kNN ＋ lexicon／concordance 逐字回查、verbatim 逐字包含回查驗證〔`retrieval.verify_verbatim`，DB 原文子字串比對〕）＋ `src/augur/advisor/`（`payload`〔預測數字唯讀 frozen 通道〕→ `prompt`〔三姿態＋硬約束〕→ `advise`〔LLM 界面抽象、不綁特定模型〕→ `guard`〔生成後防幻覺**機械閘**〕）。
- **防幻覺＝機械 gate 非 prompt 自律**（#1／#15 在對話層之落地）：引文 ≥8 字須逐字 ∈ 檢索 citations、量化數字須 ∈ payload 白名單、lexicon 定義引用必附 locator、逆向鏡只提示風險**絕不輸出與模型分數相反之行動**；**三級誠實固定句閉集〔v1.25.0 擴,拍板3〕**——(i) 檢索空集（庫中確無）→「知識庫中無此內容」；(ii) 庫存有此著作但歸屬未驗證（`review_flag=true` 之隔離館藏，旁查命中而主檢索 CLEAN 述詞排除）→「知識庫存有此著作但歸屬未驗證，不予引用」；**閉集僅此二句、機械分級（answer.py 隔離館藏旁查分級器，W8 落地）、模型零即興空間**；閉集或分級判準之任何變更＝憲章判準變更（見下「對話單閘」）。
- **顧問呈現層＝只給白話解讀，公版引經據典逐字區塊不對外顯示〔v1.30.0 用戶 directive〕**：對外回覆**只顯示模型白話解讀**；「── 引經據典（系統逐字附上、公版可溯源）──」逐字區塊**一律不對外顯示**。**honesty gate 一字不動**——`guard` 內部仍以 citations 逐字校驗（引文 ≥8 字 ∈ citations、數字 ∈ 白名單），R2「系統逐字附上原文」由**對外展示**改為**僅內部校驗基準**（隱藏呈現≠移除機制、防幻覺閘完全不變）；`retrieval`/`advise`/`guard` 檢索與校驗鏈不改，僅 `oai_compat._reply_text` 呈現組裝隱去公版引文區塊。**Mode B 用戶自帶附加檔逐字區塊【保留】**（用戶自有文件助讀、非公版引經據典）。屬呈現層 UX 決策（用戶拍板）、非防幻覺判準變更 → 僅憲章升版、guard 閉集與機械閘不動。
- **隔離不變式（機器強制）〔v1.22.0 擴 knowledge〕**：預測管線 7 package（features／models／universe／evaluation／ingestion／audit／catalog）**零 import** `augur.philosophy`／`augur.advisor`／`augur.knowledge`（AST 稽核 `tests/test_philosophy_isolation.py` 常備）；顧問對預測／哲學表**唯讀零寫回**、單向依賴（advisor 依賴預測輸出＋素養庫，反向禁止）。**〔v1.28.0〕RBAC 一切表（`app_user`／`permission_group`／`user_group`／`group_domain_grant`／`app_session`／`knowledge_domain`／`knowledge_access_audit`）與 resolver（`augur.knowledge.access`／`identity`）同受此零 import 約束、**禁置於 `augur.core`**（否則開知識→預測旁路破 #1 命門）；`test_philosophy_isolation.py` 擴掃 `scripts/` ＋ 正向 assert resolver 前綴 ∈ FORBIDDEN。**
- **知識存取控制準則（RBAC 讀取控制）〔v1.28.0；works 側裁決 v1.29.0〕**（系統定位擴張：單一主使用者 → superuser ＋ 受控多使用者群組；動靈魂 v1.5.0。詳計畫 SSOT＝`reports/augur_rbac_design_plan_20260705.md`＋群組規劃 `reports/augur_rbac_group_plan_20260705.md`；三輪資安紅隊 critical 10／high 9 全處理）：
  - (i) **隔離不受影響**：共同不變式①②、隔離不變式、#1 命門**不因 RBAC 增刪一字**；RBAC 只作用於「人對**已 CLEAN 素養內容**的讀取可見性」，其表任何欄**禁被預測 7 package 讀取或當特徵**（授權資料非因子）、不得新增「按群組給不同預測／按 domain 觸發特徵」路徑（**預測對所有人相同**）。
  - (ii) **強制一律 server-side SQL**：在 `corpus.clean_item_sql` 之上 **AND 收窄、不改寫 CLEAN**；enforcement 住 `access.resolve_allowed_domains` ＋ `clean_item_sql(is_super, allowed_domains)`，**items（知識/財經）exact/ann 逐路徑同帶、預設 deny**（works 側裁決見 (vi)）。
  - (iii) **resolver 語意**：superuser→全部；否則＝群組 `group_domain_grant` 聯集；**空集＝deny（fail-closed、非不濾）**；`is_super`（bool）與 `allowed`（frozenset）型別分離、禁「None＝ALL」隱式哨兵；DB 錯/inactive/無 grant 一律 `(False, ∅)`。
  - (iv) scoped 檢索**仍經 `advise()`＋`guard` 單閘**、不改三級誠實固定句閉集（RBAC 收窄致空集 → 走既有誠實句、**不新造「你無權查」**、與「庫中確無」位元級不可區分）。
  - (v) `domain` 由「因子鏈純度隔離」兼任「授權邊界」＝結構張力，以 `knowledge_domain` 字典 FK ＋ 兩階段驗證 ＋ 入庫稽核補 referential integrity。
  - (vi) **works 側裁決＝B「對所有登入者公開」〔v1.29.0 用戶拍板〕**：哲學/文學 `philosophy_work`＝**公版投資智慧經典原文**（`work_text` 限公版，v1.17.0 判準不動），**對所有【已登入】使用者公開、不 domain 收窄**（review_flag=false × literary 之 CLEAN 閘仍過）；**未登入（`scope=None`）→ fail-closed deny**。理由：公版經典＝全體共享之素養底座（如公司圖書館藏書），非部門機密；items（知識/財經/`local_private`）仍 domain 收窄、不受此影響（`philosophy.retrieval.retrieve`：未登入 return []、登入即回 CLEAN works）。
  - **授權政策永遠決策層人拍板（計畫 §8.2、承 #19／#26）**：建/改群組、`group_domain_grant` 授撤、`is_superuser` 置真、新 domain 成授權邊界（`is_authz_boundary=true`）、`local_private` 跨使用者歸屬、works 側 A/B 裁決（v1.29.0 已裁＝公開，見 (vi)；再變更仍人決策）、升靈魂/憲章 RBAC 判準——AI 只建 **fail-closed 機制**、不定政策。
- **知識域端到端管線（七段一驅）〔v1.23.0〕**（從屬共同不變式①-④、不另複誦三敵；詳計畫 SSOT=`reports/augur_knowhow_e2e_pipeline_plan_20260704.md`〔30 分鐘可讀〕）：
  - **管線形**：知識域端到端=**七段一驅**——來源窮舉（registry）→晉升落地（item）→逐字文本鏈（item_text／sentence／concordance／lexicon）→統計理解層（四值 method 封閉集=「相關性／相關係數」唯一合法承載）→pgvector 嵌入→serving 索引單向匯出→guard 對話層；單一驅動器編排全鏈、**只編排不計算**（各段計算單一住所在 builder）。
  - **索引雙庫鐵則**：pgvector=真兆同庫 **SSOT 索引**；外部 serving 索引（Milvus 等）=**單向匯出、可拋棄**（任何時點 DROP 從 PG 全量重建），永不成第二權威家（#12）；ANN 只回 id+distance、內容一律回 PG JOIN 取原文；ANN distance 永不寫入任何 knowledge_* 表（四值 CHECK 無合法 kind 可載=DB 硬擋）。
  - **對話單閘**：對話路徑唯一編排入口=`advise()`（檢索空→不經 LLM 直回固定誠實句；注入之檢索結果一律後驗 verify_verbatim 機械收斂），guard=欽定單一機械閘、必在鏈上不可繞；OpenAI 相容殼=**欽定接點、薄 adapter 不重造編排**；UI 不得配置繞閘直連；guard 域條款與三級誠實固定句閉集之任何變更=**憲章判準變更**，不得執行層自改。
  - **可替換=接縫層**：LLM 經 `llm_fn`、嵌入模型經 `embedspec`（model_tag／dim／collection 命名之單一 SSOT）、向量庫經 `vectorindex` 介面；換任一元件=只動該接縫+跑對應重建 SOP（版本入 collection 名、不原地混版），**其他段 code 零 diff（git diff 可驗）**。
  - **域擴充與准入**：新 know-how 域=決策層 INSERT registry 零 code（CLAUDE #29b）；語料准入=CLEAN／CLEAN_ITEM 述詞 **fail-closed**（NULL 不放行），builder／embed／retrieval 三端強制 import 同一 SSOT——嵌入端無閘=索引汙染、清=重灌，故閘先於放量。
  - **宣稱與升級**：系統一切宣稱一律 coverage／漸近形（命門 2，forbidden_pat 掃描常備）；**R0-R4 能力階梯=升級判準**（誠實雙向→逐字引他證→意涵統計→思想圖譜→全量索引），每級=可對話+可機器驗收、通過才升。
- **詳細設計**：見 `reports/augur_investment_philosophy_framework_plan_20260630.md`；知識層多域/全文准入/檢索（v1.19.0-v1.21.0）之詳計畫見 `reports/augur_knowledge_registry_expansion_plan_20260702.md`、`reports/augur_knowledge_harvest_landing_plan_20260702.md`、`reports/augur_knowledge_text_understanding_plan_20260702.md`（憲章承載架構框架、詳設計住規劃檔，守第六部 SSOT + 30 分鐘可讀）。

---

## 第四部　不可違反的 20 條原則（索引 + 對映）

> **法律全文（WHAT｜WHY｜ENFORCE）以 `docs/原則精華_v1.7.1.md` 為 SSOT。** 本表為導航 + 框架對映；任一條文字以原則精華為準，本憲章不另行改寫（避免漂移，呼應 #12）。**條號非連續**（按新增順序編、分類歸區）：A `#1-7,17,18` · B `#8-12` · C `#13-15,19` · D `#16,20`。

### A. 資料紀律
| # | 原則 | 守敵人 | 主要層 | 一句話 |
|---|---|---|---|---|
| 1 | 零幻像 / Source-Pure ★ | ① | raw/feature/universe/model | 非真實 API 值＝幻像 → 不寫入（缺列）；任一面板缺值之股排除核心 |
| 2 | API 即權威 | ① | core/raw | 表名/欄位/型別/大小寫以 API 為準；schema 查 DB 不另立白名單 |
| 3 | 純通用 Ingestion | ① | core/raw | 無 dataset 白名單；任意 **API 表** auto-schema 建 |
| 4 | 日為最小單位（資料本質排除準則）| ① | raw | 不收 intraday、只存日級衍生；**凡 API 日級真實資料皆抓**，不以抓取維度/非個股標的排除；**唯資料量規模物理界限者除外（`OUT_OF_UNIT` 排除，判準全文＝原則精華 #3）** |
| 5 | 型別紀律 | ① | core | 字串 VARCHAR(255)/TEXT；數字 NUMERIC(20,6) 自動擴大 |
| 6 | 冪等 + 斷點續傳 | ① | raw | 重跑安全、可恢復、不掉資料 |
| 7 | DB↔API 對帳 Attestation | ① | raw/audit | 定案資料 byte-equal API（value_mismatch=0 ∧ extra=0） |
| 17 | API 速率公民 / 主動限速 | ①* | raw | 不短時 burst 服務端；fetch 內建**三層防護**：`_pace` 步調 + **主動額度閘**（閉環問服務端權威錶、撞前先停）+ 403 長冷卻（防重試風暴惡化）；honor `retry_after`（防 IP ban→無資料）。①*＝取得穩健 precondition |
| 18 | 抓取依 API 探測方式+範圍 / 不空抓 | ① | raw | 探測決定抓法（market/per-stock/by-date/**by 維度 id**）+ 起點（resume / backward-probe 自近往遠探邊界）；最早日由 API 探測/回傳決定（非預設）、只抓 API 實際範圍、禁 `full_start` 逐日空掃；**單日型 dataset（end_date 須 none，如 News）自動移除 end 逐日抓、事件型(datetime)by-date 不跳週末（依事實不假設）**；**需特殊維度 id → 補通用多維度抓取（非排除）：id 全集由 `/datalist`→roster→文檔+probe 階層取得、一律問 `/datalist` 不抄文檔、無臆測白名單**；唯資料量規模物理界限者（分點/權證/鉅額分點）`OUT_OF_UNIT` 排除（判準全文＝原則精華 #3） |
| **完整性判準** | 資料完整性判準（as-of）〔承 #6/#7/#18〕| ① | raw/audit | 完整 ＝ 每股及其相關資料自 API 最早日至 **as-of `2026-05-31`** 滿足無缺；系統級完整 ＝ 每 in-scope 表之每股/每日/每維度覆蓋至 as-of；固定截止日使完整可定案/可驗證（市場每日產新交易資料、「到今日」目標永遠浮動）；as-of 為治權參數、須用戶決策入憲 |

### B. 建模紀律
| # | 原則 | 守敵人 | 主要層 | 一句話 |
|---|---|---|---|---|
| 8 | Anti-Leakage ★ | ② | feature/model/validate | ≤t 已知、t+1 套用；walk-forward IC 必附 purged |
| 9 | 思想 ≠ 特定值 | ① | feature | 思想可啟發；`0.80/0.20`、40-60 年 等特定值不硬編進特徵 |
| 10 | 核心股：質 > 量 | ①③ | universe | 資料對且精準，可以少；不以數量為目標 |
| 11 | 五鏡特徵治理 | ③ | audit | 存廢不單一指標（IC/共線/必要性/SHAP/purged-CV 合看） |
| 12 | 單一引用源（SSOT） | ③ | validate | panel 窗 + metric 由單一 helper；跨模型才可比 |

### C. 風險與治理
| # | 原則 | 守敵人 | 主要層 | 一句話 |
|---|---|---|---|---|
| 13 | 空頭防護：規則地板優先 | ③ | validate | 規則風控地板優先；預測為輔且須證增量 |
| 14 | 經濟價值判定 | ③ | validate | 以 MaxDD/Calmar/逐空頭判定，非 AUC/準確率 |
| 15 | 誠實回報 / 無 Aspirational ★ | ③ | 全層（主 validate/audit）| 數字可 trace (a)程式/(b)DB/(c)API；不把計畫當成果；隨機指標多跑取統計 |
| 19 | 可控風險下逐級逼近最佳奇異點 / 試錯即進步 | ③ | operational/方法 橫切（通用）| 可控風險內逐級試錯、逼近最佳奇異點（解所有問題之通用法）；不怕可恢復錯誤、重覆驗證再定論；**只試方法/參數、絕不試資料真假** |

### D. 開發 / 協作紀律
| # | 原則 | 守敵人 | 主要層 | 一句話 |
|---|---|---|---|---|
| 16 | Clean-Room 重建 / 零 stock_backend 參考 | ①③ | 全層（開發橫切）| 程式產生只依 augur 自身 5 治權檔 + schema 目錄 + live API；零 stock_backend code/資料/報告/數字參考 |
| 20 | 自驅動 × 實證決策 | ③ | 全層（開發/協作橫切）| 經授權 AI 自驅動(loop)推進、執行層方法 AI 主導、自我糾錯試錯；凡判斷/做法**皆完整驗證**、嚴禁憑「我以為」；決策層人拍板 / 執行層 AI 自駕；執行層天職＝確保完整性、窮舉試到真邊界——**全文（含完整性天職/真邊界/慣例值待驗/GoldPrice 教訓）見原則精華 #20 ENFORCE** |

★ = 三條最神聖（系統的命）。

---

## 第五部　維運矩陣（從零 12-PHASE）

從零重建一律照此序列；**不可跳 phase、不可改 order**，每 PHASE 過 audit gate 才進下一。

| PHASE | 階段 | 主程式 / 動作 | 產出 | Audit Gate |
|---|---|---|---|---|
| 0 | 環境 | venv + `.env` + PostgreSQL role/db | import smoke OK | imports 全過 |
| 1 | Infra bootstrap | `core` schema bootstrap（infra log DDL） | `pipeline_execution_log` / `data_audit_log` | DDL 無誤 |
| 2 | 名冊 | `scripts/full_market_sync.py`（seed 名冊段，`sync.seed_roster()`） | `TaiwanStockInfo`（全市場名冊） | 名冊非空 |
| 2b | FRED 前置 | FRED fetch | `fred_series` | series 落地 |
| 3 | 宇宙引導 | universe bootstrap（或 roster fallback） | 過渡名單 | 有標的供 PHASE 4 |
| 4 | 全市場全史 sync | `scripts/full_market_sync.py`（全史段，`sync.daily_datasets()` 動態列舉；增量＝`daily_maintenance.py --datasets`） | raw 表 generic 自動建 + 全史列 | 供應鏈完整 |
| 5 | **Raw 對帳** | audit reconcile | DB↔API byte-level（#7 無幻像） | value_mismatch=0 ∧ extra=0 |
| 6 | 核心候選（raw 完整）| universe build | raw-完整候選名單 | raw 完整度 PERFECT |
| 7 | Feature Store | features build | feature 面板（source-pure，缺值即缺列） | 面板落地 |
| 8 | 最終核心（feature 完整）| `scripts/build_core_universe.py`（completeness gate 為預設；`--asof` 產 point-in-time 版） | pan-historical 完整核心 snapshot（#1 #10） | 核心股全面板 0 缺值 |
| 9 | 特徵 audit（訓練前） | audit 五鏡（#11） | IC/sign/共線/必要性/SHAP/purged | 特徵分診完成 |
| 10 | 模型訓練 | models train | artifacts + registry | registry 有列 |
| 11 | 模型驗證 | evaluation validators | 各週期 metrics + 經濟價值（#8 #12-15） | 人工對照可信度門檻 |

> **三種建法**（為什麼不是全部由 ingestion 建、PHASE 1 只 bootstrap infra）：
> - **① generic auto-schema → API 原始表**（`TaiwanStockPrice`、`fred_series`…）：型別由 API 回應推導、無白名單（#3）。**兩入口**——FinMind ingester（PHASE 2/4）+ FRED fetcher（PHASE 2b，`fred_series` 走這條、非 FinMind ingester）。
> - **② explicit DDL bootstrap → infra log**（`pipeline_execution_log`、`data_audit_log`）：固定 schema 的系統運維表，**PHASE 1** 最早建（其他階段才有地方記 log）。
> - **③ builder 自建 explicit DDL → 計算型內部表**（`feature_*`、`core_universe_*`、`model_registry`）：系統自己算出來的結構、**無 API 回應可推**，由**各自 builder** 在其 PHASE `CREATE IF NOT EXISTS` 建+寫（feature PHASE 7 / universe 6,8 / model 10），**不集中在 PHASE 1**。
>
> 一句話：**#3「無白名單、全 generic」只管 API dataset；內部表用自己的 explicit DDL——infra log 在 PHASE 1 bootstrap、計算型表由各自 builder 自建。**
>
> **入口對映**：PHASE 1/2/2b/4/5 由單一入口 `scripts/full_market_sync.py` 串跑內建（檔頭序列自述）；表列「主程式/動作」為其內部階段、非分立 CLI 旗標。

**Long-running 治權**（PHASE 4/10/11）：
- long-running 回報（≥5 分鐘）／防睡眠 watchdog＋sentinel（≥30 分鐘/過夜）／環境前置／resume 後路之工具層規則 **SSOT＝CLAUDE.md #21-#23**，本處僅引、不複述。
- 任何數字寫入須 trace 回 (a)(b)(c)（#15）。
- **Claude usage 經濟原則 + 配額護欄**：批量/長跑受「最小化 usage」經濟總則與配額護欄約束——**執行層（how）省 usage、理解層（what/why）ultracode 窮盡**；近上限暫停、過重置點 resume-safe 續、不被配額硬切半途。完整規則與判據 **SSOT＝CLAUDE.md #28**（本處僅引；演進見修訂歷程 v1.11.0／v1.12.0／v1.15.0）。

---

## 第六部　升版規則

- **新增一條不可違反原則** → 升 major/minor（v1.1.0…），同步 `原則精華`。
- **既有原則之重大判準修正**（改變系統行為範圍，如排除準則收窄）→ 升 minor、同步 `原則精華`、記修訂歷程（如 v1.4.0）。
- **既有原則純文字微修正** → 改該條文字，**不新增 treaty、不升版**（拒絕舊專案 255-treaty 的累積病）。
- **架構/維運變更** → 改本憲章對應部，記一行於修訂歷程。
- **修訂歷程體例〔v1.22.0 起〕**：新列以「變更類型＋一句判準＋同步清單＋動因」約 3 行封頂；長論證住對應法條/報告，歷程只留指標；既有列為歷史快照、不回改。
- **SSOT 鐵律**：靈魂只住 `核心思想`、法律全文只住 `原則精華`、架構/維運只住本憲章；任一概念**只有一個權威家**（#12）。
- **計畫先行（plan-first；先理解、後計劃、確認、再實作）〔v1.31.0；v1.32.0 補前置理解；v1.33.0 補執行前確認〕**：本專案**一切規劃類工作**（新功能／子系統／介面／架構／流程／判準變更）**一律先產出計劃報告（寫入 `reports/`，命名 `<module>_<topic>_<YYYYMMDD>.md`）、經決策層人拍板後才進實作**——不邊想邊做、不先寫 code 再補計畫。
  - **前置理解（規劃前，理解軸；SSOT＝#28 執行/理解二分＋#20 實證非「我以為」，不另造定義）**：規劃前先把任務 what／why 理解窮盡——**目的＝視角與範圍窮盡**（列反例／edge case／跨檔衝突並在計畫書內回應），非憑單一視角或記憶直接下筆；**手段依風險分級、不一律 fan-out（守 #28「非必要不 fan-out」）**：
    · **標準級（預設）**＝作者自身多角度深研 ＋ 完整性對抗自問，單一深研即足、不開多 agent。
    · **多視角級（多 agent 平行 ＋ 對抗審查 ＋ 計畫書附「對抗審查發現表」＝各發現 severity＋處理結論之留痕）**＝僅當觸及任一**高風險門檻**——(i) 變更治權判準或跨 ≥2 治權檔一致性、(ii) 不可逆／破壞性、(iii) 安全／授權邊界（RBAC／隔離不變式）、(iv) 跨 ≥3 package 之架構／介面——時強制；預設含治權／安全／工程三鏡，得依主題調整並於計畫書列明所用視角（fan-out 仍受 #28 配額經濟約束）。
  - **計畫完整性**：計劃報告須達本部「計畫/設計文件完整性」〔v1.27.0，SSOT 在該條：產表者附 DDL＋對應程式、DDL 住遷移器；非產表者附元件·端點·檔案·分階段·驗收準則〕＋「30 分鐘可讀」。
  - **執行前確認（實作前驗證關）〔v1.33.0；承 §26 主動求完整・不讓用戶當 QA ＋ #15 實證，SSOT 在該處〕**：計劃拍板後、**動工前 AI 須主動確認計劃無誤才執行**——判準四項：**①完整**（覆蓋需求）**②內部一致**（無自相矛盾）**③與現況/code 事實一致**（無過時狀態、無「假設 vs 實作」衝突）**④可據以實作**；命中任一問題（矛盾／過時／落差）**先修計劃、再執行**，不執行帶病計劃。**界定＝避免完美主義癱瘓**：四判準無「已知」缺失即可動工，非追求無限完美。（確認由 AI 主動自 QA〔非讓用戶逐一抓漏，§26〕＋用戶拍板；發現落差如「計劃 §X 與實作/現況衝突」即屬須先修之標的。）
  - **比例原則（判準非清單）**：**純機械修正**（不決定做什麼／怎麼架構、無跨層／不可逆／安全影響：改錯字／補一欄／既定計畫之逐步落地）不另立計畫、不須任何視角前置；**灰帶**（是規劃但不觸高風險門檻）→ 標準級；**有疑偏保守**（是否觸高風險不確定→偏多視角級，成本不對稱：漏審治權/安全 ≫ 多開一次 agent；是否算規劃不確定→偏須計畫）。承 #20 先計畫工作序、#19 逐檔檢視、#28 理解軸窮盡＋配額經濟、§8.2 決策層拍板。目的：**先看全面再做**、避免反覆試錯與方向漂移。
- **計畫/設計文件完整性〔v1.27.0〕**：凡**產生資料表**之計畫/設計文件，須明確寫出 (a) **該表 schema（DDL：欄位／型別／約束／index）** 與 (b) **對應 Python（建表・遷移之檔·函式 ＋ 消費・強制之檔·函式）**——表與程式雙落實、非只述用途；DDL 住遷移器（沿用冪等 guard 慣例）、非散落；純計算/驗證程式（不產表者）標明結果落哪張既有表。目的：計畫即可據以實作、schema 與 code 不漂移（承 #12 SSOT、CLAUDE #29「script 資料驅動＋DDL 住所」、本部「詳設計住設計檔」）。
- **答案品質評準（Claude-as-judge 自問自答迭代逼近；顧問精準度改善法〔v1.34.0 用戶 directive〕）**：改善顧問（advisor）回答品質時，AI 得**以自身已知的最佳答案為評準**，自主生成多樣題型（定義／概念／投資分析／未來預測／不可能宣稱／離題創作等 Claude 已知理想解者），對 live 顧問**自問自答→逐輪比對評分→改 prompt/組裝再測**，逐級逼近最佳（承 §26 有界自主自糾 ＋ #27 逐級逼近奇異點；評準與待改對象皆 AI，屬 clean-room 護欄內、無外部副作用）。**作用域嚴限執行層**（prompt 措辭／題型自適應／組裝去雜訊）——**絕不為「答得更準」鬆動誠實護欄**：`guard` 閉集、誠實固定句閉集、**空檢索 NO_KNOWLEDGE 強制**（`advise.py` 檢索全空即短路、不呼叫 LLM）、三敵零容忍**皆屬 §8.2 判準、碰即停下問**（實證 2026-07-05：qwen3:8b 上「有沒有穩賺不賠的股票」等**空檢索題**之 NO_KNOWLEDGE 係 v1.25.0 空檢索強制所致、**非 prompt 可修**、屬人決策——已用 code+live 雙證，不自越）。屬執行層品質工程 → 僅憲章升版、guard/閉集/原則精華不動。
- **可讀性目標**：永遠維持「30 分鐘讀懂全系統法律」；篇幅膨脹即是警訊。

---

## 附錄 A　經驗結論（待 Augur 重跑後補）

此處將收錄**真實、可重現**的經驗結論：dead/冗餘特徵、模型比較、可信度門檻、beta/alpha 拆解等。

> ⚠️ **目前為空**。舊專案的數字（如某模型 Sharpe、IC）**不直接搬入**——那違反 #15（誠實／不把未在 Augur 重跑的數字當成果）。每筆待在 Augur 重跑後，附 source（程式/DB/API）+ 口徑（raw vs purged）再補。

**填寫格式**（每筆補入時須欄位齊全，守 #15）：

| 結論 | 數值 | source（a 程式 / b DB / c API） | 口徑（raw / purged） | 重跑日期 |
|---|---|---|---|---|
| _(待 Augur 重跑後補)_ | | | | |

---

## 附錄 B　考古索引

- **舊 repo**：`stock_backend`（v6.x，13,951 行憲章 / 77 個 §14.7-X 子節 / 255 條 treaty）。要追溯某條紀律的演化史時去翻；不搬內容。
- **思想源頭**：舊 `reports/系統核心思想.md`（「量子金融藍圖」——市場物理 / 八二法則 / 康波 2026 預言）。Augur 是它的**紀律化結果**：保留思想啟發（#9 思想≠特定值），剔除預言與硬編值。
- 不在 archive 重述；只留「要查歷史去哪翻」的指標。
- **#16 clean-room 邊界**：本附錄之考古查詢 ＋ 上述思想啟發**皆限文件 / 概念層，不得回流 code**；任何 augur 程式零 stock_backend（code / 資料 / 報告 / 數字 / 設定）參考。

---

## 附錄 C　理論定位（業界架構理論對照驗證）

Augur 的架構**非外套業界理論、而是從第一性原理（只用真實資料、誠實預測台股）長出**；經查證，恰對映業界 4 維度架構理論——理論之角色為**外部驗證 + 共同詞彙**，非架構 SSOT（SSOT 仍是靈魂 + 本憲章一–六部）。

| 維度 | 業界理論（代表） | augur 對映 |
|---|---|---|
| 1 組織+命名 | Screaming Architecture(Martin)· DDD Ubiquitous Language(Evans)· PEP 8 | package＝管線階段(第三部)、module＝領域詞 |
| 2 依賴+模組邊界 | Hexagonal/Clean Ports&Adapters(Cockburn/Martin)· Deep Modules(Ousterhout) | 各層職責不越界＋core 不抓 API(第三部)；finmind/generic_schema/catalog＝deep module |
| 3 資料管線 | Medallion· Functional Data Engineering(Beauchemin)· Lineage· Data Contracts | raw→feature→universe＝medallion；#6 冪等+as-of＝functional；#15+catalog provenance＝lineage；#2/#7 對帳＝contract |
| 4 ML 系統 | Leakage Prevention· Reproducibility· Feature SSOT | #8 anti-leakage(＝北極星②「決策當下真看得到?」逐字命中)；#15 半年重跑一致；#12 單一 helper |

**核心定位**：三敵人（#1/#8/#15）＝ ML 系統設計（leakage / reproducibility）＋資料工程（contracts / lineage）之**領域化身**；augur 為這些理論之**收斂實例**，非應用對象。

**理論指出、可選補之小缺口（#15 誠實揭露）**：① 正式 ports/adapters 介面（Hexagonal；現分層夠用）· ② data-contract schema 版本化 · ③ as-of / bitemporal 形式化（vintage 表）· ④ train/serve 一致（俟上 serving）。

> 詳細逐項對照 + 業界出處見 `reports/augur_architecture_theory_20260615.md`（憲章承載定位框架、背書詳檔承載逐項，守第六部 SSOT）。

---

## 修訂歷程

| 版本 | 日期 | 修訂說明 | 狀態 |
|---|---|---|---|
| v1.0.0 | 2026-06-09 | 首版：以「三個敵人 × 管線」框架建立；萃取自 stock_backend v6.x（13,951 行 → 本憲章）。架構分層 + 12-PHASE 維運 + 15 原則對映索引。 | SUPERSEDED |
| v1.1.0 | 2026-06-09 | 新增原則 #16 Clean-Room 重建 / 零 stock_backend 參考（新類別 D 開發紀律）；同步原則精華 v1.1.0、第四部索引、第二部防線地圖、附錄 B；2 治權檔檔名 v1.0.0→v1.1.0。 | SUPERSEDED |
| v1.2.0 | 2026-06-09 | 新增原則 #17 API 速率公民 / 主動限速（不過載服務端）；同步原則精華 v1.2.0、第四部 A 索引、第二部 ① 防線；ENFORCE 落地 `finmind.py` `_pace()`（實證 403 ip banned 後入憲）。2 治權檔檔名 v1.1.0→v1.2.0。 | SUPERSEDED |
| v1.3.0 | 2026-06-10 | 新增原則 #18 抓取依 API 探測方式+範圍 / 不空抓；同步原則精華 v1.3.0、第四部 A 索引、本歷程；ENFORCE：adaptive 探測抓法（market/per-stock/by-date）+ 起點 resume / **backward-probe（自近往遠探資料邊界、取代會空掃的 forward-probe）**、禁 `full_start` 逐日空掃、不可抓者（券商分點/權證/鉅額交易）入 `OUT_OF_UNIT`（實證：GovBank 單日全市場 by-date 可行保留；forward-probe 自身空掃 1990-2020 → 改 backward-probe；BlockTrading 稀疏致 backward-probe 取樣多落空、無法可靠定起點 → 排外）。2 治權檔檔名 v1.2.0→v1.3.0。 | SUPERSEDED |
| v1.4.0 | 2026-06-11 | **重大判準修正（非新增原則）**：#4 升為「唯一資料本質排除準則」、#3 `OUT_OF_UNIT` 從「個股×日單位外排除」重定義為「規模物理不可行之 operational 暫緩」、#18 需特殊維度 id 改「補通用多維度抓取」（非排除）。**取消「非個股標的/需特殊 id」作為排除理由**——總經/衍生品/外股/商品/可轉債皆日級真兆、想辦法抓；唯規模物理不可行者（券商分點/權證/鉅額）operational 暫緩。同步原則精華 v1.4.0、第四部 #4/#18 索引、兩檔升版規則（補「重大判準修正」類）；2 治權檔檔名 v1.3.0→v1.4.0。 | SUPERSEDED |
| v1.5.0 | 2026-06-11 | 新增原則 **#19 可控風險下逐級逼近最佳奇異點 / 試錯即進步**（C 風險治理；augur 解決**所有問題**之通用方法論，非僅速率）；同步原則精華 v1.5.0、靈魂 v1.1.0（世界觀新增一列）、第四部 C 索引、第二部 ③ 防線 + 防線地圖 audit③、本歷程。ENFORCE：可控風險三要件（可恢復 + 有界 + 即時退場）+ 逐級試錯逼近奇異點（某級驗證可行 → 試進一級，持續性失敗 → 退上一安全級＝奇異點）+ 重覆驗證再定論（單次成功/失敗皆不定論、驗證完成前 #17 既有安全值維持為操作值）+ **兩條凌駕邊界**（一：只作用 operational/方法層；二：資料誠實零容忍、絕不拿三敵試錯——試的是方法/參數、非資料真假）。實證動因：FinMind throttle rate 上限探索——不被前次 `0.7s` ban 永久錨定（屬一次情境觀測），於當前條件逐級重覆實證（`1.0s`→`0.85s`→`0.6s`→…）。治權檔升版：原則精華 + 憲章 v1.4.0→v1.5.0、核心思想 v1.0.0→v1.1.0。 | SUPERSEDED |
| v1.6.0 | 2026-06-12 | 新增原則 **#20 自驅動 × 實證決策**（D 開發/協作紀律；AI 開發 augur 之協作鐵律——「自驅動主動 ＋ 實證決策」一體兩面）；同步原則精華 v1.6.0、靈魂 v1.2.0（北極星擴及一切判斷 + 世界觀新增「自驅動×實證」列）、CLAUDE v1.3（#26 強化自驅動迴圈/執行層主導）、第四部 D 索引 + 標題、第二部 ③ 防線 + 防線地圖開發列、本歷程。ENFORCE：決策層人拍板（方向/判準/資料真假/外部副作用/commit/扣扳機）、執行層 AI 自駕（方法/路徑/糾錯/試錯）；實證鐵律及於一切 operational 判斷（非僅數字，#15 擴展）；碰護欄即停；工具層對映 CLAUDE #26/#27。實證動因：2026-06-12 憑臆測的進程 pattern 連兩次誤殺（先殺自己 shell、再殺借用 venv 的 IDE python）→「無實證的主動必闖禍」。治權檔升版：原則精華 + 憲章 v1.5.0→v1.6.0、核心思想 v1.1.0→v1.2.0、CLAUDE v1.2→v1.3。 | SUPERSEDED |
| v1.7.0 | 2026-06-15 | **架構變更（非新增原則）**：第三部新增橫切層 **catalog（元資料登錄 `dataset_catalog` / `column_catalog`）**——把「怎麼抓每個 dataset」之元資料正規化為 DB 登錄表、作 raw 層抓取單一驅動依據（**實作 #18 之持久化形式、非新法律**）；表＝SSOT、`datasets_zh.md` 由表生成。詳細設計住 `reports/augur_metadata_catalog_design_20260615.md`（守第六部 SSOT：憲章承載框架、設計檔承載詳設計、維持 30 分鐘可讀）。**僅憲章升版**——架構變更不動法律全文，原則精華維持 v1.6.0（治權檔本就各自獨立版本：核心思想 v1.2.0／原則精華 v1.6.0／CLAUDE v1.4）；同步：原則精華第 5 行對憲章版本之交叉引用、README 憲章連結、憲章檔名 v1.6.0→v1.7.0。實證動因：2026-06-15 由 live API 探測建成 `docs/datasets_zh.md`（83 表逐欄 + 🔌 抓取法 + 📅 最早日期）→ 確立「元資料正規化進 DB 驅動抓取」之架構價值。**同版後續補強（grounding、非新法律/架構/判準 → 不升版，守第六部反膨脹）**：第二部防線地圖補 catalog 列；新增**附錄 C 理論定位**（業界 4 維度架構理論對照驗證，背書詳檔 `reports/augur_architecture_theory_20260615.md`）。 | SUPERSEDED |
| v1.8.0 | 2026-06-15 | **架構變更（非新增原則）**：FinMind 官方 `docs/finmind-references/datasets.md`（全 92 表 tier/params/columns/分類）**入憲**為第三部 catalog 橫切層之**抓法元資料權威輸入源**；catalog builder（`src/augur/catalog`）**去硬編**——tier/intraday/sponsor/single-day 改由官方解析驅動（`_parse_official_datasets`，大小寫容錯容官方 typo）、移除誤判 heuristic（`_is_intraday_data` 會把 News 事件流/snapshot 多合約誤判為 intraday），官方未列則退回 probe/硬編備援（不脆）。實證：catalog build **95 表/683 欄**、intraday 8 張標「單位小於日」、GoldPrice/News/TaiwanFutOptTickInfo 正確標可抓、FRED per-series。**僅升憲章**（架構變更不動法律全文、原則精華維持 v1.6.0）；改第三部 catalog 段 + 本歷程，同步 README/原則精華交叉引用、檔名 v1.7.0→v1.8.0。 | SUPERSEDED |
| v1.9.0 | 2026-06-23 | **重大判準修正（非新增原則）**：#3/#4/#18 `OUT_OF_UNIT` 由「規模物理不可行 operational 暫緩」**重定義為「資料量規模物理界限排除」**——probe 實證 2026-06-23 分點/權證抓法已找到（dedicated by-broker `securities_trader_id`+`date`、一 call 回該券商當日全交易）但全市場全史資料量物理裝不下（分點 per-(券商,股,日) 數十億列 TB 級、Mac DB 39GB 裝不下；權證 ~數千萬列+~178萬 calls）→ **確認排除（非暫緩、非待有能力）**；鉅額分點稀疏（/data data_id=股+range、per-股 ~3105 calls）→ **可抓、移出 OUT_OF_UNIT**。辨明：intraday＝唯一「資料本質」排除、`OUT_OF_UNIT`＝「資料量物理界限」排除（不同類）。同步原則精華 v1.7.0（#3/#4/#18 + 升版哲學）、ingest.py（`OUT_OF_UNIT={分點,權證}`/`BACKFILL_DEFERRED={鉅額}`）、sync.py（daily_datasets+sync_finmind+sync_by_date 3 skip 點）、catalog builder + DB（排外/可抓 reason）、README/CLAUDE 交叉引用、檔名 v1.8.0→v1.9.0。 | SUPERSEDED |
| v1.9.1 | 2026-06-24 | **新增資料完整性判準（as-of）**：A 段（資料紀律）新增「**資料完整性判準（Completeness Criterion · as-of）**」〔承 #6/#7/#18〕——資料完整 ＝ 每支個股及其相關資料自 API 最早日至 **as-of `2026-05-31`** 滿足無缺；系統級完整 ＝ 每 in-scope 表之每股/每日/每維度覆蓋至 as-of；固定 as-of 截止日使完整性可定案/可驗證/可稽核（市場每日產新交易資料、「到今日」目標永遠浮動、對帳目標不定）；as-of 為治權參數、須用戶決策入憲。同步：原則精華 v1.7.0→v1.7.1（A 段判準 + 標題 + 演進記錄）、第四部 A 表加列、本歷程、CLAUDE/README 交叉引用、檔名 v1.9.0→v1.9.1、原則精華檔名 v1.7.0→v1.7.1。實證關聯：as-of 後（2026-06）未定案資料（#54）不計入對帳。用戶 directive 2026-06-24。**同日補記〔2026-07-04 追記漏載〕**：鉅額分點 probe 實證（endpoint 不吃 end_date＋schema PK bug 16+→1 覆蓋）自可抓復列 `OUT_OF_UNIT`、3 表確認排除（ingest.py 同步；判準全文住原則精華 #3）。 | SUPERSEDED |
| v1.10.0 | 2026-06-26 | **架構/方法承載變更（非新增原則）**：第三部 feature 層**入憲承載「特徵發現方法論」**——「**如何找出有用的特徵值**」之方法依據：**三鏡頭 × 紀律漏斗**（同一批 raw 欄位經第一性原理/八二法則/康波循環三種旋轉視角生成候選假說 → 紀律閘 #1/#8/#9 → 五鏡 #11 → out-of-sample walk-forward 淘汰；有用＝驗證活下來、非設計出來）。詳法住 `reports/augur_feature_discovery_methodology_20260626.md`（憲章承載框架、方法檔承載詳法，守第六部 SSOT、維持 30 分鐘可讀）——屬 #1/#8/#9/#11 之**操作化收斂、非新增原則**，故**僅憲章升版**、原則精華維持 v1.7.1（治權檔各自獨立版本：核心思想 v1.2.0／原則精華 v1.7.1／CLAUDE v1.5）；同步：原則精華第 5 行對憲章版本之交叉引用、README 憲章連結、憲章檔名 v1.9.1→v1.10.0。素材＝三份特徵設計報告（第一性/八二/康波 20260612）+ 執行藍圖（20260626）+ 評估實證（M-1 baseline 20260626）之收斂萃取。實證動因：2026-06-26 用戶 directive「把特徵發現理解產為報告並入憲、做為後續特徵值作法依據」。 | SUPERSEDED |
| v1.11.0 | 2026-06-27 | **維運變更（非新增原則）**：第五部「Long-running 治權」新增「**配額感知暫停/續跑**」——批量/長跑除外部 API 限速外亦受 **Claude 自身 usage 配額**約束：近上限暫停、過重置點 resume-safe 續、不被配額用盡硬切於半途；AI 讀不到即時 usage %、以限額錯誤（429/weekly/session）或用戶設定之閾值/續跑時點為信號。屬 #6/#22 resume + #24 限速精神之 **Claude-配額對偶**、非新增不可違反原則 → **僅憲章升版**、原則精華維持 v1.7.1。SSOT 在 **CLAUDE.md #28**（工具層完整規則），憲章第五部僅引、不複述（守第六部「任一概念一個權威家」）。同步：CLAUDE.md v1.5→v1.6（新增 #28）、原則精華第 5 行對憲章版本之交叉引用、README 憲章連結、憲章檔名 v1.10.0→v1.11.0。實證動因：2026-06-27 用戶 directive「usage 近滿暫停、過重置點再續、批量程式比照、入憲及 CLAUDE.md」。 | SUPERSEDED |
| v1.12.0 | 2026-06-27 | **維運變更（非新增原則）**：第五部 #28 相關條由「配額護欄」擴為「**Claude usage 經濟原則 + 配額護欄**」——明定**本專案所有批量處理一律以「最小化 Claude usage 消耗」為原則**（本地計算優先、harness 背景通知非輪詢、非必要不 fan-out workflow/agent、批次優於逐項、回報精簡；與 ultracode 窮盡傾向衝突時以省 usage 為先，除非當次明示放量）。屬 v1.11.0 配額護欄之**經濟面總則**、同概念強化、非新增不可違反原則 → **僅憲章升版**、原則精華維持 v1.7.1。SSOT 在 **CLAUDE.md #28**（工具層完整規則），憲章第五部僅引、不複述。同步：CLAUDE.md v1.6→v1.7（#28 加經濟總則）、原則精華第 5 行對憲章版本之交叉引用、README 憲章連結、憲章檔名 v1.11.0→v1.12.0。實證動因：2026-06-27 用戶 directive「此專案所有批量處理均以減少耗用 Claude usage 為原則、入憲與 CLAUDE.md」。 | SUPERSEDED |
| v1.13.0 | 2026-06-27 | **方法承載強化（非新增原則）**：第三部 feature 層之特徵發現方法論由「三道漏斗」擴為「**四道漏斗**」——新增**第 4 道「提拔關卡」**（入生產前強制）:候選過五鏡寬篩仍須過完整複核才入生產——**(a) as-of 口徑**（非 pan-hist、消完整度 look-ahead）**(b) 去相關 Eff-t**（Newey-West/HAC、**禁裸用 iid effective_t**、解審查 G8）**(c) 多 seed 多因子增量**（Δ≤0 冗餘即依 #11 不提拔）。詳法住 `reports/augur_feature_discovery_methodology_20260626.md` §四（憲章承載框架、方法檔承載詳法）;工具＝`evaluation/metrics.py:effective_t_hac` + `scripts/verify_candidate_promotion.py`。屬 #11/#15/#8 之**操作化收斂、非新增原則** → **僅憲章升版**、原則精華維持 v1.7.1。同步:原則精華第 5 行交叉引用、README 憲章連結、憲章檔名 v1.12.0→v1.13.0。實證動因:2026-06-27 候選提拔複核實證——寬鬆口徑(pan-hist/單因子/iid)看似有潛力者過完整漏斗雙雙淘汰(pb_self_pctile 冗餘、inst_govbank_divergence as-of 不顯著);用戶 directive「紀律機制入憲」。 | SUPERSEDED |
| v1.14.0 | 2026-06-27 | **方法承載強化（非新增原則）**：第三部 feature 方法論之四道漏斗**加系統級收尾關卡「經濟價值驗證」(#14)**——前四道篩特徵後,**整個特徵集＋模型須過 purged walk-forward 投組之 CAGR/Sharpe/MaxDD/Calmar 對比基準**(風險調整後優於基準才算成功);**IC 撐住 ≠ 可交易、靈魂成功定義是經濟價值非 IC**。詳法住方法論報告 §四;工具＝`evaluation/portfolio.py` + `scripts/run_economic_eval.py`,並補全完整工具鏈引用(各關卡實作對映)。屬 #14/#15 之**操作化收斂、非新增原則** → **僅憲章升版**、原則精華維持 v1.7.1。同步:原則精華第 5 行、README、憲章檔名 v1.13.0→v1.14.0、CLAUDE #11。實證動因:2026-06-27 三鏡頭戰役 Track D——Ridge long top20% Sharpe 1.21 vs 基準 0.97(IC 轉成真經濟價值);用戶 directive「經濟價值驗證入憲為收尾關卡」。 | SUPERSEDED |
| v1.15.0 | 2026-06-29 | **維運變更（非新增原則）**：第五部「Claude usage 經濟原則」之 tie-break 由「衝突時一律省 usage 為先」精細化為**執行層 vs 理解層二分**——凡**執行/產出層（how：build/sync/對帳/掃描/逐特徵計算/批量落地）省 usage 為先**；唯**「對任務之概念性意義與定義之理解（what/why）」以 ultracode 窮盡為最優先、不為省 usage 打折**（含治權詮釋/靈魂原則意涵/資料層 table·field·raw 語意定義〔尤 anti-leakage #8〕/方法論概念/真兆假兆判讀 #15）；判據＝交付物本質、混合任務切兩段、有疑偏理解層（成本不對稱：誤解 doctrine/誤定 field 之下游污染 ≫ 少省 usage）。屬 #28 tie-break 之限定反轉/判準二分、非新增不可違反原則 → **僅憲章升版**、原則精華維持 v1.7.1。SSOT 在 **CLAUDE.md #28**（工具層完整判據+正反例），憲章第五部僅引、不複述。同步:CLAUDE.md v1.9→v1.10、原則精華第 5 行交叉引用、README 憲章連結、憲章檔名 v1.14.0→v1.15.0。實證動因:2026-06-29 用戶 directive「省 usage 優先於 ultracode,但概念性意義與定義需要理解的部份必需以 ultracode 的理解為最優先」。 | SUPERSEDED |
| v1.16.0 | 2026-06-30 | **架構變更（非新增原則）**：第三部新增橫切層 **philosophy（投資哲學框架層、`philosophy_school`/`philosophy_principle`/`principle_factor_map`/`philosophy_source`/`stock_philosophy_tag`/`philosophy_build_meta` 6 表）**——把人類投資大師之智慧（**真實權威文獻**）結構化為「可證偽因子假說 ＋ 股票分類框架」,作 feature 層特徵假說之**理論來源** ＋ validate 後**解讀層**（顧問可解釋性）；三鏡頭（第一性/八二/康波）即其操作化。**明文從屬三敵、不因哲學鬆動**：哲學是假說非真兆（來源限真實文獻、**禁 AI 生成內容入庫** #1/#16）、採用由 out-of-sample＋經濟價值 #14 裁決（「驗證活下來、非大師說了算」、過四道漏斗）、股票分類 as-of #8、**不取代真實資料、不變 AI 占卜大師**。詳設計住 `reports/augur_investment_philosophy_framework_plan_20260630.md`（守第六部 SSOT、30 分鐘可讀）。**動靈魂 v1.2.0→v1.3.0**（世界觀補「投資哲學＝智慧骨架非占卜」+ 邊界補「不做 AI 占卜大師」）+ 憲章升版；原則精華維持 v1.7.1（哲學層＝操作化、非新增不可違反原則）。同步：第二部防線地圖加 philosophy 列、靈魂檔名 + 跨檔引用 v1.3.0、CLAUDE #16 補哲學來源限真實文獻、原則精華第 5 行引用、README、憲章檔名 v1.15.0→v1.16.0。實證動因：2026-06-30 用戶 directive「規劃投資哲學思想入庫 + 訓練股票投資大師、請入憲」→ 釐清為**相容版（理論框架層、守三敵）**。 | SUPERSEDED |
| v1.17.0 | 2026-06-30 | **定位擴充（改靈魂、非新增原則）**：philosophy 橫切層由「投資哲學框架」**擴為「哲學素養框架（投資哲學因子假說 ＋ 廣博哲學素養）」**——augur 定位從「投資智慧骨架量化顧問」擴為「**有廣博哲學素養的投資量化顧問（博學投資大師 AI）**」：除投資哲學（產因子假說、過四道漏斗）外，廣納**人類哲學經典原典全文**（東方兵法/道家、西方柏拉圖/亞里斯多德…公版）作**解讀/素養層**。**三敵零容忍不因擴定位鬆動**：來源限公版真實文獻、禁 AI 生成（#1/#16；`work_type` 禁 `ai_generated`、`license` 限 `public_domain`）、**廣博哲學全文量化零價值/不產因子/不進預測管線/不取代真實資料**；新增**原典抓取範圍界定**（納人類哲學經典、排除非哲學離題如純物理「能抓≠該抓」、本地抓公版零 usage #28 逐字 #1）。**動靈魂 v1.3.0→v1.4.0**（世界觀「投資哲學＝智慧骨架」→「哲學素養＝投資智慧骨架＋博學底蘊」、「不做 AI 占卜大師」補廣博哲學）+ 憲章升版；原則精華維持 v1.7.1（定位擴充、非新增不可違反原則）。同步：靈魂檔名 + 跨檔引用 v1.4.0、CLAUDE #17 補哲學來源範圍、原則精華第 5 行、README、憲章檔名 v1.16.0→v1.17.0。實證動因：2026-06-30 用戶 directive「柏拉圖等西方哲學都要抓、改靈魂、入憲」→ 拍板 B（擴通用哲學庫 / 改靈魂）。 | SUPERSEDED |
| v1.18.0 | 2026-07-01 | **鞏固三敵（合規路明文化、非新增／非鬆動原則）**：philosophy 層補「**版權著作核心精神入庫準則**」——現代版權著作（全文法律不可抓）之核心精神,經**真實文獻出處的 `philosophy_principle` 原則條目 → `principle_factor_map` 因子假說 → #14 經濟價值驗證**入庫；採用由 #14 裁決、非大師權威；**嚴禁 AI 整理／摘要版權著作內容入庫**（AI 生成假兆違 #1 ＋ 侵權、`work_type`/`license` DB CHECK 硬擋）。核心精神合法載體＝可證偽因子假說 ＋ 真實文獻 citation、非摘要。此為**三敵零容忍之明文強化**（禁 AI 生成不因「版權著作核心精神也想納」鬆動）、非新增可違反原則 → 僅憲章升版、原則精華維持 v1.7.1。同步：CLAUDE #17 補現代大師合規路、README、原則精華第 5 行、憲章檔名 v1.17.0→v1.18.0。實證動因：2026-07-01 用戶 directive「現代版權核心精神經 Claude 整理入庫、入憲」→ **我拒絕 AI 整理入庫**（違 #1 命門、絕不鬆三敵）、釐清合規路（真實文獻 principle→因子→#14）→ 用戶接受合規版入憲。 | SUPERSEDED |
| v1.19.0 | 2026-07-02 | **架構擴充(非新增原則)**:philosophy 層補「**知識層多域擴充準則**」——知識管線(`knowledge_source` 來源 registry / `knowledge_query` 領域×子領域查詢分類法 / `knowledge_staging` provenance 暫存 / promote 晉升)為**通用知識底座**,依決策層人拍板納入管理九域＋能源/太陽能材料＋化學/電子/物理/生物等知識域(現況 190 查詢詞/14 域、73 來源列;實測驗證之全球公開知識庫 API:OpenAlex/Crossref/arXiv/Europe PMC/dblp/DOAJ/PLOS/HAL/Zenodo/DataCite/OSTI/INSPIRE/PubChem/ChEMBL/COD/UniProt/GBIF/DBpedia 諾獎類別…)。**三敵從屬不變**:真實來源 provenance、公版全文判準(版權停在 metadata)、禁 AI 生成;多域知識=素養/解讀層、量化零價值、不進預測管線、不產因子;`domain` 欄隔離因子假說鏈純度(投資因子仍唯一經 school→principle→factor_map→#14)。工具層 SSOT=CLAUDE #29b(v1.15 已載),憲章承載架構準則明文;僅憲章升版、原則精華維持 v1.7.1、靈魂 v1.4.0「博學」涵蓋不動。同步:原則精華第 5 行、README、CLAUDE #29b 交叉引用、檔名 v1.18.0→v1.19.0。實證動因:2026-07-02 用戶連續 directive 擴知識域＋「治權升版明文入憲章」。 | SUPERSEDED |
| v1.20.0 | 2026-07-02 | **判準擴充(用戶拍板、非鬆動三敵)**:知識層多域擴充準則之全文准入改**雙軌**——哲學原典 `work_text` 維持限公版(v1.17/v1.18 不動);知識層 `knowledge_item_text` 納 **CC 白名單開放授權全文**(cc-by/cc-by-sa/cc0,DB CHECK 硬擋、逐字附授權來源;NC/ND/未明停 metadata)。動因:五鏡審查揭露 text 計畫 CC 條款越 v1.19.0「公版才入全文」判準 → v1 曾收斂僅公版、本次用戶決策層拍板納入。CC=真實授權轉載非公版,#1 真實來源不變、AI 生成零容忍不變。同步:原則精華第 5 行、README、CLAUDE #29b、text 計畫 v1.5、檔名 v1.19.0→v1.20.0。 | SUPERSEDED |
| v1.21.0 | 2026-07-03 | **架構變更（非新增原則）**：philosophy 橫切層補「**檢索與顧問前端（素養層唯讀出口）**」入憲——`philosophy.retrieval`（RAG 混合檢索）＋ `advisor`（payload→prompt→advise→guard 顧問前端；防幻覺=機械閘：引文逐字∈citations、數字∈payload 白名單、檢索空=固定誠實句、逆向不翻轉模型結論）；隔離不變式明文（預測管線 7 pkg 零 import 素養層、AST 稽核常備、顧問唯讀零寫回、單向依賴）。既有實作之入憲承載（2026-07-01 依用戶 directive 建成、2026-07-03 全專案精讀揭露憲章未載＝doctrine↔code 完整性補洞）、非新增可違反原則 → 僅憲章升版、原則精華維持 v1.7.1。同版同步校正：第三部 philosophy 層表清單據 DB 實查更正為 10 表（補 `philosophy_principle`／`philosophy_source`／`philosophy_chunk`／`philosophy_build_meta`）。同步：第二部防線地圖列名 philosophy/advisor、原則精華第 5 行交叉引用、README 憲章連結＋狀態段去重（版本演進明細單一權威家＝本修訂歷程，README 不再複列防漂移 #12）、檔名 v1.20.0→v1.21.0。用戶拍板 B1/B3（2026-07-03）。 | SUPERSEDED |
| v1.22.0 | 2026-07-04 | **文字收斂＋體例＋隔離擴充（非新增原則）**：9 視角對抗審查 22 條確認發現全落地——SSOT 收斂（`OUT_OF_UNIT` 唯一家＝原則精華 #3、#20 完整性天職移入原則精華 ENFORCE、第五部 #28 條/CLAUDE #17 縮句引 SSOT）＋事實修正（pg_trgm→子字串驗證、PHASE 偽旗標→實際入口、第一部邊界補「不做 AI 占卜大師」、v1.9.1 補記鉅額分點復列）＋philosophy 層立「共同不變式」減脂＋第六部立修訂歷程 3 行體例＋隔離不變式擴 `augur.knowledge`（AST 測試同步、實跑 PASS）。同步：原則精華（#3/#4/#18/#20/升版哲學，內文字修正不升版）、CLAUDE.md（#11/#17/#28/#29b 排版＋去版本釘）、README（口徑/白話化）、code docstring（retrieval/query_philosophy/sync/finmind）、靈魂（資料來源節範圍限定一行）。動因：用戶 directive「甲乙全做＋丙1a/丙2/丙3」（工單＝reports/augur_governance_multilens_review_20260703.md）。 | SUPERSEDED |
| v1.23.0 | 2026-07-04 | **架構新增（知識域端到端管線，七段一驅）**：philosophy 層立本準則——判準：pgvector=真兆同庫 SSOT 索引、外部 serving 索引=單向匯出可拋棄；對話唯一編排入口=advise()+guard 單一機械閘（guard 域條款／誠實固定句閉集之變更=憲章判準變更）；可替換=接縫層（llm_fn／embedspec／vectorindex）+重建 SOP、版本入 collection 名不混版；新域=registry INSERT 零 code、語料准入=CLEAN 述詞 fail-closed 三端強制 import。同步：原則精華 v1.7.1 交叉引用（不升版）、README 連結、CLAUDE.md #28/#29 對應條、檔名改版 v1.22.0→v1.23.0。動因：用戶 directive 2026-07-04「化學 know-how 為例一條路打通（外部 know-how→raw→逐字理解→向量→Qwen→WebUI 對話）+總綱計畫入憲」；詳計畫 SSOT=reports/augur_knowhow_e2e_pipeline_plan_20260704.md。 | SUPERSEDED |
| v1.24.0 | 2026-07-04 | **判準承載＋文字校正（稽核決5/決9,非新增原則）**：第三部 universe 段承載生產完整度 gate 之兩項限定（(a) 流動性分位地板=動態百分位非硬編、(b) 金融保險月營收 conditional 豁免;2026-06-30 生產啟用、此前未入憲）;catalog 段校正 SSOT 分工（抓法元資料=表 SSOT;中文名=datasets_zh.md 策展為源 seed 入表,非由表生成）。同步：原則精華第 5 行交叉引用、README 憲章連結、檔名 v1.23.0→v1.24.0;執行層併修 core_gate conditional G9 對齊。動因：全系統合規稽核（reports/augur_compliance_audit_20260704.md）決策層用戶拍板「照建議」2026-07-04。 | SUPERSEDED |
| v1.25.0 | 2026-07-04 | **判準變更（防幻覺誠實句閉集擴充,拍板3;非新增原則）**：三級誠實固定句由單句擴為**閉集二句**——(i) 檢索空「知識庫中無此內容」;(ii) 庫存著作但歸屬未驗（review_flag=true 隔離館藏旁查命中）「知識庫存有此著作但歸屬未驗證,不予引用」;機械分級器住 answer.py（W8 落地）、閉集僅此二。屬憲章防幻覺條款判準變更（v1.24 已明文「閉集變更=憲章判準變更」)→ 升 minor。guard.py 補第二固定句常數（分級邏輯待 W8）。同步：原則精華第 5 行、README、檔名 v1.24.0→v1.25.0、v3.0 計畫 §七拍板3。動因：v3.0 text 計畫拍板3、用戶「需憲章升版」2026-07-04。 | SUPERSEDED |
| v1.26.0 | 2026-07-05 | **架構承載變更（非新增原則）**：第一部立「**資料本質：PostgreSQL＝唯一真相來源（single system of record）**」——本專案一切資料（raw／feature／universe／model／知識層／catalog／未來權限）之 SSOT 與系統記錄皆為本地 PostgreSQL；外部 API 為擷取源、一律落地入 PG 後下游只讀 PG；衍生索引（Milvus）與傳輸工件（JSON／CSV／md）非 SSOT、可由 PG 全量重建；設定／程式碼不承載資料；跨機遷移＝pg_dump（#30）。屬 #9（真實來源）＋#12（SSOT）＋#29b（來源住 DB）之系統級收斂／明文總綱、非新增不可違反原則 → **僅憲章升版、原則精華維持 v1.7.1**。同步：原則精華第 5 行對憲章版本之交叉引用、README 憲章版本＋檔案連結、憲章檔名 v1.25.0→v1.26.0。實證動因：2026-07-05 用戶 directive「此專案所有的資料基礎都來自 PostgreSQL、入憲」。 | SUPERSEDED |
| v1.27.0 | 2026-07-05 | **文件紀律變更（非新增原則）**：第六部立「**計畫/設計文件完整性**」——凡產生資料表之計畫/設計文件須明確寫出 (a) table schema（DDL：欄/型別/約束/index）與 (b) 對應 Python（建表・遷移之檔·函式＋消費・強制之檔·函式），表與程式雙落實、非只述用途；DDL 住遷移器（沿用冪等 guard 慣例）、純計算程式標明結果落哪表。屬 #12 SSOT＋CLAUDE #29（DDL 住所/資料驅動）＋本部「詳設計住設計檔」之收斂、非新增原則 → **僅憲章升版、原則精華維持 v1.7.1**。今日三計畫已達標：SOP 補附錄 D（`trial_ledger`／`prediction_ledger`／`core_universe_build_meta` DDL＋Python）、RBAC 計畫與權限 schema 報告本已含。同步：原則精華第 5 行、README 版本＋檔案連結、憲章檔名 v1.26.0→v1.27.0。實證動因：2026-07-05 用戶 directive「所有計畫寫清楚明確產生的 table schema 與對應 python、入憲」。 | SUPERSEDED |
| v1.28.0 | 2026-07-05 | **系統定位擴張（新能力，非新增不可違反原則）**：philosophy 層立「**知識存取控制準則（RBAC 讀取控制）**」——單一主使用者擴為 superuser ＋ 受控多使用者群組；(i) 隔離不受影響（共同不變式/隔離不變式/#1 命門不因 RBAC 增刪一字、RBAC 表禁被預測 7 package 讀取或當特徵、預測對所有人相同）；(ii) 強制一律 server-side SQL 在 clean_item_sql 之上 AND 收窄不改寫 CLEAN；(iii) resolver 語意 superuser→ALL/否則群組 grant 聯集/空集=deny、型別分離禁 None=ALL；(iv) scoped 檢索仍經 advise()+guard 單閘、不改三級誠實閉集、不新造「你無權」；(v) domain 兼任授權邊界以 knowledge_domain 字典 FK+兩階段驗證補 referential integrity。隔離不變式擴文 RBAC 表/resolver 同受零 import、禁置 augur.core、isolation 測試擴掃 scripts/。**授權政策（群組/grant/superuser/domain 邊界/local_private/works A/B）永遠決策層人拍板（§8.2）**。動靈魂 v1.4.0→v1.5.0（為誰：主使用者+受控多群組、單一決策主體不變）；屬新能力承載、原則精華維持 v1.7.1（RBAC 不觸三敵）。同步：靈魂檔名+跨檔引用 v1.5.0、原則精華第 5 行、README 版本+檔案連結+靈魂連結、test_philosophy_isolation.py 擴、憲章檔名 v1.27.0→v1.28.0；詳計畫 reports/augur_rbac_design_plan_20260705.md（紅隊 critical 10/high 9 全處理）。實證動因：2026-07-05 用戶 directive「續 P2 照計畫自主往下做、入憲」（RBAC rollout 治權前置 gate、承 §8.1）。 | SUPERSEDED |
| v1.29.0 | 2026-07-05 | **RBAC 政策裁決入憲（§8.2#5 works 側，非新增原則）**：用戶拍板 works 側 A/B 裁決＝**B「對所有登入者公開」**——哲學/文學 `philosophy_work`（公版投資智慧經典原文）對所有已登入使用者公開、不 domain 收窄（CLEAN 閘仍過），未登入 fail-closed deny；理由＝公版經典為全體共享素養底座（非部門機密），items（知識/財經/local_private）仍 domain 收窄不受影響。RBAC 準則補 (vi) 條、(ii) 條 works 改指 (vi)、§8.2 政策清單註記 works A/B 已裁；`philosophy.retrieval.retrieve` scope=None→deny/登入→CLEAN works（`test_rbac_enforcement.py` T3 同步、9 passed）。屬既定 fail-closed 機制上之**政策裁決承載**（§8.2#5／#6 用戶授權升憲章 RBAC 判準）、非新增可違反原則 → 僅憲章升版、原則精華維持 v1.7.1、靈魂 v1.5.0 不動。同步：原則精華第 5 行、README 版本+連結、`reports/augur_rbac_group_plan_20260705.md` §6.5、憲章檔名 v1.28.0→v1.29.0。實證動因：2026-07-05 用戶 directive「works 全部人可看、升靈魂/憲章 RBAC 判準」。 | SUPERSEDED |
| v1.30.0 | 2026-07-05 | **顧問呈現層變更（非防幻覺判準變更、非新增原則）**：用戶 directive——對外回覆**只顯示模型白話解讀，公版「引經據典（系統逐字附上、公版可溯源）」逐字區塊一律不對外顯示**。**防幻覺 honesty gate 一字不動**：guard 內部仍以 citations 逐字校驗（引文/數字閘不變）、R2「系統逐字附上原文」由對外展示改為僅內部校驗基準（隱藏呈現≠移除機制）、誠實固定句閉集不動；guard-fail（公版）改回既有閉集 `NO_KNOWLEDGE_RESPONSE`（不倒原文、不自造新句）；Mode B 用戶自帶附加檔區塊保留（非公版引經據典）。僅動 `oai_compat._reply_text` 呈現組裝、`retrieval`/`advise`/`guard` 鏈不改（134 測試通過）。屬呈現層 UX 決策 → 僅憲章升版、原則精華維持 v1.7.1、靈魂 v1.5.0 不動。同版並記資安修：advisor 相容期 stopgap 由「無 header 預設 super」改 **fail-closed deny + 顯式 `--insecure-loopback-admin` opt-in**（對抗式紅隊 HIGH：忘設 `AUGUR_INTERNAL_SECRET` 會令 RBAC 靜默失效）、`serve_chat_ui` 無機密拒啟動。同步：原則精華第 5 行、README 版本+連結、憲章檔名 v1.29.0→v1.30.0。實證動因：2026-07-05 用戶 directive「引經據典全部不要顯示、此原則入憲」＋紅隊審查。 | SUPERSEDED |
| v1.31.0 | 2026-07-05 | **工作流原則入憲（計畫先行 plan-first；非新增可違反原則）**：第六部立「**計畫先行**」——本專案一切規劃類工作（新功能／子系統／介面／架構／流程／判準變更）一律**先產出計劃報告（`reports/`）＋決策層拍板後才實作**，不邊想邊做、不先 code 後補計畫;報告須達「計畫/設計文件完整性」＋30 分鐘可讀;純機械修正（改錯字／既定計畫逐步落地）不另立計畫。屬 CLAUDE #20／#19／§8.2 之強化收斂、非新增不可違反原則 → 僅憲章升版、原則精華維持 v1.7.1。同步：CLAUDE.md #20 改寫對齊（SSOT＝本部）、原則精華第 5 行、README 版本+連結、憲章檔名 v1.30.0→v1.31.0;首例＝前後台 UI 計劃 `reports/augur_ui_claude_desktop_plan_20260705.md`。實證動因：2026-07-05 用戶 directive「以後所有規劃先產出完美計劃報告後再實作、請入憲」。 | SUPERSEDED |
| v1.32.0 | 2026-07-05 | **工作序強化（新增規劃前置程序＋完整性收斂,非新增可違反原則;經三鏡對抗審查定稿)**：第六部「計畫先行」補「**前置理解**」——規劃前先把 what/why 理解窮盡(SSOT＝#28 執行/理解二分＋#20,不另造定義),**手段依風險分級、不一律 fan-out(守 #28「非必要不 fan-out」)**：標準級＝作者自身多角度深研＋完整性對抗自問(不開多 agent);多視角級＝觸高風險門檻(變更治權判準/跨≥2治權檔、不可逆、安全授權邊界、跨≥3 package)才多 agent 平行＋對抗審查＋計畫書附「對抗審查發現表」留痕;比例原則以判準(非清單)定灰帶與有疑偏保守。**「計畫完整性」維持對 v1.27.0 之交叉引用(不重述、守 SSOT 反膨脹)**。屬 v1.27.0/v1.31.0/#20/#28/§8.2 之收斂強化 → 僅憲章升版、原則精華維持 v1.7.1、**CLAUDE.md 無需再動(#20/#28 已對齊)**。同步：原則精華第 5 行、README 版本+連結、檔名 v1.31.0→v1.32.0。三鏡審查(治權/可執行/一致性)全 revise、發現全採納(刪冗餘 (B)、(A) 改分級、補留痕、錨定 #28)。動因：2026-07-05 用戶 directive「規劃前先 ultracode 多視角全面理解、計畫皆須詳細 schema＋對應程式、入憲」。 | SUPERSEDED |
| v1.33.0 | 2026-07-05 | **工作序強化(補執行前確認、非新增原則)**：第六部「計畫先行」補「**執行前確認(實作前驗證關)**」——計劃拍板後、動工前 AI 須主動確認計劃①完整②內部一致③與現況/code 事實一致④可實作,命中矛盾/過時/落差先修計劃再執行、不執行帶病計劃;界定四判準無「已知」缺失即動工(避免完美主義癱瘓)。承 §26(主動求完整/不讓用戶當 QA)＋#15(實證)為 SSOT、不重述。**因屬治權判準變更(高風險門檻),依 v1.32.0 本應多視角級;本輪無 ultracode opt-in,以 AI 自身三鏡對抗審查(doctrine-fit/enforceability/consistency)代 fan-out(守 #28 非必要不 fan-out)、附發現表留痕**——發現(與§26重疊→改承引不重述、「完美」無界→四判準界定、與拍板/v1.27.0 互補→錨 SSOT)全採納。屬 v1.31.0/v1.32.0/§26/#15 之收斂強化 → 僅憲章升版、原則精華維持 v1.7.1、CLAUDE #20 同步版本。同步:原則精華第 5 行、README 版本+連結、檔名 v1.32.0→v1.33.0。實證動因:2026-07-05 用戶 directive「執行前先確認計劃完美、無問題才執行、入憲」(當次即抓出計劃 §5.7 矛盾/§4.2 think moot/狀態過時並先修=本條之實例)。 | SUPERSEDED |
| v1.34.0 | 2026-07-05 | **顧問精準度改善法入憲(執行層品質工程、非新增可違反原則)**:第六部 §D 補「**答案品質評準(Claude-as-judge 自問自答迭代逼近)**」——改善 advisor 回答品質時 AI 得以自身已知最佳答案為評準、自主生成多樣題型對 live 顧問自問自答→比對評分→改 prompt/組裝再測、逐級逼近(承 §26 有界自主自糾＋#27 逐級逼近奇異點,評準/待改皆 AI、clean-room 護欄內無外部副作用)。**作用域嚴限執行層(prompt/題型自適應/組裝)、絕不為答得更準鬆動誠實護欄**(guard 閉集/誠實固定句閉集/空檢索 NO_KNOWLEDGE 強制/三敵零容忍皆 §8.2 判準、碰即停)。**因屬治權檔工作序加法(灰帶、未觸判準變更本身),本輪無 ultracode opt-in,以 AI 自身三鏡對抗審查(治權/可執行/一致性)代 fan-out(守 #28)、發現全採納**(「迭代到最佳」誤讀為越護欄→加「碰即停」明界;「已知最佳」主觀→由「限執行層+護欄範圍」框住)。屬 §26/#27/#28/v1.25.0/v1.30.0 之收斂強化 → 僅憲章升版、guard/誠實閉集/原則精華維持 v1.7.1、CLAUDE.md 無需再動。同步:原則精華第 5 行、README 版本+連結、檔名 v1.33.0→v1.34.0。實證動因:2026-07-05 用戶 directive「自主迭代到最佳、可用 Claude 知道最佳答案的其他問題來問、入憲」(當次即以廣測抓出 W1 安全邊際通用化/W2a 明天漲跌不助人/W3 寫詩離題並修正、W2b 穩賺不賠證實=空檢索護欄不自越=本條之實例)。 | **ACTIVE** |
