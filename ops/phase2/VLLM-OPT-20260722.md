# vLLM 最佳化執行證據 — 2026-07-22

* **性質**：[I] 證據（執行 [`reports/local_llm_vllm_optimization_plan_20260722.md`](../../reports/local_llm_vllm_optimization_plan_20260722.md)）。
* **主機**：`aitopatom-b96e`
* **窄案**：倉庫 mcp.json **未**改預設（仍 Ollama）；本波僅優化運維／客戶端／建議模型。

## 拍板假設（用戶「先執行 vllm 最佳化計畫」）

| 項 | 採納 |
|---|---|
| A | GO P0→P1→P2→P3 |
| B | 先最佳化，**不**自動執行窄案改預設 |
| C | 從 L1 找小而夠用 → 鎖定 **Qwen3-4B** |
| D | P2 全做（thinking＋per-tool max_tokens） |
| E | **不** enable；**未** commit（待你指示） |

## P0 — 運維

| 檢查 | 結果 |
|---|---|
| `--gpu-memory-utilization 0.35` | 沿用 |
| 並存紀律 | runbook 寫清；實測前 `ollama stop` UI／coder 大模 |
| preflight latency（L0） | models **7 ms**；chat **39 ms** |
| embed 並存（L0 active） | `nomic-embed-text` dim=768 OK |
| unit enable | **否** |

## P2 — MCP 客戶端

| 項 | 結果 |
|---|---|
| `chat_template_kwargs.enable_thinking=false` | L0／L1 chat 回 `pong`（無 think） |
| `_strip_think` | selftest 鎖；剝後空→isError |
| per-tool max_tokens | ask256／summarize512／…；env 可覆寫 |
| `python3 -m tools.local_llm_mcp.selftest` | **OK** |

## P1 — 模型階梯

固定提示：`local_summarize(ops/phase2/VLLM-SPIKE-20260722.md, max_sentences=5)`

| 階 | 模型 | summarize 延遲 | 品質摘錄（真實輸出） |
|---|---|---|---|
| L0 | `qwen3-0.6b`＠vLLM | **138 ms** | `* vLLM spike 通过测试。`（過短、簡體、資訊不足） |
| L1 | `qwen3-4b`＠vLLM | **6020 ms** | 能述安裝／MCP openai／mcp 未改／顯存風險（繁中可用） |
| 對照 | `qwen3-coder-next`＠Ollama | **176201 ms**（含冷載） | 摘要更完整、判斷更準 |

L1 其餘：ask `pong`／research 綠；preflight chat **104 ms**；embed 並存 dim=768。

**L1 首次失敗根因**：缺系統／venv **`ninja`**（`FileNotFoundError: ninja`）。`pip install ninja` 入 `~/augur-venvs/vllm` 後重試 **PASS**。

**選定**：手動／unit 建議模型＝**`Qwen/Qwen3-4B`／`qwen3-4b`**（優於 L0；未挑戰 L2 30B-A3B 本波）。  
**奇異點**：濃縮品質仍弱於 coder-next，但延遲數量級較佳；適合當「快速濃縮」後端，深度仍可回退 Ollama coder-next。

## P3 — 角色邊界（現行）

| 角色 | 後端 | 模型 |
|---|---|---|
| Cursor local-llm（手動 env） | vLLM `:8000` | **`qwen3-4b`（建議）** |
| Cursor local-llm（倉庫預設） | Ollama | `qwen3-coder-next` |
| project-memory embed | Ollama | `nomic-embed-text` |
| 產品 UI | Ollama | `qwen3:30b-a3b` |

### 建議手動 env

```bash
export LLM_BACKEND=openai
export OPENAI_BASE_URL=http://127.0.0.1:8000/v1
export OPENAI_API_KEY=EMPTY
export LLM_MODEL=qwen3-4b
# 可選：OLLAMA_TEMPERATURE=0.2
systemctl --user start augur-vllm   # 範本已指 4B；須先 pip install ninja
```

## 程式／設定變更清單

- `tools/local_llm_mcp/tools.py` — thinking 關閉／剝離、profile max_tokens  
- `tools/local_llm_mcp/selftest.py`／`README.md`  
- `ops/phase2/vllm_preflight.sh` — 預設模型 `qwen3-0.6b`、latency、enable_thinking false  
- `ops/phase2/systemd/augur-vllm.service` — **Qwen3-4B**  
- `ops/phase2/VLLM-GB10.md`、`recommended.env` 註解  

## 未做（明示）

- 改 `.cursor/mcp.json` 預設  
- `systemctl --user enable`  
- L2 30B-A3B 載入驗收  
- P4 quant／Cloud 共用
