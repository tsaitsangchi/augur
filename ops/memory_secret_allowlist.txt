# 記憶 export 密碼掃描——誤報豁免清單（M-O3，2026-08-03）
# 判準 SSOT＝src/augur/audit/plaintext_credential.py；強制點＝sync_memory.py export（fail-closed）。
#
# 格式: 檔名<TAB>規則<TAB>值指紋<TAB>核准者<TAB>核准日<TAB>理由
#   檔名   ＝ memory 檔名（不含目錄）
#   規則   ＝ kv_strong / kv_weak / login_pair / known_prefix / url_credential
#   值指紋 ＝ sha256(值) 前 12 碼（掃描器紅訊息會直接印出可貼上之骨架列）
#   核准者 ＝ **人**（hugo）——AI 不代填人簽欄；每一列即一次「我看過、這不是機密」之留痕
#   核准日 ＝ YYYY-MM-DD
#   理由   ＝ 為何此命中不是明碼憑證（一句話，供日後回頭稽核）
#
# 紀律
#   · 只存**指紋不存明碼**——豁免清單自身不得成為洩漏管道。
#   · 六欄缺一／任一欄留空 ⇒ 視為格式錯誤 ⇒ 掃描判紅（壞掉的豁免不得靜默生效）。
#   · 鍵不含行號（行號隨編輯漂移會使既有豁免失效、製造假紅）；換檔名即不再豁免。
#   · 本檔不存在＝零豁免（缺檔只會更嚴，不是放行）。
#   · **誤報的正解優先是改記憶檔**（明碼換成 ⟨見 .env XXX⟩ 引用），豁免是次選。
#
# 現況：2026-08-03 對 handoff_memory/ 81 檔實掃 0 殘留 ⇒ 本清單零列。
