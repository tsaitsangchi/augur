# 機器基礎資訊：`PC002-S1800`

> **性質**：本機實測快照 **[I]**（自動產生，勿手改；手動註記請寫檔尾 NOTES 區塊）。
> **產生工具**：`ops/collect_machine_info.sh` ｜ **產生時間**：2026-07-23 08:10 CST
> 跨機共享說明（專案相依、治理差異）見 [README.md](README.md)。

## 摘要

| 面向 | 值 |
|---|---|
| 主機名 | `PC002-S1800` |
| 平台 | WSL：yes (WSL2) ｜ 架構：x86_64 |
| OS / 核心 | Ubuntu 24.04.4 LTS ／ `6.6.87.2-microsoft-standard-WSL2` |
| systemd | running |
| CPU | Intel(R) Core(TM) i5-10500 CPU @ 3.10GHz（6 核 / 12 緒） |
| 記憶體 | 11.7 GiB（swap 64.0 GiB） |
| 系統碟 `/` | 1007G total, 773G avail (20% used) |
| GPU | 無 nvidia-smi |
| GPU 直通 `/dev/dxg` | 存在 (WSL2 直通) |
| CUDA `nvcc` | (未安裝) |
| PostgreSQL | 17.9（17/main port 5432 (online)） |

## 工具鏈

| 工具 | 版本 |
|---|---|
| git | 2.43.0 |
| gh | (未安裝) |
| python3 | 3.12.3 |
| node | 18.19.1 |
| npm | 9.2.0 |
| gcc | 13.3.0-6ubuntu2~24.04.1) 13.3.0 |
| make | 4.3 |
| cmake | (未安裝) |
| nvcc | (未安裝) |
| psql | 17.9 (Ubuntu 17.9-1.pgdg24.04+1) |
| docker | (未安裝) |
| ollama | (未安裝) |

## 手動註記

<!-- NOTES:START -->
（基準快照 2026-07-23 08:10 CST — 硬體／WSL 記憶體與服務鎖定）

## 角色
開發／驗證 + 資料層（PostgreSQL + qdrant + 全棧 UI）；本地小／中模型（CPU／iGPU，無獨顯）。  
**≠** `DESKTOP-8MQPFS8`（Ryzen + GTX 1650）——同為 WSL2 開發向，但是**另一台實體機**。  
對立大模型機：`aitopatom-b96e`（GB10）。

## Windows 主機（實體）
- 廠牌／型號：ASUSTeK · **ASUS EXPERTCENTER D700TA_M700TA**
- OS：Microsoft Windows 11 Pro（10.0.26200）
- CPU：Intel Core i5-10500 @ 3.10GHz（6C/12T）
- 實體 RAM：**15.9 GiB**
- GPU：**Intel UHD Graphics 630** only（driver 30.0.100.9805；AdapterRAM 回報 1GB）——**無 NVIDIA 獨顯**
- 磁碟：C: 930.5 GB（可用 ~436 GB）
- Windows pagefile：`C:\pagefile.sys` Alloc≈13.8 GB
- Windows 使用者 profile：`C:\Users\S114013.GSMCTW`

## WSL2
- Distro：`Ubuntu-24.04`（WSL2）
- `.wslconfig`（`%UserProfile%\.wslconfig`）：`memory=12GB`、`processors=12`、`swap=64GB`
- 實測（`free -h`）：Mem ≈ **11 GiB**、Swap **64 GiB**
- `/dev/dxg` 存在（直通節點有），但 **無 `nvidia-smi`／無 CUDA**（僅 iGPU，WSL 內不可當 NVIDIA 推論）
- 備份：`.wslconfig.bak.20260723-080154`（改前：memory=10GB、swap=4GB）

## 服務（2026-07-23 實測）
- PostgreSQL **17.9** `17/main` @5432 **online**；DB `augur` ≈ **56 GB**
- Ollama **0.31.1** @11434（binary：`/home/hugo/ollama/bin/ollama`；PATH 未裝故 collect 腳本顯示「未安裝」）
  - 模型：`qwen3:4b`、`qwen3:8b`
- Qdrant **1.18.2** @6333；collection `kn_sent_it_ime5s30b1cd_tn1`
- user systemd active：`augur-chat`(:8090)／`augur-advisor`(:8399)／`augur-admin`(:8500)／`augur-probability`(:8600)／`augur-ollama`／`augur-qdrant`
- vLLM：未跑（:8000 無服務）
- docker：未安裝

## 專案佈局
- 正典 clone：`/home/hugo/project/augur` ↔ `https://github.com/tsaitsangchi/augur`（`main`）
- 使用者：`hugo`；venv Python 3.12.3；`.env` 存在（`ENV=development`）
- 此機設定包：`ops/machines/packs/PC002-S1800/`

## 建議模型／勿做
- **主路徑（chat／advisor）**：`qwen3:8b`（程式預設；`.env` 勿寫 `OLLAMA_MODEL`，以免誤蓋 MCP）
- **MCP `local-llm`**：釘死 **`qwen3:4b`**（`.mcp.json`／`.cursor/mcp.json`：`LLM_MODEL=qwen3:4b`、`OLLAMA_NUM_CTX=4096`、`OLLAMA_TEMPERATURE=0`、`OLLAMA_KEEP_ALIVE=30s`）
- **勿**假定有 NVIDIA／CUDA；**勿**用 GB10 預設大模型（`qwen3:30b-a3b`／`qwen3-coder-next`）當 MCP 預設
- **勿**與 `DESKTOP-8MQPFS8` 混用路徑（該機正典在 `/home/giga/augur/augur-code`）
- Cursor：改 mcp.json 後需 **Reload MCP／重開視窗** 才載入新 env
<!-- NOTES:END -->
