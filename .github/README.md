# GitHub Actions

Lint workflow template: [`tools/constitution_lint/github-workflow.yml`](../tools/constitution_lint/github-workflow.yml) (inert until copied here).

Copying into `.github/workflows/` needs a token with the **`workflow`** scope. Until then run locally:

`python3 -m tools.constitution_lint --selftest`
