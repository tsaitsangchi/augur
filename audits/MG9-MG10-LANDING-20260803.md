# M-G9／M-G10 落地帳（2026-08-03）

> 位階：[I] · 對應 `reports/augur_optimization_master_plan_20260803.md` 第 14 步  
> 授權：M-G9 證據列 upsert；M-G10 **僅驗收改碼／dry-run**（hugo 拍＝wiring_only；**不開** FinMind 補抓）

## M-G9（新鮮度哨兵）— 關閉

| 條件 | 結果 |
|---|---|
| `--selftest` | rc=0 |
| 全量 `--check` | ~7–30s（&lt;60）；red **5**／amber 17／green 60 |
| TRI 必須紅 | max=**2026-07-09**、停更 **16** 交易日；`--only TRI` → **rc=1** |
| `validation_evidence` | `E10_dataset_freshness` → **red**（`last_verified_at` 2026-08-03 14:33+08） |

紅表（現查）：`CrudeOilPrices`、`ExchangeRate`、`GovernmentBondsYield`、`TaiwanStockTotalReturnIndex`、`USStockPrice`。

## M-G10（TRI／by-dim-id 接線）— 改碼半閉；補抓未開

### 做了什麼（碼側已就位，本輪親驗）

1. `sync.sync_all_by_date(..., dimension_sync=)`：`True`→`_dimension_sync`；預設 `False`＋`needs_dimension_sync` 旗標（不再靜默通過）。
2. `daily_maintenance.py`：`--with-dim-sync`／`--dim-sync-dry-run`。
3. `run_arena_daily_pipeline.py`：`AUGUR_DIM_SYNC=1` 才帶 `--with-dim-sync`（預設關）。
4. `--dim-sync-dry-run`：**6** 張 by-dim-id；TRI＝`doc-seed` `['TAIEX','TPEx']`、resume=**2026-07-09**。
5. `python -m augur.ingestion.sync --selftest`：含 wants_dimension_sync／mock 接線段 → **全通過**。

### 未做（誠實殘項）

| 項 | 狀態 |
|---|---|
| FinMind 補抓／`--with-dim-sync` 實跑 | **未開**（FZ 補洞仍凍＋本輪 wiring_only） |
| 驗收④ `TRI max ≥ PriceAdj max`／`market_direction_feature` 前進 | **未達**（仍靠補抓） |
| 污染期 arena 預測標記／重跑 | Steward 另裁 |
| 常態開啟 dim-sync（crontab／unit） | **未改**；維持 env 單次門 |

### 與 FZ／arena 白名單

- 白名單日頻：`daily_maintenance.py --end <當日>`（**預設無** `--with-dim-sync`）。
- 開 dim-sync ＝補洞回填面 → **另需明示授權**，且須與 M-G5 錯開額度日。

## 復現

```bash
python scripts/check_dataset_freshness.py --selftest
python scripts/check_dataset_freshness.py --check --only TaiwanStockTotalReturnIndex   # expect rc=1
python scripts/daily_maintenance.py --dim-sync-dry-run                                 # 零 API
python -m augur.ingestion.sync --selftest
AUGUR_DIM_SYNC=1 python scripts/run_arena_daily_pipeline.py --dry-run                  # 只印不跑
```
