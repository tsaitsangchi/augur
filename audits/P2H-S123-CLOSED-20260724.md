# P2H S1–S3 CLOSED — prodset→預測熱路徑 [I]（2026-07-24）

* Steward 開工句：「**開 prodset 熱路徑**」
* 拍板碼：`P2H-P-yes`＋`P2H-E123`＋`FC-empty`＋`FZ-keep`（`audits/P2H-PLAN-APPROVED-20260724.md`）
* 計畫 SSOT：`reports/augur_prodset_predict_hotpath_plan_20260724.md`
* 硬邊界：零 FinMind／FRED；≠可交易／≠確立級；禁 silent fallback canonical；✅ U 對抗＝`audits/P2H-ULTRACODE-20260724.md`

---

## 1. 做了什麼

| 階段 | 內容 |
|---|---|
| **S1** | `augur.core.prodset_contract`：`load_active_features`／`resolve_prodset_feats`（active∩panel 窗至少一次）；philosophy writer re-export 常數；`setup_predict_role`：prodset SELECT＝准、其餘 `evolution_*`／`promotion_queue`＝拒；`--apply --confirm` 已 refresh GRANT |
| **S2** | `baseline.resolve_train_feats`（預設 prodset）；`train_ranker --feature-source=prodset` 預設；空 active→`ProdsetEmptyError`（FC-empty）；metrics 記 `feature_source`／`n_feats`／`feats` |
| **S3** | `predict_asof`：serve＝artifact 凍結 feats；prodset 漂移＝當下 resolve vs 凍結→**拒載**；canonical 舊模型僅告警；FC-empty 中止 |
| **哨兵** | `scripts/verify_prodset_hotpath.py --check`／`--selftest` |

---

## 2. 驗收表 A*（機械）

| ID | 結果 | 證據 |
|---|---|---|
| **A1** | ✅ PASS | `import_isolation` 0 違規；`pytest tests/test_philosophy_isolation.py` 9 passed |
| **A2** | ✅ PASS | `load_active_features`＝DB active：`inst_cumflow_position_120d`、`volume_gini_60d` |
| **A3** | ✅ PASS | predict SELECT prodset＝准；`evolution_run`／`promotion_queue`＝拒（dry-run＋verify） |
| **A4** | ✅ PASS | train prodset：`n_feats=2`；feats⊆active；registry 記來源 |
| **A5** | ✅ PASS（碼徑） | `ProdsetEmptyError`／FC-empty 訊息；禁 fallback（selftest＋CLI） |
| **A6** | ✅ PASS | 漂移非死變數；prodset 拒載／canonical 告警 |
| **A7** | ✅ PASS | 本輪零 FinMind／FRED 呼叫 |
| **A8** | ✅ PASS | 庫內 as-of `2026-05-31` train＋predict `--dry-run` 成功 |
| **A9** | ✅ PASS | 對外句無「可交易／確立級／已解凍」 |
| **A10** | ✅ PASS | U-P2H DONE（`audits/P2H-ULTRACODE-20260724.md`）；G-PME-HOTPATH 維持 none |
| **A11** | ✅ PASS | 明示 `n_feats=2`／特徵名單 |

**階段 DONE（S1–S3 機械＋U）**＝A1–A11。**≠**可交易、**≠**解凍、**≠**G-PROM 多數綠。

---

## 3. 親驗數字（真兆）

| 項 | 值 |
|---|---|
| active | 2：`inst_cumflow_position_120d`、`volume_gini_60d` |
| as-of | `2026-05-31`（庫內最新） |
| train | `RankRidge_H60_2026-05-31_seed42_1420b777665a099f`；`feature_source=prodset`；`n_feats=2`；`train_rows=12034`；panels=35 |
| predict dry-run | 同 model；`n_feats=2`；漂移通過；未寫庫 |
| API | 零 |

---

## 4. Gap

| ID | 後 | 說明 |
|---|---|---|
| **G-PME-HOTPATH** | **none** | 熱路徑真讀 prodset active；S123＋U-P2H；**仍≠可交易** |

---

## 5. 重放路徑

```bash
python -m augur.core.prodset_contract --selftest
python -m augur.audit.import_isolation
pytest tests/test_philosophy_isolation.py -q
python scripts/setup_predict_role.py --apply --confirm   # GRANT 收斂
python scripts/verify_prodset_hotpath.py --check
python scripts/train_ranker.py --run --asof 2026-05-31
python scripts/predict_asof.py --run --dry-run --asof 2026-05-31
```

---

## 6. 建議下一句

* 「**資料地基等解凍條件再續**」（FZ-keep；取數洞另帳）
* **禁**：解凍 FinMind／FRED；禁把 n_feats=2 說成生產完備／可交易
