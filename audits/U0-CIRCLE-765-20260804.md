# U0-CIRCLE 定案＋dry 7／65（2026-08-04）

> **位階**：[I]。**Steward**：`U0-CIRCLE: 7,65=登(a) ; 37,80,97=俟`  
> **語意**：7／65＝批准以 W2-1=(a) 分隔字串**登錄**；37／80／97＝結構俟。  
> **寫庫**：本檔含 **DRY**；**EXECUTED 已落地** → `audits/U0-CIRCLE-765-EXECUTED-20260804.md`  
> （授權句曾為 `REGISTRY-GO: Q-R1=a + honesty=7,65 + decided_by=hugo`；honesty 本批已消費）

## 圈選結果

| binding | 鍵 | 裁 |
|---:|---|---|
| **7** | `tw.convertible_bond.terms` | ☑ 登(a) |
| **65** | `tw.option.institutional_flow.after_hours` | ☑ 登(a) |
| 37／80／97 | （既有提案） | ☑ **俟** |

---

## ⚠ DRY／勿執行 — binding 7

```sql
BEGIN;
SET LOCAL augur.honesty_write = 'on';
INSERT INTO world_concept (concept_key) VALUES ('tw.convertible_bond.terms');
INSERT INTO world_concept_version
    (concept_key, category, authoritative_binding_id, ts_semantics, knowability_rule,
     cross_market_axis, provenance, finality_predicate, conflict_set_ref,
     decided_by, decided_at)
VALUES (
    'tw.convertible_bond.terms', 'state', 7, '條款／發行事件日（表內轉換起迄）',
    '公開後當日可得；表內無統一公告欄時為法定公開規則型（WM.31(b)）', NULL,
    jsonb_build_object(
        'basis', 'U0-CIRCLE 7,65=登(a)',
        'dry_sql_ref', 'audits/U0-CIRCLE-765-20260804.md',
        'w2_1_form', 'delimiter_string',
        'category_note', 'state＝條款狀態；entity 替代讀未採'),
    '條款值於公開後定案（未宣告更嚴規則前依 WM.32 缺省）', NULL,
    '⟨hugo 親打⟩', TIMESTAMPTZ '⟨YYYY-MM-DD HH:MM:SS+08⟩');
UPDATE world_channel_binding
   SET concept_key='tw.convertible_bond.terms', mapping_status='mapped',
       source_column='IssuanceAmount,InitialDateOfConversion,DueDateOfConversion',
       provenance=coalesce(provenance,'{}'::jsonb)||jsonb_build_object(
           'map_note','U0-1 登(a)','w2_1','delimiter_string',
           'out_candidate','cb_name 不入')
 WHERE binding_id=7 AND superseded_at IS NULL AND mapping_status='unmapped' AND concept_key IS NULL;
ROLLBACK;
```

## ⚠ DRY／勿執行 — binding 65

```sql
BEGIN;
SET LOCAL augur.honesty_write = 'on';
INSERT INTO world_concept (concept_key) VALUES ('tw.option.institutional_flow.after_hours');
INSERT INTO world_concept_version
    (concept_key, category, authoritative_binding_id, ts_semantics, knowability_rule,
     cross_market_axis, provenance, finality_predicate, conflict_set_ref,
     decided_by, decided_at)
VALUES (
    'tw.option.institutional_flow.after_hours', 'quantity', 65, '交易日（盤後時段）',
    '盤後資料發布後當日可得；法定公開規則型（WM.31(b)）', NULL,
    jsonb_build_object(
        'basis', 'U0-CIRCLE 7,65=登(a)',
        'dry_sql_ref', 'audits/U0-CIRCLE-765-20260804.md',
        'w2_1_form', 'delimiter_string',
        'known_gaps', jsonb_build_array('列鍵 option_id/call_put/institutional_investors＝Q-R2 殘')),
    '當日值於次一交易日收盤後定案', NULL,
    '⟨hugo 親打⟩', TIMESTAMPTZ '⟨YYYY-MM-DD HH:MM:SS+08⟩');
UPDATE world_channel_binding
   SET concept_key='tw.option.institutional_flow.after_hours', mapping_status='mapped',
       source_column='long_deal_amount,long_deal_volume,short_deal_amount,short_deal_volume',
       provenance=coalesce(provenance,'{}'::jsonb)||jsonb_build_object(
           'map_note','U0-4 登(a)','w2_1','delimiter_string')
 WHERE binding_id=65 AND superseded_at IS NULL AND mapping_status='unmapped' AND concept_key IS NULL;
ROLLBACK;
```

## 寫庫解鎖句

```text
REGISTRY-GO: Q-R1=a + honesty=7,65 + decided_by=hugo
```

期望 COMMIT 後：mapped **18→20**；sc **8→10**；`--check` 兩鍵 ✓。  
**實測**：已達成（見 `audits/U0-CIRCLE-765-EXECUTED-20260804.md`）。
