# OPT-P0＋TRIAGE-65 執行／驗收帳 [I]（2026-08-04）

> **位階**：[I] 拍板＋執行留痕（非 META-CONSTITUTION [N]）。  
> **授權（本輪 Steward 明示甲案）**：  
> `OPT-P0-20260804-go + TRIAGE-65-go + FZ-keep + GATE-keep + NHC-keep`  
> **上游 step 授權（不矛盾、本輪擴權）**：`audits/OPT-STEP-R2-20260804-GO.md`  
> （該檔原裁 Step1＝`wait_done`／禁開 65 triage；**本輪甲案＝結輪後正式開工** Step0 收口＋Step1 triage。ready sentinel＝`audits/RUN22-READY-FOR-STEP1-20260804.md`。）

## 做了什麼

| # | 步驟 | 路徑／證據 |
|---|---|---|
| 1 | 複跑 morning 五驗（修前） | `observe_twevo_run22.py --morning` → rc=1；④紅、`gain_basis=None` |
| 2 | 查 ④ 根因 | DB：`tw-20260803-r01` 之**欄位** `gain_basis='none'`；`gain_evidence` JSON **無** `basis` 鍵 |
| 3 | 真修探針（非手補 ledger） | `scripts/observe_twevo_run22.py`：`COALESCE(gain_basis, gain_evidence->>'basis')`；自測加 `None→紅`／`'none'→綠`；預設 audit 改 `OPT-W0-RUN22-FINAL-20260804.md` |
| 4 | morning 收口 | `--morning` rc=0 全綠；`--write-audit` → `audits/OPT-W0-RUN22-FINAL-20260804.md` |
| 5 | 65 triage 唯讀 | `reports/augur_w2_65_triage_20260804.md`（分類 65/65；零 Registry 寫入） |
| 6 | survey 複核 | `reconcile_channel_columns.py --survey`：mapped **13／98**；`source_column` **3／98** |

## morning ④ 最終狀態

| 項 | 值 |
|---|---|
| 狀態 | **綠** |
| 根因 | 探針讀錯欄：M-E3 後 basis 在正規欄 `gain_basis`，舊 SQL 讀 `gain_evidence->>'basis'` → 恆 `None`＝假紅 |
| 處置 | 修 observe；**未**手改 ledger、**未**繞閘接受假綠 |
| 最新 ledger | `iteration_uid=tw-20260803-r01`；`gain_basis='none'`（可比、無增益）；`source_run_id=22`；status=succeeded |
| 五驗 | ①②③④⑤ 全 ✓（見 FINAL audit） |

## 65 triage 關鍵計數（真查）

| 分流 | 筆數 |
|---|---:|
| 已被草案／mapped 消費（同表已 mapped） | **0** |
| B0/infra 緩登 | **13**（B0×11＋infra×2） |
| 需新概念卡 | **44**（熱路徑 1＋U0 抽樣 6＋其餘 37） |
| out_of_scope 候補 | **8** |
| **合計** | **65** |

配套覆蓋：mapped=**13**；草案殘 unmapped=**20**；`source_column` 非空=**3**；母體=**98**。

熱路徑優先（vendor 基線仍直綁且在 65 內）：`binding_id=39` `TaiwanStockBlockTrade`。

## 未做什麼

- 未解凍 FinMind／FRED；未跑 sync／probe／放量  
- 未 INSERT／UPDATE `world_concept`／`world_channel_binding`；未假寫 mapped  
- 未 `--allow-apply`、未搶 `heavy_slot`、未手動發 TWEVO  
- 未做 P0-C 草案 86／35／70 dry（甲案可選第二刀；本輪未開）  
- 未 commit／push  

## 改寫檔

| 檔 | 性質 |
|---|---|
| `scripts/observe_twevo_run22.py` | ④ 探針路徑＋自測＋預設 FINAL audit 路徑 |
| `audits/OPT-W0-RUN22-FINAL-20260804.md` | morning 全綠結案 |
| `reports/augur_w2_65_triage_20260804.md` | 65 分流報告 |
| `audits/OPT-P0-TRIAGE65-20260804.md` | 本執行帳 |

## 複核指令

```bash
cd /home/hugo/project/augur && set -a && . ./.env && set +a
venv/bin/python scripts/observe_twevo_run22.py --selftest
venv/bin/python scripts/observe_twevo_run22.py --morning          # 預期 rc=0
venv/bin/python scripts/reconcile_channel_columns.py --survey
# 報告：reports/augur_w2_65_triage_20260804.md
```

## 建議 Steward 下一句（若續）

- 確認 **out 候補 8** 是否踢出 K1 分母；或  
- 開 P0-C：草案殘 dry（86／35／70 等）——仍禁未簽寫庫；或  
- 圈選熱路徑 39＋U0 六條概念卡形制（親簽另授）。
