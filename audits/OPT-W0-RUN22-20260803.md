> ## ⚠ 本檔為**非最終／作廢**——不得引為「I5B 失效」之依據
>
> **補記時刻**：2026-08-03T16:08:55+08:00（後補抬頭，下方原文一字未刪，留痕）
>
> **寫出時刻**：2026-08-03T15:41:23+08:00，由 `observe_twevo_run22.py --morning --write-audit` 產出。
> **當時 run 22 尚未發車**——夜輪 cron 為 `0 23 * * 1-5`（`install_cron.sh:86`），而下方 JSON 自證
> `latest_run_id=21`／`latest_run_status=succeeded`、`pending_by_run={"21": 17}`。
>
> **故 ①–④ 之紅係「跑太早」，非機制失效**：五項驗收之判準皆繫於 run 22 已發車並結輪之後果——
> ①要 `latest_run_id=22`；②要出現 I5B 首見之 `superseded` 列；③要 `pending_auto` 已換代為 run 22；
> ④讀最新一列 `evolution_iteration_ledger` 之 `gain_evidence->>'basis'`，本輪未結輪故為 `null`
> （**非** `incomparable`）。在 run 22 未發之時點上，這四項**必然為紅**。
> ⑤（`evolution_apply_log` 窗內無新增）於該時點為真，惟其窗以 prerun 為界，不構成驗收。
>
> **正式驗收**：run 22 結輪後或隔晨重跑
> `python3 scripts/observe_twevo_run22.py --morning --write-audit`。
> 該指令以 `write_text` **整檔覆寫**本檔（`observe_twevo_run22.py:180`）
> ⇒ **不含本抬頭之版本即為正式帳**；仍見本抬頭者，即表示正式驗收尚未重跑。

# OPT-W0-RUN22 觀察帳（2026-08-03）

> 位階：[I] · M-T6／runbook T-22:5x＋隔晨 · **零人工搶 slot、零 --allow-apply**
> 寫入：2026-08-03T15:41:23+08:00

## 前置快照

- CSV：`audits/prerun22_pending_snapshot_20260803.csv`

## 隔晨機械五項

| # | 條件 | 結果 |
|---|---|---|
| ① latest evolution_run status=succeeded 且 run_id=22 | ✗ |
| ② superseded 列 > 0（I5B 首次） | ✗ |
| ③ pending_auto 全屬 run 22（或 0 列） | ✗ |
| ④ 最新 ledger gain basis ≠ incomparable | ✗ |
| ⑤ evolution_apply_log 無偷跑新增（相對窗） | ✓ |

## 現查數字

```json
{
  "n_pending_auto": 17,
  "pending_run_min": 21,
  "pending_run_max": 21,
  "pending_by_run": {
    "21": 17
  },
  "n_superseded": 0,
  "latest_run_id": 21,
  "latest_run_status": "succeeded",
  "gain_basis": null,
  "apply_log_new_since_prerun": 0,
  "apply_log_today_n": 0
}
```

**總評**：有紅——對照 runbook「不對就停」
