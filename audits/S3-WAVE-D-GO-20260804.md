# S3-WAVE-D GO 帳 [I]（2026-08-04）

> **位階**：[I] 授權留痕（非 META [N]）
> **計畫**：`reports/augur_s3_wave_d_sequence_graph_plan_20260804.md`（plan-first，已呈用戶過目並經 `AskQuestion` 選定範圍）
> **母 SSOT**：`reports/augur_s3_features_for_market_model_families_20260804.md` §6.2

---

## 1. Steward 原文與範圍選定

觸發句：使用者「單一最高槓桿下一手」→ 呈計畫書＋`AskQuestion` 三選項 → 使用者選定：

```text
S3-WAVE-D-go | FZ/GATE-keep | skip-sync | no-SIM-apply
```

**範圍＝選項「phase12b」**：Phase 1＋2a＋2b（規劃到 dry-run；**不含**首次寫庫）。**Phase 2c（`stock_graph_edge` 首次 `--commit` 寫入）明示排除**，須另一次明示（`+ allow-graph-edge-commit`或等價）才可執行。

## 2. 授權四要件（CLAUDE #26）

| 要件 | 內容 |
|---|---|
| (a) 範圍 | 組 12 序列窗 library＋CLI（唯讀）；組 13 產業分布查詢＋`stock_graph_edge` DDL＋builder `--dry-run`；**不含** `--commit` |
| (b) 期限／結束條件 | 本次會話內完成 Phase 1+2a+2b 即結束；逾期或會話中斷須重新授權 |
| (c) 可撤銷 | 隨時可撤回；撤回後停在最後完成之 Phase |
| (d) 計畫參照 | `reports/augur_s3_wave_d_sequence_graph_plan_20260804.md` §4 分階段表 |

## 3. 硬邊界（本波不可逾）

- `skip-sync`：零 FinMind／FRED 呼叫（全程只讀既有 raw／`core_universe_asof`／`feature_values`）。
- `no-SIM-apply`：不碰 sim。
- `FZ/GATE-keep`：不改任何既有 gate 判準。
- **`stock_graph_edge` 不寫入資料列**（DDL 建表允許，資料 INSERT 不允許）。

---

*GO 記錄完成，接續 `audits/S3-WAVE-D-EXECUTED-20260804.md`。*
