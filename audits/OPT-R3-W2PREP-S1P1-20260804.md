# S1／P1 輕量執行帳（W2prep · 2026-08-04）

> **授權**：`S1P1-light-go`。**禁** `--apply`／搶 slot／宣稱可交易。

## S1 sim

| 指令 | rc／結果 |
|---|---|
| `python scripts/run_sim_calibration_cell.py --selftest` | **0**／全通過 ✓（格點／門防衛／ledger SQL 紅測） |
| `python -m augur.catalog.world_concept --selftest` | **0**／全通過 ✓ |
| `python scripts/verify_sign_consistency.py --selftest` | **0**／全通過 ✓ |

**未做**：`run_sim_calibration_cell --apply`；`--record` 符號尺寫庫。

## P1 庫內 predict dry

| 指令 | 結果 |
|---|---|
| `python scripts/predict_asof.py --run --dry-run` | **拒載中止（誠實）**：`feature_source=prodset` 特徵漂移——frozen `lending_fee_rate_mean_20d` vs current `lending_fee_rate_mean_30d`（＋`cycle_position_252d` 等） |

→ **非 API 問題**；屬 prodset／artifact 與現行特徵名不一致。後續須另裁：對齊特徵名、或明示研究臂 `--feature-source=canonical`（若 CLI 仍供）、或重訓 as-of artifact——**本輪不擅改**。

## 不做

- 未假稱預測成功；未寫 `prediction_values`；未開 FinMind。  
