# MC as-of＝2026-08-04 重跑 [I]（2026-08-05）

> **位階**：[I] 工具／接續記憶（非 META [N]）  
> **授權**：Steward「其 as-of 應該更新到 2026-08-04」＋ AskQuestion `rerun_emit`  
> **範圍**：`mc_simulation_run` cone · horizon_td=30 · 雙法 · 與 08-03 同一 52 檔 · **零** FinMind／FRED · 不 `--allow-apply`

---

## 1. 指令／log

```text
# 股票清單＝庫內 asof=2026-08-03 h=30 block_bootstrap 之 52 檔（SQL DISTINCT）
python scripts/simulate_mc_paths.py --run --stocks <52> \
  --asof 2026-08-04 --horizons 30 --n-paths 10000 --seed 42
```

- **log**：`logs/mc_asof_20260804_rerun_20260805.log`
- **結束**：stdout `✓ 模擬完成`（seed=42；iid_bootstrap + block_bootstrap）
- **未** sync；**未**持有 `heavy_slot`

---

## 2. 驗收（SQL 親查）

| 條件 | 列數 |
|---|---|
| `asof_date='2026-08-04' AND horizon_td=30 AND method='block_bootstrap' AND summary ? 'cone'` | **52** |
| 同上 `method='iid_bootstrap'` | **52** |
| latest-asof 排名宇宙（DISTINCT ON target → asof） | **min=max=2026-08-04，n=52** |

舊列 `asof_date∈{2026-05-31,2026-08-03}` **仍並存**（本輪未刪）。UI `_mc_*` 已 `ORDER BY asof_date DESC` → 自動讀 08-04。

樣本：2330 block_bootstrap ret_p50≈**+6.22%**（last_close **2320** → px_p50≈**2464**；相對 08-03 榜 last_close 2370 已改寫為 08-04 收盤）。

---

## 3. UI

| 項 | 結果 |
|---|---|
| 服務 | `systemctl --user restart augur-probability`（loaded active） |
| 預期腳注 | `MC 榜 as-of 2026-08-04（n=52）` |
| 誠實 | 模擬非預測・`is_simulation` 硬綁・只存分位錐 |

---

*完成日：2026-08-05。位階 [I]。*
