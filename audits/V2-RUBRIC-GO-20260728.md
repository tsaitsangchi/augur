# V2-RUBRIC-go ＋ SUNSET-C-align 執行登錄（2026-07-28）

> **性質**：判準層變更之執行登錄。**拍板**：hugo 2026-07-28 對話逐字「**V2-RUBRIC-go與SUNSET-C-align 都給**」（承前一日 `audits/V2-SUNSET-C-DISPUTED-20260727.md` §五之 S-1/S-2/S-3 提案與量測數字）。
> **繕寫**：claude 起草並執行、決策者＝hugo（§8.1 分立記載，不冒充親簽）。修訂唯增列（P4.E3）。

## 一、換尺程序（有意識地換，非漂移）

| | 舊尺 | 新尺 |
|---|---|---|
| `eval_code_hash` | `f3075238eb55`（07-26） | **`ef142e9374c1`**（中繼 `0646872fdce7`＝robot v1 僅離線臂，未用於任何 LLM 臂） |
| `set_id` | `4183475c5089` | **不變**（S-4 凍結集重建另案待裁） |
| 舊 run | **留檔未刪**，`--compare` 明列排除不並排 | — |
| 尺之錨 `PINNED` | — | 更新為新 hash；再漂移即自測紅 |

## 二、判準器變更四件（S-2 ①②③＋S-3）

**① `ABSTAIN_RE` 補「未找到／找不到」**。07-27 實證 pack 於 L3 至少 6 題明文「未找到匹配的文獻標題《X》」且零編造，被詞表缺口誤殺。**安全驗證**：補詞後四支對照臂 A 軸逐格零變動（floor 仍 0、ceiling 仍 1）——只更準不更鬆。

**② L1 F 軸加「加料年份否決」**（`fact_exact(source_text=)`，`judge` 貫通題幹）。事實全中但答案冒出**來源沒有的年份**→ F=0（照抄＋捏造＝捏造）。07-27 實證 pack 8/30、grammar 2/30 之 L1 答案冒出題幹沒有的年份（2024/2026），舊尺 F 仍全判 1＝RAG 幻覺量不到。無 `source_text` 時 veto 關閉（向後相容）；ceiling／behavior 實測零影響。

**③ floor 換最強退化常數**。舊樣板五格全 0 是**空證**（剛好不含「查無/多筆」二詞的弱字串）；新 floor＝`查無此鍵;同名多筆請指明。欄位型別 VARCHAR 或 NUMERIC。請執行:SELECT * FROM knowledge_item WHERE title = ...;`（拒答詞＋多筆詞＋二值型別字＋單一表名 SQL 模板、零年份）。**語意反轉**：floor 在無鑑別力之格「就該高分」，格子空不空由此曝光。自測改鎖**剖面** `[0,1,1,1]`。

**④（S-3）robot 第五對照臂＋鐵則升級**。零知識格式規則機入 `ARMS_OFFLINE`（只看題幹表面：`[檢索片段]`→照抄片段、`[無檢索片段]`→按問句模板推表名出 SELECT、否則「查無 多筆」；判序=文獻→KI、相關→FC、餘→CC）。`evidence_protocol.ARMS` 增 `robot`：**live 未嚴格勝過 robot 之格＝格式即可達＝判 `none`**（robot 缺席時判讀同前，不溯及舊資料）。另修正 `evidence_protocol` 自測之虛指錨——「live 0.933 兩機逐位元一致」不實（逐位元一致者僅四離線臂；live 兩機 0.933 vs 0.9667 不一致＝複現未成立之實證）。

**⑤（隨批）`run_id` 帶 attempt 序**。舊式 `run_id=hash(五元組)`＋`ON CONFLICT DO NOTHING` 使同尺同臂重跑**第二次結果被靜默丟棄**——SUNSET (c) 之「可被獨立重跑複現」結構上不可記錄。今起每次重跑各成一列。

## 三、新尺離線五臂實測（`set_id=4183475c5089`／`ef142e9374c1`，各 120/120）

| 格 | ceiling | floor | shuffled | mismatched | **robot** |
|---|---|---|---|---|---|
| L1.F | 1.000 | 0.333 | 0.167 | 0.000 | **1.000** |
| L1.P | 1.000 | 0.333 | 0.900 | 0.533* | **1.000** |
| L2.P | 1.000 | 0.333 | 0.900 | 0.000 | **1.000** |
| L3.A | 1.000 | 1.000 | 0.967 | 0.000 | **1.000** |
| L4.A | 1.000 | 1.000 | 0.967 | 0.000 | **1.000** |

（*彙總欄 P=0.267；逐格值沿 07-27 拆解。）**判讀**：ceiling 全 1.000＝尺未被修壞；floor 剖面與預測逐格一致；**robot 五格全 1.000**＝本凍結集**每一格都是零知識格式可達**——在鐵則下，任何 live 臂於本集**任何格**至多 `none`。這不是新尺壞了，是新尺誠實說出本集無法證明能力；要有可證能力之格，唯 S-4（凍結集重建）能給，該案待 hugo。

## 四、SUNSET-C-align（S-1 處置）

週報 (c) 判定式重寫為**逐字對齊凍結原文**（criteria_sha `65eda893…` 未動）：同尺（set_id＋eval_code_hash、`is_invalid=false`）之**受測臂** `axis_f` **嚴格** > floor 且 > mismatched（首半）＋同尺同臂 **≥2 個獨立 run 皆成立**（複現）。解釋登錄：「任一臂」取受測臂——字面連 shuffled 都滿足＝荒謬。實跑輸出：

```
⬜ (c) 同尺 4183475c/f3075238:floor F=0.0 mism F=0.0;
   首半(嚴格同勝)之臂=['behavior','pack:…'];複現(≥2 run 皆勝)之臂=**無**
```

第一行回到「**三條件皆未達成**」。⚠ 07-27 之「⚠未判定」過渡態同日退場；07-27 前任何「(c) 已達成」宣稱依本檔 §二⑤ 皆屬無效（複現當時不可記錄）。

## 五、明確不做（本批）與殘餘待裁

- **不做**：S-4 凍結集重建（換 `set_id`＝作廢一切既有比較）；S-5 pack retire；S-6 (b) 基線位移認定；S-7 榮譽制認領；S-8 判準 A 語意權——**全部仍待 hugo**（清單住 DISPUTED audit §五）。
- **不自行製造複現**：新尺首輪 LLM 臂各跑一次；(c) 之複現要件今起**可以**被第二次重跑滿足，但在 S-8（門檻語意）未裁前，claude **不主動**排第二次同臂重跑去閉合一條 SUNSET 條款——閉合與否連同其語意由 hugo 決定。

## 六、重跑狀態

離線五臂已落庫（§三）。LLM 三臂（grammar→behavior→pack）背景重跑中（bg `b40tciaqo`，估 ~4h45）；完成後 `--compare` 出新尺全表。

---

## 七、EVALSET-V2 落地追記（同日；P5 判準 A 退史料）

**v2 集已凍結**：`set_id=4e15a143ff4b`（132 題、五格、全真列）、孿生尺 `eval_code_hash=35aeffc3e160`。**P2 官方閘全過**：ceiling 五格 1.000；能力格 C1.F／C2P.F 之 robot=0.500、floor=0.500（＝盲策略理論上界，對照孿生如構造生效）、mismatched=0.000。首批「零知識機器通不過」的能力格成立。

**判準 A 正式退史料**（EVALSET-V2-go 授權之 §三處置，兩句記錄、不塗改）：
1. 判準 A 之數值門檻通過為真（behavior F@L1 0.933／0.9667 > 0.167 > 0，各機各尺留檔）。
2. 其能力語意經對抗驗證不成立（常數字串 0.2333、零知識規則機 1.000 皆越過同一門檻）——**此後任何能力宣稱不得引判準 A**，一律走 A′（v2 能力格 `evidence_level ≥ weak`＝嚴格勝 floor ∧ mismatched ∧ robot，且同臂 ≥2 獨立 run 皆成立）。

**中繼尺 `ef142e9374c1` 三臂 INVALID 事故記錄**：grammar 44/120、behavior／pack 0/120（120×URLError）——死因非模型非 harness，係 `augur-ollama.service` 於 ~09:04 停止、另一 Cursor session 於 09:05 手動 `ollama serve`（疑似遷移 OLLAMA_MODELS）。INVALID run 留檔不刪。**P4（v2 LLM 臂 ×2）與 S-5 重評暫緩至 Ollama 後端歸屬經 hugo 確認**——不在施工中的車道上放 10 小時級批跑。

**§七追記之更正與定稿（同日 09:30）**：孿生尺定稿為 **`aeff01c18ace`**（`35aeffc3e160` 為中繼——BEHAVIOR_PROMPT 措辭仍引 v1 題殼「[檢索片段]」，對 v2 統一殼「[檢索結果]」不公平地壓 behavior 臂，適配後最後一次換錨；兩集離線臂已在最終尺全數落庫，v2 能力格閘複驗不變：robot=0.500／floor=0.500／ceiling=1.000）。**Ollama 車道親驗記錄（hugo 指示查證）**：`augur-ollama` 為 user unit、09:12:43 起 active（canonical 配置 `OLLAMA_MODELS=~/ollama/models`，qwen3:4b/8b/nomic 三模齊）——先前「unit 被刪／野進程」判讀係誤用 system 層查詢，更正；慢（16 token 3m24s）之根因＝**qwen3:8b 正被 chat/advisor 端並行使用＋4b/8b 雙載致 swap 4.5G**，非服務損壞。P4/S-5 已掛哨兵自動放批（8b 靜＋探針連兩快才開）。
