# U0-37 dry SQL — binding 37／`jp.daily_bar`（2026-08-04）

> **位階**：[I]。  
> **授權**：`Q-R8=jp-ok`＋`REGISTRY-GO: binding=37 + honesty=37 + decided_by=hugo`  
> **jp-ok**：`audits/U0-37-JP-OK-20260804.md`  
> **honesty**：`audits/U0-37-HONESTY-ISSUED-20260804.md`  
> **形制**：Q-R1=(a) 原地 UPDATE；W2-1=(a) 分隔字串；category=`quantity`（概念卡 U0-2）。  
> **硬界**：`Adj_Close` **出／不入**本 binding；欄位須對 `information_schema` 全 present（fail-closed）。

---

## 1. 假設摘要

| 項 | 值 |
|---|---|
| binding | **37** |
| source_table | `JapanStockPrice` |
| concept_key | `jp.daily_bar` |
| category | `quantity` |
| channel_role（既有） | `observation` |
| source_column | `Open,High,Low,Close,Volume`（無空白） |
| cross_market_axis | 日本市場交易日曆軸（非台股；與台股 as-of 對齊規則待裁） |
| 出欄（本批不登） | `Adj_Close` |

---

## 2. ⚠ DRY — BEGIN…ROLLBACK（演練形）

```sql
-- U0-37 dry / ROLLBACK rehearsal — binding 37 only
BEGIN;
SET LOCAL augur.honesty_write = 'on';

-- fail-closed：五欄皆須存在於 JapanStockPrice
DO $$
DECLARE
  missing text;
BEGIN
  SELECT string_agg(c, ',') INTO missing
    FROM (VALUES ('Open'),('High'),('Low'),('Close'),('Volume')) AS t(c)
   WHERE NOT EXISTS (
     SELECT 1 FROM information_schema.columns
      WHERE table_schema='public' AND table_name='JapanStockPrice' AND column_name=t.c
   );
  IF missing IS NOT NULL THEN
    RAISE EXCEPTION 'U0-37 fail-closed: missing JapanStockPrice columns: %', missing;
  END IF;
END $$;

INSERT INTO world_concept (concept_key) VALUES ('jp.daily_bar');
INSERT INTO world_concept_version
    (concept_key, category, authoritative_binding_id, ts_semantics, knowability_rule,
     cross_market_axis, provenance, finality_predicate, conflict_set_ref,
     decided_by, decided_at)
VALUES (
    'jp.daily_bar', 'quantity', 37, '交易日（日本市場）',
    '收盤後當日可得（法定公開規則型；表內無公告欄）',
    '日本市場交易日曆軸（非台股；與台股 as-of 對齊規則待裁）',
    jsonb_build_object(
        'basis', 'Q-R8=jp-ok + REGISTRY-GO binding=37',
        'dry_sql_ref', 'audits/U0-37-DRY-SQL-20260804.md',
        'w2_1_form', 'delimiter_string',
        'q_r8', 'jp-ok',
        'observation_columns', 'Open,High,Low,Close,Volume',
        'out_of_scope', jsonb_build_array('Adj_Close'),
        'category_note', 'quantity＝日頻 OHLCV 觀測（概念卡 U0-2）'),
    '當日值於次一交易日收盤後定案', NULL,
    'hugo', clock_timestamp());

UPDATE world_channel_binding
   SET concept_key='jp.daily_bar', mapping_status='mapped',
       source_column='Open,High,Low,Close,Volume',
       provenance=coalesce(provenance,'{}'::jsonb)||jsonb_build_object(
           'map_note','U0-2 jp-ok 2026-08-04',
           'w2_1','delimiter_string',
           'out_candidate','Adj_Close 不入')
 WHERE binding_id=37 AND superseded_at IS NULL AND mapping_status='unmapped' AND concept_key IS NULL;

-- 期望：INSERT concept 1；INSERT version 1；UPDATE 1
SELECT concept_key, category, authoritative_binding_id, cross_market_axis, decided_by
  FROM world_concept_version
 WHERE concept_key='jp.daily_bar' AND superseded_at IS NULL;

SELECT binding_id, concept_key, source_column, mapping_status
  FROM world_channel_binding WHERE binding_id=37 AND superseded_at IS NULL;

ROLLBACK;
```

---

## 3. COMMIT 變體（親簽執行）

同上塊，僅將最後一行：

```sql
ROLLBACK;
```

改為：

```sql
COMMIT;
```

其餘字面不變（含 `decided_by='hugo'`、`SET LOCAL augur.honesty_write='on'`、fail-closed DO 塊）。  
執行後寫 `audits/U0-37-REGISTRY-EXECUTED-20260804.md`；honesty 本批**已消費**。

---

## 4. 驗收指令（COMMIT 後）

```bash
cd /home/hugo/project/augur && set -a && . ./.env && set +a
venv/bin/python -m augur.catalog.world_concept --resolve jp.daily_bar
venv/bin/python -m augur.catalog.world_concept --check
# 期望 mapped 20→21；sc 10→11；binding 37 mapped／source_column 如上
```

---

*完。dry＝ROLLBACK 形；COMMIT 僅改末行。*
