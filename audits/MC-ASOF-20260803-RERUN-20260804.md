# MC as-of＝2026-08-03 重跑 [I]（2026-08-04）

> **位階**：[I] 工具／接續記憶（非 META [N]）  
> **授權**：Steward 裁——跳過 sync、直接重跑 MC（解凍 audit 見 `audits/API-THAW-20260804.md`；本任務**零** FinMind／FRED）  
> **範圍**：`mc_simulation_run` cone · horizon_td=30 · 雙法 · 現有 cone 52 檔 · 不搶 `heavy_slot` · 不 `--allow-apply`

---

## 1. 指令／log

```text
# 股票清單＝庫內既有 cone asof=2026-05-31 之 52 檔（SQL DISTINCT，非手抄）
python scripts/simulate_mc_paths.py --run --stocks <52> \
  --asof 2026-08-03 --horizons 30 --n-paths 10000 --seed 42
```

- **log**：`logs/mc_asof_20260803_rerun_20260804.log`
- **START**：2026-08-04T08:56:43+08:00 · `STOCKS_N=52` · `HEAVY_SLOT_STILL_FREE_AT_START`
- **結束**：stdout 含 `✓ 模擬完成`（seed=42；雙法並列 iid_bootstrap + block_bootstrap）
- **未**持有／申請 `heavy_slot`；**未** `--allow-apply`；**未** sync

---

## 2. 驗收（SQL 親查）

| 條件 | 列數 |
|---|---|
| `asof_date='2026-08-03' AND horizon_td=30 AND method='block_bootstrap' AND summary ? 'cone'` | **52** |
| 同上 `method='iid_bootstrap'` | **52** |
| distinct `target_id`（h=30／asof=08-03／cone） | **52** |

舊列 `asof_date='2026-05-31'` h=30 雙法各 52 **仍並存**（本輪未刪）。

---

## 3. UI 修補（最小）＋ as-of 文案

| 項 | 結果 |
|---|---|
| `_mc_run` | `ORDER BY asof_date DESC LIMIT 1`（同 method／fallback 皆優先最新 asof） |
| `_mc_p50_rankings` | `DISTINCT ON (target_id) … ORDER BY target_id, asof_date DESC, method 優先` |
| `--selftest` | ✓ |
| `--check` | ✓；`/simulate` Top10 真日 **`['2026-08-03']`** |
| 渲染親證 | 口徑句 `as-of＝2026-08-03`；腳 `MC 榜 as-of 2026-08-03（n=52）`；rank asof_lo=hi=2026-08-03 |
| 服務 | `systemctl --user restart augur-probability`（已載入） |

**commit**：本輪**未** commit；UI 修補仍在工作樹，待 Steward 明示是否入庫。

---

*完成日：2026-08-04。位階 [I]。*
