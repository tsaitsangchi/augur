---
status: go
series: s5_predict
depends_on:
  - audits/PREDICT-EMIT-H20-20260731-EXECUTED-20260805.md
  - audits/S3-JULY-CORE-FEAT-EXECUTED-20260805.md
---

# GO｜日頻面板 2026-08-04 特徵／出單／emit · 2026-08-05

> **授權**：Steward AskQuestion `execute_chain` → `go_full`  
> paste：`S5-DAILY-2026-08-04-CHAIN-go | FZ/GATE-keep | skip-sync | no-SIM-apply`  
> **口徑**：非另建日頻特徵表——對 **交易日 2026-08-04** 寫一筆 `feature_values.panel_date`（與月末同 builder），再 core asof → RankRidge H20 predict → P6 emit。  
> **self-reported（#32a）**。

## 為何「另開」

| 既有 | 缺 |
|---|---|
| `daily_direction_feature_values` 已到 08-04 | RankRidge／prodset 讀的是 **`feature_values`**（須 panel_date＝出單日） |
| `feature_values`／core／pv／pp 止於 **07-31** | 無 08-04 列 → predict／emit 拒跑 |

## 執行序

1. `build_feature_panel.py --panels 2026-08-04 --asof`
2. `build_core_universe.py --since 2014-01-01 --liquidity-pct 25 --exempt-revenue-financial --asof`
3. `predict_asof.py --run --horizon 20 --asof 2026-08-04`
4. `calibrate_relative_probability.py --emit --horizon 20 --asof 2026-08-04`

## 不做

FinMind／FRED；SIM-apply；改 DEFAULT_FREEZE；重 fit 校準器（沿用既有 H20 calibrator 套新分位）。
