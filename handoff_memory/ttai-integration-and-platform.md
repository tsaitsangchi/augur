---
name: ttai-integration-and-platform
description: 本機成接續主機(DB restore)+ augur 前後台平台重建 + TTAI/Tiptop ERP 整合計畫(深審+拍板)
metadata: 
  node_type: memory
  type: project
  originSessionId: c3c40e0c-7154-4936-8937-6d9ce947808c
---

**2026-07-06 本機(WSL2 /home/hugo/project/augur)成接續主機 + 平台重建 + TTAI 整合計畫**(tag `ttai-integration-plans-20260706`、commit 6ffb364)。

## 換機 + DB
- 本機 pull 進 remote 58 commit(另一台機器的 RBAC/UI/advisor/models F3/text v3.0/治權 v1.21-v1.35)→ HEAD 對齊。
- **DB 完全取代**:匯入 `C:\augur\augur_20260705_2327.dump`(6.69GB custom、165 表、含預測層+RBAC+chat)取代本機舊 44GB DB。方法:vector ext 非 trusted+augur 非超級使用者→不能 DROP/CREATE DB→**就地 drop 全表(保留 public schema+vector ext)+ `pg_restore --clean -L 濾掉 extension 的 TOC`**。實測數:knowledge_item 111,259/lexicon 154,875/concordance 49.1M/app_user 1(admin superuser)/feature_values 2.42M。
- 治權現行:靈魂 v1.5.0/原則精華 v1.7.1/憲章 **v1.36.0**(2026-07-06 加 owned_local 三軌)/CLAUDE **v1.18**。詳 HANDOFF.md(根目錄)。

## 平台前後台(全 127.0.0.1、stdlib http.server)
- **前台對話** http://localhost:8090(serve_chat_ui)→ **顧問殼** :8399(serve_advisor_openai,advise+guard)→ **Ollama** :11434(qwen3:8b,已載);**後台知識控制台** :8500(serve_admin_console)。
- 登入:`admin` / `⟨見 .env AUGUR_ADMIN_PASSWORD⟩`(前後台同組;我重設 app_user 密碼=同值;admin 後台亦可帳號留空走 env 後門)。`.env` 加 AUGUR_INTERNAL_SECRET/AUGUR_ADMIN_PASSWORD/OLLAMA_TIMEOUT=1800(不進 git)。
- Ollama user-space 裝(免 sudo):`~/ollama/bin/ollama`,新版是 `.tar.zst`(用 pip `zstandard` 解壓,無 zstd binary)。serve 用 `setsid nohup` detached 常備。
- **CPU 無 GPU→qwen3:8b 分鐘級**:advisor→ollama `OLLAMA_TIMEOUT` 與前台→advisor proxy(serve_chat_ui.py)兩層逾時都調 300/600→**1800s**。端到端實測 MBB/SMBB guard pass=true。嫌慢可換已裝的 qwen2.5:3b(快)或接 Gemini(.env 有 key)。
- 重啟服務用**埠號精準 kill**(`lsof -ti tcp:PORT`)非 pkill(自匹配自殺教訓)。CLAUDE #7:改常駐服務碼須重啟載新碼。

## TTAI/Tiptop ERP 整合計畫(plan-first、未實作、待拍板)
- **TTAI** = 本機 `/home/hugo/project/ttai` + `ttai` DB(214MB)= 成熟 Tiptop ERP 知識抽取專案(自有治權/Qdrant);ttai DB 16 表全在 **buffer schema**;⚠**2026-07-17:augur 庫現有 `ttai_import` schema 22 表(16 BASE+6 VIEW)、ERP 已放量嵌入**(原「public 無鏡像」已過時)、knowledge_unit 142,040(unit_type:module/program/table/column/function/code…)、bge-m3/1024/Qdrant。
- 定位:ERP 知識整合進 augur **知識/素養層**(讓「誠實博學的我」答 ERP/4GL/4FD 問題)、守 v1.19 **零量化價值/不進預測管線/不產因子/clean-room 不回流 code**。
- 三份計畫(reports/,tag 內):`augur_ttai_erp_integration_master_plan_20260706`(母)/`augur_ttai_vector_unification_plan_20260706`(向量)/`augur_prediction_sop_master_20260706`(股市預測 SOP,另線)。
- **五鏡深度審計 43 agents/24 confirmed** 修正(向量計畫『public 可讀』假事實=實為 buffer 拒、retrieval 路徑=`philosophy/retrieval.py`、雙軌建 item 去重命門、圖問答無檢索路徑、domain 隔離靠 RBAC 拓撲非 code 過濾);**M6/M8/M9 用戶拍板=A**(D11 單一 item 走 promote_item 掛載/D12 圖問答列未來工作現版只落圖資料/D13 domain 靠 RBAC 單域對單群組拓撲、retrieve_all 不 filter domain〔retrieve_items 才收 domain 參數〕)。
- **向量決定**:ERP 用 **e5-small 重嵌進 augur pgvector、棄 Qdrant/bge-m3**(維度 1024 vs 384 不可混、embedspec.dim_for fail-closed);走既有 `--scope items`→`knowledge_sentence_embedding` 零新 DDL。**bge-m3 品質取捨**(bge-m3>e5-small、TTAI 為繁中 ERP 才選它)列 pilot rank@k gate;用戶問過「未來換 6144 core+128GB 統一記憶體要不要 bge-m3」→答:大庫+好硬體 bge-m3 較適、CPU 算的向量與 GPU 同(現在改不白做);**bge-m3 vs e5-small pilot 執行中**(背景 b13wvlbxh、獨立臨時表 pilot_bge_chunk、2000 chunk 側對照,結果定 D0)。
- **待拍板/待辦**:D2 domain 註冊、其餘 D 清單。

## 2026-07-06 續:三軌入憲 + TTAI 放量 + advisor Tier-1 + 股市預測 B(兩封存)
- **憲章 v1.35→v1.36.0 全文准入三軌**:哲學公版 work_text／知識 CC item_text／**自有私有 owned_local**(硬綁 `access_scope='local_private'`、DB CHECK `chk_itext_owned_local_private`;為 ERP 自有內容鋪合法軌,pilot 撞 license CHECK 而生)。同步 corpus.LICENSE_WHITELIST／webupload.LICENSES／migrate DDL CHECK。
- **D0=A(e5-small)定案**:bge-m3 pilot MRR 僅 +0.016 但 11.9× 慢 → 選 e5-small。**TTAI 放量嵌入竣工**:erp_tiptop item 141,825／owned_local item_text 150,685／句嵌入 **151,849**(99.93% 覆蓋);**治理零違規**(owned_local 全 = local_private,放量下守住)。
- **advisor Tier-1 誠實 relevance 閘**(修 MBB out-of-corpus 自信講錯):新 `advisor/relevance.py`——query↔citation 內容詞重疊 < FLOOR 0.30 → 判實質空檢索走既有 honesty decline;**只更嚴不更鬆**(guard.py byte-identical)。**四鏡對抗 workflow 全 PASS**(guard 未鬆／生產走 Mode A 閘會觸發／測試無粉飾〔5 既有測合法對齊、斷言逐字未動〕／prompt 無編造)。端到端實測:前台 8090 登入(admin/`⟨見 .env AUGUR_ADMIN_PASSWORD⟩`、**表單密碼欄名=`pw` 非 password**)→ MBB → 「知識庫中無此內容」。殘留 low:短 CJK query 含高頻字巧合重疊可漏放(非新回歸、退主路徑)。
- **股市預測 STAGE B 提拔三審通過**(A'-3 新嚴格 embargo:逐折保證 embargo ≥ h+62td):H20 HAC-t **+6.09**／H60 **+6.95**(≫2 顯著)、shuffle IC≈0(無洩漏)、H20 as-of≤pan-hist / H60 輕旗標;**IC 未因嚴 embargo 崩(H60 +0.1521 vs 舊 +0.1418)=alpha 對 purge 穩健(真兆)**。限制:n=25、僅 IC 層、經濟價值 #14 為 C/D。報告 `reports/augur_prediction_stageB_promotion_verdict_20260706.md`。
- **兩封存**:①`treaty-v1.36-predict-isolation-20260706`(d797392:憲章三軌+A' 隔離雙閘 import_isolation/augur_predict role+A'-3 embargo)②`advisor-tier1-predict-stageB-20260706`(f3e4852:advisor Tier-1+B 裁決)。中間 commit 7a80527 補齊 d797392 漏收的憲章內文。
- **教訓**:(a)`git mv` 已 staged rename 後,若 `git add <清單>` 漏列該檔,**內容編輯不會進 commit**——d797392 只收 R100 純 rename、憲章 owned_local 條文/標題/修訂史全落在工作區,commit 訊息一度與內容不符(#15)、次 commit 補;(b)co-author 本 session 起改 **Claude Fable 5**(前段 Opus 4.8,誠實反映換模型);(c)advisor secret 模型:前台無條件送 `X-Augur-Internal`(同 .env)、安全重啟只更嚴不破前台。

## 2026-07-06 再續:C/D 經濟價值 + TTAI 檢索裁決 + 蒸餾 S1-S5(第三封存 `distill-pipeline-stageCD-ttai-verdict-20260706`、commit 248e847)
- **股市預測 STAGE C/D 經濟價值 #14 通過(有界)**:8-cell 矩陣(Ridge/GBDT×H20/60×LO/LS、top10%、cost 0.585%、A'-3 embargo、asof)。**H60 long-only=可交易真兆**:Ridge 淨 Sharpe **1.20(2014起)/1.27(2021起)** 皆勝基準 0.76/0.94、Calmar~1.2、對樣本期穩健。**H20 扣成本已死**;**long-short 期間脆弱**(2014起 1.68 亮眼但 2021起 0.406 輸基準)。Ridge>GBDT。報告 `reports/augur_prediction_stageCD_economic_verdict_20260706.md`。**方法論教訓(#15)**:對抗 workflow 兩鏡都不完全對——run agent headline 過度樂觀 LS、cherrypick 鏡指控「捏造」但歸因錯(用了不同 --since 2021 vs run agent 2014)——**唯作者親跑同口徑對照才定案**(我 --since 2014 精確重現 run agent 數字=未捏造,但 --since 2021 證 LS 近期崩=cherrypick 鏡實質對)。真兆須自證、勿轉述 agent。
- **TTAI 全庫跨語檢索裁決=局部可用**(報告 `reports/augur_ttai_retrieval_verdict_20260706.md`):zh 檔名式 query rank@1(料件/工單/庫存/供應商主檔),但**英文問 ERP 幾乎不可用、跨語幾乎全垮**;**e5-small 誠實崩壞為真**(呼應 MBB #15):查應收帳款回**應付帳款且 0.89 最高分**(語意反)。根因=權限設定檔 boilerplate 29,121 句(19.2%)污染 + 破碎 SQL 殘句 + ERP 不在 concordance(純 ANN 無保底)+ 電報式 schema 標籤跨語對齊弱。建議(後續、待排序):清 boilerplate(最高 CP)→ 濾破碎句 → 建 ERP concordance/rerank → 雙語擴展或 bge-m3。**advisor Tier-1 相關度閘對 ERP 亦必要防線**(擋應付當應收)。
- **advisor 蒸餾 Tier-2/3 決策(用戶拍板)**:硬體未到位(⚠**2026-07-17 實查 RAM=15GB 非 7.7GB**→原「7.7GB→S6 蒸餾延後」之前提值得重估;GPU 待確認 nvidia-smi;qwen3:8b LoRA 物理不可行之結論須以新 RAM 重評)→ S6 訓練延後;**現在跑 S1-S5 資料合成**;Tier-3 先不決定。**S4 teacher 機制=workflow Claude agents**(.env 無 ANTHROPIC_API_KEY)。
- **蒸餾管線 S1-S5 建置(第三封存)**:5 腳本(migrate DDL+S2 生題+S3 真檢索 context+S4 teacher 骨架+S5 guard/grounding 校驗→SFT jsonl)+ 2 表 `advisor_distill_question/context`(**界線-A 住 advisor 側、界線-B context/target_response 分欄、target S4 前 NULL**)。隔離擴充:`import_isolation.py` 加 DISTILL 稽核(**0 違規、負向測試證抓得到**)、`setup_predict_role.py` FORBIDDEN 加 `advisor_distill_`。guard.py byte-identical。pilot 38 題 DP7 過(out-of-corpus+decline 60.5%)。**重要誠實發現**:情境1 in-corpus 題多數 S3 實檢索 relevant=False(多數非化學知識項 metadata-only 無 embedded 全文)→ 樂觀 ANSWER 標籤須以 S3 真相關覆寫(低相關→DECLINE)。三封存 tag 見 memory/git。
- **蒸餾 S1-S5 pilot 完成(2026-07-07、報告 `reports/augur_advisor_distill_pilot_20260706.md`)**:S2 擴 **pilot2 274 題**(in-corpus 只從真有句嵌入的 21 部哲學經典+chemistry 取)→ S3 真檢索 → **S4 teacher workflow 23 agents 生 274 gold**(界線-B:事實只溯 context)→ S5。**pilot bank 171 條乾淨 SFT**(`data/distill/sft_pilot2.jsonl`、gitignore 本地)、**drop 37.6%<40% GATE 過關**。**教訓**:(a)**teacher 嚴守界線-B 拒張冠李戴**——~21 題「檢索到錯書」的 ANSWER 誠實改判 DECLINE(六韜→莊子/論語核心思想→論衡/三十六計→王陽明);(b)**古籍抽象問法(XX核心思想)檢索召回弱、常撈到錯書**(同 TTAI e5-small 崩壞);(c)**S5 首輪 100% drop=校驗器 flaw 非 gold 爛**——`grounding_ok` 用文言算白話覆蓋(墨子 gold cov 0.21 其實 grounded)+ 自然 decline≠固定句閉集 + guard_attribution 誤殺點書名 decline;**修 S5(執行層、生產 guard 一字未動)**:grounding floor 0.6→0.3+verbatim-substring 路徑+decline/refuse 豁免 grounding(誠實由 guard 管),自測防偽仍在(IC 0.9987 編造/白話外插皆 FAIL);(d)**usage 決策(用戶拍板 B)**:171 對 pilot 足夠→**bank、不重跑 S4**(省 1.6M usage),teacher-prompt 對齊生產 guard(固定句 decline+逐字片段 ANSWER+不點書名)留給未來 3k 放量。**未 commit**:advisor_distill_generate_questions.py(_embedded_works)+ advisor_distill_validate.py(S5 修)。**workflow args 傳陣列失敗教訓**:巢狀陣列 args 到 script 變 undefined→改把批次 id 寫死進 script literal。

## 2026-07-07/08 續:L6 關聯圖 + 預測階段3 裁決 + Qdrant 階段0 引擎(封存 `qdrant-stage0-l6graph-20260708`、commit fe8b4cb)
- **股市預測階段3(ensemble/lambdarank)裁決=不提拔**:portfolio.py 加 `ENS_ridge_gbdt`(等權 rank-average Ridge+GBDT)。實測 H60 LO since2014 淨 Sharpe **1.046 < Ridge 1.197**(Calmar/回檔亦全輸;GBDT 單獨 1.009)→ #14 **Ridge 保持現任、模型側飽和坐實**(前沿在 horizon/宇宙/執行非模型)。lambdarank 需 per-panel group(`_fold_xy` 未給邊界)標後續、不做假近似。與 [[prediction-headline-undeflated]] 一致(釘地板非換模型)。
- **L6 跨學說關聯圖(整合、非重造)**:`src/augur/knowledge/concept_graph.py` 唯讀查既有 affinity——`related_thinkers`(group_affinity tfidf_cosine_counts、count-based 同語言非 e5)/`related_terms`(term_affinity npmi)/`cooccurrence_evidence`(concordance 交集→knowledge_sentence char_range 逐字證據)/`term_coverage`(in_vocab vs in_corpus_stats 誠實)。**教訓**:計畫 v1 基於不完整現況實查(漏查 affinity 已存在 296 萬對/group_affinity/derivation_method no-AI CHECK 閘)提議重造→ 4 鏡對抗 workflow(wioqo9d5w)抓 REVISE→v2 據實重寫(reports/augur_knowledge_cross_doctrine_graph_plan_20260707.md)。**L6 machinery ~80% 早已建好**;no-AI 靠 method_key FK→knowledge_derivation_method(CHECK counting/closed_form_stat/string_rule/sql_join)結構封死、跨語沿用 count-based 不引 e5。term-level junk(仁↔今天)待精修(basis_n 是邊際計數非 pairwise)。
- **⏸ 清 ERP boilerplate 已暫停(2026-07-08 用戶 pivot 只做股票預測)——但關鍵發現先存**:ERP `權限設定檔%` boilerplate 29,121 句(19.2%、全 distinct 但結構近重複「權限設定檔 CLASS|程式碼:about,accept,cancel,controlg…」通用 UI 動作詞);**verdict 建議的「retrieval 加 `NOT LIKE '權限設定檔%'` 過濾」實證無效**——`knowledge_sentence_embedding` 是 **HNSW 索引**(`idx_sent_emb_hnsw`),HNSW 先取最近 ef 候選再 post-filter,採購單/會計科目/折舊鄰域全 boilerplate → 過濾後 **rowcount=0**(比不濾更糟);**強制 `enable_indexscan=off` exact scan 證真內容在(`採購單單頭·欄位 ta_pmm02:電話` 0.895 高分相關)**。**正解=刪 29,121 boilerplate 嵌入(移出 HNSW 索引)+ DB-driven `knowledge_lowvalue_pattern` 表(#29b)+ embed_knowledge low-value 閘**,非 retrieval post-filter。**另:HNSW post-filter 陷阱也影響 production `retrieve_items` 的 domain 過濾(部分解釋 en→erp 失敗)——獨立更大議題(pre-filter/partial index)**。恢復時 ttai-retrieval-verdict #1 建議須更正。
- **Qdrant 遷移階段0 引擎(建+全驗、讀取仍 pgvector 不 cutover)**:計畫 `reports/augur_vectorstore_qdrant_migration_plan_20260707.md`(§3.6 批次增量)。**決定保留 pgvector**(GPU 加速嵌入/LLM 非 ANN 搜尋);Qdrant 為未來可拋棄影子索引。三件:`vectorindex.py +QdrantIndex`(照抄 MilvusLiteIndex 五方法封閉介面、lazy import qdrant_client 1.18〔`search` 已移除用 `query_points`〕)+`scripts/migrate_qdrant_sync_ddl.py`(qdrant_sync_state 帳本、CLEAN 反差欄)+`scripts/export_qdrant_index.py`(CLEAN 命門匯出、游標 resume、雙向 anti-join 對帳、COUNT/PKSET/SELECT 共用同一 CLEAN 述詞 #12)。**CLEAN 命門=關鍵**(既有 export_milvus 缺它=洩漏):匯外部前經 corpus.py 閘(works review_flag=false∧corpus_class=literary;items license∈白名單∧entity_type∈語意准入∧`access_scope='public'`)。**對真 embedded Qdrant 全驗**:items/en 15,718+works/zh 33,314=49,032 向量、對帳差=0、重跑冪等 upsert 0、pk 枚舉自驗 len==count。**CLEAN 擋 151,849 私有 ERP**(owned_local⇒local_private 憲章 v1.36.0):items/zh 147,196 全私有+items/en 4,653 私有全擋、只匯 public/cc-by(實查 access_scope 分佈零誤殺)。`~/qdrant_local` 194MB=驗證產物(embedded >20k 點慢、正式用 Docker/Cloud)。**pgvector 3 嵌入表~48 萬向量、e5-small 384-dim/HNSW**。

## 2026-07-08 advisor 接真預測端到端打通(commit 883789d;承持續再驗證 harness 誠實地板)
選股題 → advisor surface 真 picks(prediction_values 34 檔)+ 最誠實 caveat(未過 deflation/薄 edge/系統建議人決策)、guard pass、真 ollama 驗過。**第 5 次現況救場**:advisor 接真預測(D4)**早已 wire**(`build_prediction_payload`+`picking_intent` 路由+serve_advisor `picking_payload_fn`);本 session 增強 caveat 接 harness 地板(revalidation_baseline deflated 0.265/0.070+revalidation_verdict deploying_unestablished)+修過時 survivorship(下市≈0 閉環/incumbency −16%)。**端到端實測(重啟服務 #7 http.server 不熱更+真 ollama)揭 3 既有問題致選股 decline、逐一修**:①**prompt 污染**——選股題 citations=0→舊 `has_context=False`→誤加知識題「無內容就 decline」指示→LLM 自說「知識庫無此內容」;修=`has_picks` 即 context+選股專屬 kind_hint(picks 是答案不 decline/數字白名單逐字勿湊整/caveat 改述不加引號/不下買賣)。②**caveat 自敗**——我加的 caveat 嵌近似數(76%/0.26/~1.2/−16%)非白名單、LLM 轉述被 guard 攔;修=caveat 改 qualitative、精確值留 validation。③**guard 負號 bug**——`\d+\.\d{2,}` 掉負號→正確 net_maxdd「-0.1392」抽成「0.1392」≠白名單誤攔;修=`-?\d+\.\d{2,}`(移 false-positive、不放行編造=更精確非更鬆、22 測無回歸)。**教訓**:(a)診斷 LLM decline 必看 raw 生成+guard issue 逐項(自 decline vs 數字 vs 引號三源);(b)guard 嚴格×小模型(qwen3:8b 7.7GB RAM 偶 OOM `unexpected EOF`、偶誤抄 symbol)=多重 decline 源須逐層剝;(c)餵 LLM 的 caveat 勿嵌近似數(=self-defeat)。GPU/更大模型或 Gemini(.env 有 key)可提升可靠度。**本輪預測部署層全鏈打通**:predict_asof(投組+風控)→harness(deflated 地板+兩軌三態判停)→advisor(真 picks+誠實 caveat surface 給用戶)。

**2026-07-08 收尾 Tier 1「誠實+整合完整性」(commits 0a9a158+f4dec71)**:①**augur_predict DB 隔離硬閘閉環**——第 7 次現況救場:role **早已 apply 且隔離正確**(philosophy/knowledge/chat SELECT 拒、feature/prediction 准/可寫);本批補完善(WRITABLE 補 harness 記錄表、role 已存在時跳過 ALTER ROLE〔**augur 非 superuser 無權改**、原 abort 致 grants 沒跑〕僅 refresh grants、dry_run 表名加引號保大小寫)+ **新機械隔離測試** `tests/test_predict_role_isolation.py`(素養層取樣 SELECT 一律拒=任一准 FAIL、預測管線准、輸出可寫;3 測 PASS)——與 import_isolation(AST 靜態閘)成雙閘、**兩閘皆有測試**、隔離從「約定」變機械測試。debt c DB 閘閉環;pipeline 活化(連 augur_predict)為 defense-in-depth 運維步、未做(現 import-gated+parameterized query 已安全)。②**廣宇宙定為主誠實地板**(拍板細化兩者並存)——advisor `build_prediction_payload` caveat 反轉:廣宇宙(當下可算、incumbency 修正、deflated ~0.07)為主地板、全史齊 1.20 降對照上界;部署 picks 仍全史齊、harness 軌B 仍追部署 cell、但對外誠實 headline=廣宇宙。**未竟排序**(誠實):Tier2 advisor 接 Gemini(qwen3:8b 7.7GB OOM)/Tier3 deflation 多seed·成本realism·H120訓練/Tier4 軌B 實戰驗證·H120 定論(卡資料~季/~5年)。真天花板=資料累積+硬體,非碼。

**⭐2026-07-08 Tier2 外部 Gemini 拒用+入憲(憲章 v1.36.0→v1.37.0、commit 5ed9645)**:擬接 Gemini 解 qwen3:8b OOM→**grounding 揭治權命門**:advisor 把檢索到之 citations(**可能含 owned_local 自有 ERP 私有內容**)組入 prompt 送 LLM,Gemini 為外部=私有外流 Google=違憲章 v1.36.0「owned_local 永不離本機」→我停下攤開(#26 碰治權護欄)→**用戶拍板「不可用外部 Gemini、入憲」**。已建之 `advisor/gemini.py` 移除(未 commit 無痕、backend 碼正確〔gemini-2.5-flash 煙測✓、2.0-flash 配額耗盡 429、1.5 deprecated 404〕但拒用)。**入憲 v1.37.0**:第三部「LLM 可替換接縫」加隱私上限——**advisor llm_fn 必須本機推理、禁任何外部 LLM 服務(Gemini/OpenAI/Anthropic API/雲端);LLM/嵌入接縫可換但只能換本機模型;可靠度不足之解走本機路徑(更大本機模型/GPU/量化)不得以外部 LLM 換穩定度**。屬 owned_local 鐵律之機械延伸(推理層)、非新原則→原則精華維持 v1.7.1、僅升憲章;跨檔同步(原則精華5行/README/檔名 rename)。**教訓:接外部服務前必查「prompt 會不會含私有內容」——RAG advisor 的 prompt 含檢索結果、外部 LLM=私有外流**。**故 advisor 可靠度只能靠本機(GPU/更大模型),不能靠雲端 LLM。**

**⭐2026-07-09 as-of 全系統凍結入憲(原則精華 v1.7.1→v1.8.0 / 憲章 v1.37.0→v1.38.0 / CLAUDE v1.20→v1.21、commit 5069a62)**:用戶 directive「全系統股市資料以 as-of 2026-05-31 為期限、整體系統完美後才接最新資料、請入憲」。**原則精華「資料完整性判準」增 FREEZE 子條**:as-of 2026-05-31 亦定為**現階段全系統資料期限凍結**(feature/universe/model/validate/advisor 皆以此凍結快照開發驗證=**develop-on-frozen-snapshot**、可定案可複現);**接入最新資料明文延後至系統完美後**(含 live 再驗證/軌B 新資料追蹤/增量 sync),此前**不追新資料、不以「資料過期」為缺陷、不為此掛排程**。屬既有 as-of 判準(v1.9.1)之擴展、**首次連動原則精華升版**(判準內容實質擴展非僅憲章承載)。用戶指定併原則精華 v1.8.0〔非 v1.7.2〕+ 加 CLAUDE 交叉引用 + 精簡措辭;跨檔一致全過(檔名 rename、README 凍結狀態、0 殘留)。**⚠ 路線重定:B(軌B live 新資料追蹤自動化)正式歸「系統完美後」未來階段**——grounding 實證軌B 現無新資料可驗(harness 已建但止於 2026-05-31、無排程、raw 價僅到 2026-06-17、下個可評估 panel ~2026 末);**C(H120 部署評估)仍成立**(凍結快照上做、切合「先完美系統」)。另 memory 記的 v1.8.0 待拍板項(憲章#20索引行歸家)**未併入**、仍待用戶就內容拍板(我明示不靜默混入)。

**⭐⭐2026-07-09 完整現況 handoff 快照(跨機接續 SSOT=git 內 `HANDOFF.md` 已完整重寫;memory 機器本地不隨 git、換機須複製 ~/.claude 或讀 HANDOFF.md)**:
- **git**:HEAD=`5ad038f` 全 push;工作區乾淨。本 session commit 鏈:491e595(H120 eval)→307e0c1(敘述磨版)→5069a62(FREEZE 入憲)→c3fedd9(建構理解 v1)→7d5e460(omniscient 計畫)→efc689b(短 horizon 計畫)→12a8b17(計畫完整性 v1.39.0 入憲)→bf0a0d7(2 計畫回補 schema)→5ad038f(建構理解 **v2 對抗審查**)。
- **治權現行**:靈魂 v1.5.0 / 原則精華 **v1.8.0**(FREEZE)/ 憲章 **v1.39.0**(計畫完整性強化「所有計畫附 schema+程式」)/ CLAUDE **v1.22**。
- **DB**:46GB(public 178 表 + ttai_import 22 表);dump=**`C:\AI\augur_pg17_20260709.dump`**(6.62GB `-Fc`、pg_restore --list 驗過);向量=**pgvector 在 DB 內**(~1.7GB)、`~/qdrant_local` 194MB 休眠可重建不需搬。服務 8399/8090/8500/11434 皆 up。
- **建構理解 v2**(`reports/augur_construction_understanding_20260709.md`、對應 memory [[augur-construction-map]]):augur=**兩半系統(預測管線+顧問知識)只被 DB+PredictionPayload 接起、import_isolation 單向強制**;對抗驗證修正第一版 8 類錯(feats_hash 死碼/per-module 非統一/philosophy 9 表/DSR N=16…)。
- **預測現況(誠實)**:全鏈+誠實四關建成、deflation 地板釘實=**headline 未過 deflation(DSR<95% 未確立)**、成本敏感度帶主地板 1.5% 成本翻負、H120 近門檻已凍結 tracked-candidate、harness LIVE(deploying_unestablished);**真天花板=資料累積+硬體非碼**。
- **⏳ 活躍計畫(plan-first 待拍板)**:**⭐統一 Master 計畫 `reports/augur_omniscient_master_plan_20260709.md`(commit b9cd5ad)= consolidate 前兩份為一條路**(軌 A know-how 管線 10 stage 全 BUILT + 軌 B 多 horizon matrix H20/H40/H60/H120+候選H80 誠實四關 + 軌 C 顧問三通道合流);唯一真新 code=向量後端 factory vectorstore.py;W1-W12 閘優先;無靈魂/憲章判準變更。子計畫①omniscient(軌A)②短 horizon(軌B)。**⭐對抗審查關鍵新 catch(前兩計畫漏、親驗)**:**F1/G6** concept_graph.cooccurrence_evidence 接進 advise() 無 clean_item_sql RBAC 收窄=逐字洩漏 owned_local(TTAI ERP)私有內容、guard 擋不到(查捏造非查授權)、破憲章 v1.36.0——`retrieval.py:190-195` docstring **自己早警告過**、既有計畫忽略;G6 收窄=接入硬 blocker。**F2/G3** make_llm_fn base= 繞過 host 閘→assert 最終 url。**待拍板**:30/60/120=日曆(H20/H40/H80)還交易日(H20/H60/H120)、接受多半薄、H120 已最強候選但保守 N=16 下 DSR FAIL 仍未確立。詳見 [[prediction-headline-undeflated]] [[investment-philosophy-framework]]。
- **教訓**:memory ~/.claude 不隨 git → 跨機真 SSOT 是 repo 內 HANDOFF.md + reports;DB 跨機各自獨立、現況一律實查(#15)。
