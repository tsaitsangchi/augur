# 風險模擬擴充計畫段——①garch_fhs 補齊＋②episode 庫五窗預註冊

> **性質**：[I] 執行層計畫段（#20；hugo 2026-07-27 拍板「①②屬執行層我可以直接做先補一段計畫」）。
> **上位**：`augur_portfolio_risk_sim_plan_20260725.md`（A 軌縮版）＋重凍結計畫甲′——**episode 重放＝主結論、bootstrap 族＝參考層**之地位不動；本段只加對照組與情境，零判準變更。
> **③（copula-t／DCC-GARCH 尾部相依）不在本段**——涉新方法揭露格式，另案計畫先行。

## ① garch_fhs 補齊（一行安裝）

- 動作：venv `pip install arch`（單套件；**安裝後驗 numpy/pandas 版本未被拖動**——llama.cpp requirements 拖壞 venv 之教訓）→ 重跑 `--run` 補 garch_fhs 列。
- 驗收：四法對照表 garch_fhs 有數（非 SKIP）；`import numpy; numpy.__version__` 前後一致。

## ② episode 庫擴充——五窗**預先註冊**（防事後挑窗；資料底實查 PriceAdj 自 1992-01-04 全覆蓋）

沿用既有慣例（`EPISODES` 人工註冊危機日曆段、確定性重放、零參數估計）：

| episode | 窗（起 → 迄） | 危機錨（註冊理由） |
|---|---|---|
| `1997` | 1997-08-01 → 1999-02-28 | 亞洲金融風暴＋本土型金融風暴全段（TAIEX 1997-08 高點 → 1999-02 低點） |
| `2000` | 2000-02-01 → 2001-09-30 | dot-com 崩跌全段含 911 谷底（2000-02 高點 → 2001-09 低點） |
| `2011` | 2011-02-01 → 2011-12-31 | 歐債危機年（2011-02 高點 → 12 月低點區） |
| `2015` | 2015-04-01 → 2015-09-30 | A 股連動＋8/24 全球閃崩（2015-04 高點 → 8-9 月低點區） |
| `2018` | 2018-10-01 → 2019-01-31 | 貿易戰急跌段（2018-10 高點 → 2019-01 低點） |

**窗一經本檔 commit 即凍結**——重放結果不好看不得回改窗（挪門柱紀律）；要改窗＝新 episode 名另註冊。

**覆蓋規則（沿用既有機械閘、預先聲明結果解讀）**：`MIN_EPISODE_W_COVER=0.70` 不動——1997／2000 窗當代 33 檔成分股多數未上市或資料未及，**「覆蓋不足 → 誠實拒跑」是合法且預期的結果，不是失敗**；屆時記入 summary（partial 不硬出數）。每一 episode 之權重覆蓋率如實揭露。

## Schema／程式（v1.39.0）

- **零新表**（沿用 `mc_simulation_run`）；`migrate_mc_method_check_ddl.py` 白名單 7→**12 值**（＋`episode_replay_{1997,2000,2011,2015,2018}`；typo 仍擋）。
- `scripts/simulate_portfolio_risk.py`：`EPISODES` dict ＋5 列（本段窗值逐字）；其餘零改動（重放引擎／四鎖／揭露原樣）。
- 註（#29b 誠實）：episode 窗屬**預註冊判準**（凍結、不增減策展）故留 code 常數，與既有三窗同慣例；若日後窗多到需策展再遷 DB。

## 驗收（精準）

1. 白名單 constraint def 實查含 12 值；
2. 五 episode 各得：落庫 summary（MaxDD／cum／覆蓋率）**或**「覆蓋不足拒跑」之誠實輸出——兩者皆合格；
3. 無參數現況表列出全部已跑 run；`--compare` 參考層四法齊；
4. 主結論地位不變：輸出仍標 episode 為主、bootstrap 為參考、`is_simulation=true` 四鎖硬綁。
