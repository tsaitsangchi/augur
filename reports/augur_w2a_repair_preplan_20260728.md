# W2-a 修復預案——own_daily_rolling 校準鏈修法（條件觸發、本檔零 code 變更）

> **性質**：[I] 乙項交付（hugo 2026-07-28「順手乙」）。**預案非執行**：依預註冊 `audits/ARENA-WATCHLIST-PREREG-20260726.md` W2 判準，**下一獨立批（~08-03 標籤到期）重排百分位仍 >95 才觸發修 code**；本檔把修法預先設計凍結，屆時即刻執行、不現場設計。若下一批 <80 記反證、本檔封存不用。
> **病因 SSOT**＝`reports/arena_own_daily_rolling_probe_20260726.md`（排查已結案：無反向 bug；過度自信機制 CONFIRMED＝isotonic 尾部裁剪 × 逐股切分校準集）。

## 一、修法（對症、最小侵入）

| # | 修 | 對應病因 | 內容 |
|---|---|---|---|
| R1 | **校準集改時間切分** | 病因 (2) 主 | `cut` 由「逐股堆疊後 85/15」改為**按 panel/時間後 15% 窗**（全股票皆入校準、只切時間）——校準集見全宇宙、尾 bin 樣本量恢復 |
| R2 | **輸出護欄 clip [0.02, 0.98]** | 病因 (2) 尾部釘死 | isotonic 保留（不換校準法＝不引入新自由度），輸出後夾——0.9896 級極端自信不再可能；ε=0.02 為工程護欄非統計宣稱，落 code 註解 |
| R3 | **early-stop 驗證切分改時間序** | 附帶觀察 (4) | `early_stopping` 內部驗證由隨機混切改時間尾切（訓練窗內、非前視修復，屬衛生） |

**明確不修**：特徵集／模型類型／horizon（反排序 AUC<0.5 經排查定位=1 cluster 觀察非 code bug——動特徵=改參賽者本質，須另案人簽）。

## 二、身分與帳本紀律

- 觸發後修 `src/augur/arena/adapters.py` `OwnDailyRolling`，`direction_arena_candidate.code_sha` 隨 commit 更新；`model_key` 不變（預註冊 W2-a 明文「修 code 重跑」＝同隊修復非新隊）；修復日起之出單在計分板上以 code_sha 斷代可稽。
- **不回改已結算列**（#12 不 hand-patch）：舊 p_up／已結算成績原樣留檔；修復效果只看修復後批次。
- 修復 commit 須引用本預案＋觸發批之 `verify_arena_watchlist.py --run` 輸出（觸發證據）。

## 三、驗收（機械）

1. selftest：時間切分正確性（校準集=最後 15% panel×全股）＋clip 邊界；
2. 修復後首批出單前 dry：`p_up` 分佈 `max ≤ 0.98、min ≥ 0.02`、不再單側長尾（對照排查 §三之形狀）；
3. 修復後兩個獨立批：W2 儀器重排百分位——**目標 <95**；若連兩批仍 >95 → 修法無效，升級「模型重設計」另案人簽（停損）。

## 四、觸發流程（屆時零裁量）

```
~08-03 新批結算 → verify_arena_watchlist.py --run
  ├─ W2 百分位 >95 → 依本檔 R1-R3 實作+selftest → commit(引本檔+證據) → 重啟出單鏈
  ├─ 80–95 → 不觸發、不反證,續觀察(預註冊原文範圍外=不動)
  └─ <80  → 記反證 1/2,本檔備而不用
```
