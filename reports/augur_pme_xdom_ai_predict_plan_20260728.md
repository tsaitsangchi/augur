# PME-XDOM-AI-PREDICT 異域進化軸短計畫 [I]（2026-07-28）

* **性質**：[I] plan-first 短計畫（CLAUDE #16／#20）；**不創設 [N]**  
* **母計畫**：`reports/augur_pme_cross_domain_evolution_enable_plan_20260728.md`（`PME-XDOM-YES` 已開通異域寫側）  
* **對照探針**：RKI `RKI-AI-PREDICT-EVO`／`RKI-AI-PREDICT-EVAL`／`RKI-FP-AI-ITER`／`RKI-FP-PREDICT-ITER`＝**檢索探針**；本碼＝允許**寫進 investment school 假說／map**（文獻可溯源），**不是**顧問-only  
* **姊妹**：SUNZI-MGMT S01 CLOSED＝`audits/PME-XDOM-SUNZI-MGMT-S01-CLOSED-20260728.md`（範式對齊）

### Steward 拍板欄

| 欄 | 內容 |
|---|---|
| **日期** | 2026-07-28 |
| **狀態** | ✅ **已拍**；同輪執行＝**S0＋S1**；S3／S4 另候 |
| **拍板碼** | `PME-XDOM-AI-PREDICT`＋`GATE-keep`＋`FZ-keep`＋`NHC-keep` |
| **效力** | 開通 **AI 模型進化 × 投資／預測模型進化** → PME 假說鏈；對齊 `PME-XDOM-YES`；範圍自 SUNZI-MGMT **擴**到本軸 |
| **留痕** | `audits/PME-XDOM-AI-PREDICT-APPROVED-20260728.md` |

---

## 0. 一句結論

把「AI／統計學習／金融 ML **可證偽方法論概念**」（OOS／正則／集成／迭代回饋／第一性拆解）人撰成 **investment** school 原則＋`principle_factor_map`，走既有 G-PROM／G-ECON 全鏈——**仍**禁 AI 造原則、禁降閘、禁 FinMind／FRED、禁 hardcode 專答、禁自動下單、**不**把 RKI 探針命中當過閘憑據。

---

## 1. 範圍／非目標

### 1.1 範圍（近程）

| 項 | 決定 |
|---|---|
| **閉環** | AI 模型進化 × 投資／預測模型進化（文獻橋） |
| **載體** | 新建（或既有）`domain='investment'` school（建議名 `ml_predict_evolution`）→ principle → factor_map |
| **進** | 可核 citation 之人撰假說；feature ∈ 庫內 `feature_values`（可對映桶） |
| **注記軸** | 可選 `principle_domain_map.domain='ai_ml'`（**非**量化資格；I8） |
| **provenance** | `xdom_loop=ai_predict` |
| **近程階段** | **S0＋S1**；S2 缺特徵僅列帳；**S3／S4 未開** |

### 1.2 非目標（硬紅線）

| 不做 | 理由 |
|---|---|
| 降閘／ECON-only／SKIP＝PASS | `GATE-keep` |
| FinMind／FRED sync | `FZ-keep` |
| 領域 hardcode 答案／專答樹 | `NHC-keep`；策展住 DB |
| AI 生成原則／citation／摘要入庫 | #1；`source_type` CHECK |
| ERP dump／太陽能閉環自動 SEED | 仍鎖（另拍） |
| RKI 探針 cite 率當 G-PROM 通過 | 正交；探針≠map 資格 |
| embedding／knowledge 列當 feature | A.16；soul-vs-raw |
| 自動下單／可交易／確立級 | G-NOEXEC；門二未過 |
| 本輪跑 S3 local-gates／S4 重訓 | 待 `開 PME-XDOM-AI-PREDICT-S3` |

---

## 2. 分階

| 階段 | 內容 | 本輪 |
|---|---|---|
| **S0** | 範圍釘死＋候選假說 3–8＋`feature_values` 三桶；明示拒 SEED | ✅ |
| **S1** | 文獻策展 school／principle／map／可選 domain_note；`--selftest`；冪等 | ✅ |
| **S2** | 缺特徵零 API 可建者另令；本輪只列帳 | ⏸ |
| **S3** | local-gates；雙綠∧kill clear 才 APPLY | ❌ 待開 |
| **S4** | prodset 變動後重訓 | ❌ 待開 |

---

## 3. Schema／程式（最小）

| 物件 | 角色 |
|---|---|
| `philosophy_school`（investment） | 可新建 `ml_predict_evolution` |
| `philosophy_principle`／`philosophy_source` | 人撰＋可核 citation |
| `principle_factor_map` | feature＋direction；provenance `ai_predict` |
| `principle_domain_map` | 可選 `ai_ml` 注記 |
| **新** `scripts/curate_pme_xdom_ai_predict_map.py` | S1 dry-run／`--apply`／`--selftest` |
| 既有 evolution／P2H | **本輪不呼叫** |

---

## 4. 驗收（近程）

| ID | 條件 |
|---|---|
| V-SCOPE | 書面範圍＝AI×預測進化文獻橋；ERP／solar 拒 |
| V-CHAIN | 每 map 掛 investment principle＋可核 citation |
| V-AI | 無 `ai_generated` |
| V-NHC | 無領域專答 hardcode；SEED 住腳本／DB |
| V-GATE／V-FZ | 未跑閘、未降閾、零市場 API |
| V-ORTH | 不以 RKI／顧問命中率宣稱過閘 |

---

## 5. 修訂

| 日期 | 說明 |
|---|---|
| 2026-07-28 | 初版：Steward `PME-XDOM-AI-PREDICT`；對齊 SUNZI S0／S1 範式；S3 另令 |
